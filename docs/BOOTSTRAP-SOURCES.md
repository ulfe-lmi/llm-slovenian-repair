# Source identity and role distinction

Root PLAN/ARCHITECTURE/CRITICAL preserve supplied bytes at bootstrap. PLAN protected;
architecture later evolves only under controlled current-source/projection changes;
CRITICAL grows by exact authorized EOF append. Historical docs/bootstrap snapshots
are immutable reference inputs, not competing current law. Original AGENTS is archived
as BOOTSTRAP-GENERATOR.md so no bootstrap-role instruction is inherited there.

Source mapping and exact hashes: [lock](../oap/bootstrap-sources.lock.json).
Source snapshots retain original-context links/citations unchanged; such historical
links are not generated active navigation or assertions of current web availability.
Current governance [map](../oap/governance/DISTILLATION-MAP.md) distinguishes source
doctrine from project choices: router/budgets/paths/OK/SELF/suffix/update grammar.
No snapshot checksum proves authorization, semantic fidelity or product acceptance.
