# Future evaluation contract

Software synthetic/fake tests prove deterministic boundaries, not language quality.
Later approved real target outputs and correct-text controls need human labels for
actual error, acceptable alternative, style preference and required wider edit.
Keep source/model settings/rights/review status; freeze calibration/held-out splits
and prevent duplicates/leakage. Qwen is not sole final judge of its own changes.

Compare original, no-edit CPU detector, direct proofreading benchmark and constrained
method; benchmark rewriting is not production architecture. Report precision with
evaluated-edit count, harmful edits per originally correct words, unnecessary style,
detector vs repair coverage, domains/sample counts/uncertainty, separate main/review
usage and latency/queue/CPU memory. Zero observed harm in a small clustered sample
does not prove zero risk. ~99% precision/one harm per 10k are goals, not authorization.
Show useful measured benefit while preserving conservative gates, even at low coverage.
No linguistic experiment, annotation or Qwen invocation occurred during bootstrap.
