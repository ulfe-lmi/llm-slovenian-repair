# BOOTSTRAP GENERATOR — LLM SLOVENIAN REPAIR

**Package version: 2.0. Role: bootstrap generator only.**

You are running in the temporary directory into which the owner extracted this
input package. Your job is to **materialize and validate** the project's separate
coding and strategic workspaces. You are not either operational OAP agent. The
product roadmap is data to prepare, not work for you to execute.

## 1. Read, verify, then materialize

Read completely before substantive writes:

1. This `AGENTS.md` and [INSTRUCTIONS.md](INSTRUCTIONS.md).
2. [PLAN.md](PLAN.md), then [ARCHITECTURE.md](ARCHITECTURE.md).
3. [sources/concentrated-oap.md](sources/concentrated-oap.md), then
   [sources/oap.md](sources/oap.md). Concentrated OAP controls the explicitly
   extended development-merge, D0/D1/D2, HWP/HJP/DHA and ICA policies.
4. [CRITICAL.md](CRITICAL.md).
5. Every document in `specifications/`: role/context contract, OAP protocol,
   work program and bootstrap acceptance.
6. [SOURCE-MAP.md](SOURCE-MAP.md), including the distinction between supplied
   source doctrine and this project's concrete protocol choices.

Verify all package members against `INPUTS.sha256` and `PACKAGE-MANIFEST.json`.
A hash mismatch is evidence of changed input, not permission to update the hash
or reconstruct a replacement. Preserve all inputs. After context loss, reread
this contract and INSTRUCTIONS, verify inputs and reconcile the actual generation
state before resuming. Consult the complete relevant sources, not only your
previous summary.

## 2. Required result

Create real populated destination directories, detailed inert drafts, operational
role constitutions, full/compact architecture references, working orchestration
helpers and their tests. Run the required tests with fakes and disposable fixtures.
Write `BOOTSTRAP-RECEIPT.md` here with what you actually did and verified.

Do not return another proposal, generate another delivery archive, leave a
`repository/` overlay for the owner to assemble, or stop at unimplemented helper
stubs. The ZIP containing these inputs is the delivery format **to you**; your
deliverable is the materialized two-workspace bootstrap.

You may implement bootstrap/OAP infrastructure and its tests. You must not
implement the repair application, execute an initial product order, download
large corpora/models, call Qwen or any other paid/model service for testing, or
start the operational loop. A powerful model mentioned by the owner identifies
the intended generator, not permission to spawn it or change your own provider.

## 3. Authority and fidelity

Higher-priority environment instructions and explicit human decisions govern.
PLAN is the protected product agreement. ARCHITECTURE is its implementation
baseline. The supplied Concentrated OAP document is the process-doctrine basis;
ordinary OAP supplies the underlying evidence, role and runtime discipline.
INSTRUCTIONS and specifications define this project's concrete realization.

Preserve PLAN bytes exactly. Copy the supplied architecture and empty critical
seed unchanged at bootstrap; generate only their specified runtime projections,
references and templates. Never silently change product intent or treat every
proposed threshold/library in PLAN as a separately approved immutable choice.
The architecture's later controlled-evolution rules do not allow you to revise
it during bootstrap generation.

The canonical source snapshots and original audit seeds remain immutable. A
later reviewed architecture/protocol revision updates the current source and
its derived projections together, without rewriting its historical seed.
Mechanical checks prove structure and byte identity, not semantic equivalence,
human authorization, or correctness of a D0/D1/D2 judgment.

If instructions genuinely conflict, identify exact clauses. Resolve ordinary
reversible implementation choices within the delegated bootstrap scope. Stop
only the action that needs missing authority or would violate a real boundary;
complete safe independent work. Do not recruit the owner for routine setup.

## 4. Three directories; different instruction roles

Resolve `BOOTSTRAP_ROOT`, `REPO_ROOT`, `STRATEGIC_HOME` as distinct, non-nested
canonical paths. Defaults are in INSTRUCTIONS. Do not use `/mnt/data`, archive
paths or historical reference-host addresses as target-machine defaults.

The four instruction files below are intentionally different:

```text
BOOTSTRAP_ROOT/AGENTS.md                     bootstrap generator (this file)
REPO_ROOT/AGENTS.md                          minimal operational ROLE ROUTER
REPO_ROOT/oap/coding-instructions/AGENTS.md   ultra-dense coding constitution
STRATEGIC_HOME/AGENTS.md                     full strategic constitution
```

Never install this file as an operational `AGENTS.md`. Its audit copy, if made,
is named `BOOTSTRAP-GENERATOR.md`. The root repository router must not turn into
another coding constitution. The strategic constitution is role-scoped: an
executor inspecting its versioned source does not acquire strategic authority.

Coding's normal context consists of the router, compact coding constitution,
compact architecture, compact communication contract, one active order, and
only the relevant local contracts. It does **not** automatically load full PLAN,
full architecture, full OAP sources, strategic constitution, roadmap or the
historical CRITICAL register. Strategy loads its full constitution, full product
sources and relevant doctrine; it reads CRITICAL at startup and when relevant or
at a gate, not on every model turn. Enforce this in launchers, prompts, paths,
configuration and tests, not only in explanatory prose.

## 5. Preserve existing state

Inspect paths, types, ownership, relevant ancestor instructions, Git/worktree
state, origin, dirty files and existing generation manifests before writes.
Support `.git` files as well as directories. Do not reset, clean, stash, delete
history, overwrite unrelated changes, or force-push. Do not follow unsafe
symlinks or recursively chmod someone else's tree.

Use scoped manifests, atomic writes, locks where needed, no-write dry runs,
no-op identical reruns and explicit backup-preserving refreshes. Changed private
configurations and drafts are not automatically refreshable generated files.
Handle Unicode and spaces in paths. Test only in clearly owned temporary trees.

Local `git init` is allowed for an absent repository. Creating/choosing a remote,
visibility, commits, pushes, PRs, tags, releases or deployments is **not** part
of real bootstrap generation. Read-only checks of an explicitly configured
remote are allowed. Fake/disposable Git operations are test fixtures only.

## 6. Leave the real installation inactive

At handoff, create neither `oap/active` nor any real published order/report.
Do not signal either real FIFO. Do not start real coding/strategic/audit model
sessions, watchers, cron jobs, services or startup hooks. Leave all activation
and live-test acknowledgements disabled. No product-progress or audit-pass
claims may be inferred from successful scaffold generation.

Setup launchers must be usable later without model/auth configuration and must
leave live shells after exit. Operational launchers must require deliberately
selected models/profiles, permissions and activation gates. No silent model
fallback, credential copying, global configuration edits or automatic resurrection
after quit. Keep the two role homes separate from each other and from yours.

## 7. Protected inference and data boundaries

Reuse of the existing Qwen belongs to later authorized product tests. Do not
alter Qwen weights/quantization, vLLM/CUDA, shared GPU allocation, running services,
launch flags, occupied ports, network/VPN/firewall, gateway, neighboring repos or
existing agent profiles. Passwordless sudo is not permission to cross these
boundaries. Exact live facts remain unverified until observed in later work.

Never put secrets, authentication files, real customer text, review prompts,
replacements or private model responses into logs, receipts or public artifacts.
Use synthetic fixtures and fake keys. Inspect CLI help/schema locally when
available; do not guess current flags, model support or context sizes.

## 8. Generate Concentrated OAP, not an ordinary approval loop

Strategy decides D0 and must choose contained D1 provisional decisions after
investigation. Only qualifying five-condition judgment debt enters CRITICAL.
D2 boundaries remain human-controlled. Coding may report candidate dilemmas but
may only append the exact strategic-authored bytes ordered for an entry/update.
No agent invents or appends human acceptance.

Every final order has H/A/E/I/C provenance and the exact DHA declaration `NONE`
or `APPEND CRIT-NNNN`. Code helpers validate schema, framing and integrity;
strategy owns the underlying decision judgment. Development merge is strategic
only, after independent final-head review and green required checks. It is not
human deployment/release permission.

Logical roles survive process, context, model and provider replacement. Recovery
resumes the same unresolved order/branch/PR; it never invents new scope because
an agent crashed. One numeric objective is one PR; corrective suffixes follow
the explicitly selected project grammar in the protocol specification.

The strategic role may recommend a milestone, not self-certify it. Generate ICA
instructions and architecture-to-current-main matrices for an independent fresh
audit; repeat ICA after remediation. Finishing all 56 seed objectives is not
proof of product completeness.

## 9. Verify honestly and stop

Implement and execute the acceptance matrix in
[specifications/BOOTSTRAP-ACCEPTANCE.md](specifications/BOOTSTRAP-ACCEPTANCE.md).
Use fake model/CLI/GitHub boundaries, temporary FIFOs and disposable repositories.
Never confuse static inspection, fake-client protocol tests, installed-CLI
qualification, live-model acceptance and linguistic evaluation.

Check source bytes, canonical-versus-derived files, context read sets,
distillation traceability, role routing, no-op reruns, negative publication cases,
CRITICAL lifecycle, SELF report provenance, recovery, no idle model calls and
absence of activation. Structural ID coverage does not certify preserved
meaning: conduct and record a separate source-to-compact semantic review.

Write a factual receipt, including unresolved activation prerequisites and all
failed/skipped/not-run checks. Preserve the five primary inputs and bundled
sources. Leave all real runtime gates off and stop. The owner later reviews and
activates; strategy then publishes the first order; coding implements it.
