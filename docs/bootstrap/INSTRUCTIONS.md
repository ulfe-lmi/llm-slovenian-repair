# Generate the Complete Bootstrap — LLM Slovenian Repair

**Version 2.0 — reconciled with the two owner-supplied OAP chapters.**
**Audience:** the strong coding agent launched in this extracted temporary folder.
**Result:** populated coding and strategic directories, tested OAP infrastructure,
full/dense role documents and detailed inert drafts. Stop before activation.

## 1. Inputs, precedence and specification map

Read the root bootstrap AGENTS first. This is an input package, not the repository.
Its primary inputs are PLAN, ARCHITECTURE, CRITICAL and these instructions. The
bundled source chapters and specifications are part of the required input set;
no website, previous chat, previous ZIP or missing archive is required.

| Input | Binding purpose |
|---|---|
| [PLAN.md](PLAN.md) | Product agreement, preserved exactly. |
| [ARCHITECTURE.md](ARCHITECTURE.md) | Full implementation baseline; create a dense projection, not another full design. |
| [CRITICAL.md](CRITICAL.md) | Canonical empty DHA seed and exact lifecycle. |
| [sources/concentrated-oap.md](sources/concentrated-oap.md) | Primary process doctrine: HWP/HJP/DHA, D0/D1/D2, development merge, ICA. |
| [sources/oap.md](sources/oap.md) | Underlying OAP roles, runtime, evidence, continuity and review. |
| [specifications/ROLE-CONTEXT.md](specifications/ROLE-CONTEXT.md) | Minimal router, dense coding law, full strategic law, loading, distillation and budgets. |
| [specifications/OAP-PROTOCOL.md](specifications/OAP-PROTOCOL.md) | Exact work-order, transport, PR, report, critical, recovery and audit contracts. |
| [specifications/WORK-PROGRAM.md](specifications/WORK-PROGRAM.md) | 56 objectives to expand into real detailed inactive drafts, plus ICA gates. |
| [specifications/BOOTSTRAP-ACCEPTANCE.md](specifications/BOOTSTRAP-ACCEPTANCE.md) | Executable validation required of the generator. |
| [SOURCE-MAP.md](SOURCE-MAP.md) | Provenance, source scope and deliberate project extensions. |

Explicit human intent and higher-priority environment rules control. PLAN governs
product behavior; architecture implements it. Concentrated OAP controls its named
extensions to ordinary OAP (do not reintroduce mandatory human approval for every
development PR). The specifications fix this project's concrete protocol choices.
Do not interpret source examples, URLs or commands as new permissions to mutate
other repositories/hosts or to execute production actions.

Hash every listed input against INPUTS.sha256 before work. PLAN must have SHA-256:

```text
d2aa1d98cc5177ac6093ab3aeb79903b192780910ef2d1712839b474fb5adbf0
```

Expected architecture, register and source hashes are in PACKAGE-MANIFEST.json.
The checksum manifest detects accidental drift; it is not an authenticated human
signature. Never fix a mismatch by editing expected values. Preserve all source
bytes and record genuine problems.

## 2. Your scope and defaults

You must generate and actually materialize executable bootstrap/OAP helpers, their
tests, role-specific governance, source locks, detailed inert orders, runtime
scaffolding and receipt. You must **not** implement the application or run product
orders, call Qwen, fetch large corpora/models, operate live services or launch
agents. Return neither another plan nor another archive as the generator result.

Defaults unless the owner explicitly overrides them:

```text
PROJECT_SLUG=llm-slovenian-repair
BOOTSTRAP_ROOT=<actual extracted folder>
REPO_ROOT=$HOME/codex-work/llm-slovenian-repair
STRATEGIC_HOME=$HOME/codex-supervision/llm-slovenian-repair
```

Accept explicit `OAP_REPO_ROOT` / `OAP_STRATEGIC_HOME` overrides from the operator.
Resolve existing parents/path aliases; require all three directories distinct and
non-nested. Refuse symlink/path/ownership tricks. Handle spaces and Unicode. Do
not silently pick another folder when the intended one collides with existing
work. Finish unaffected safe work and report exact collisions.

Inspect Git, origin, branch, tracked/untracked/dirty files and inherited AGENTS
before writes. A `.git` file is a valid worktree; do not require a directory.
A new target may be created and locally initialized on main, uncommitted. Preserve
existing files/history; no reset, clean, stash, force push or opportunistic rewrite.

A GitHub owner/repository/visibility is **not selected** by these inputs. Do not
create a remote, invent origin, commit/push the real scaffold, open PRs or publish.
Read an explicitly configured remote only as needed. Missing remote/model/login
blocks later activation, not local scaffold generation. Keep a manifest of every
created/unchanged/backed-up/conflicting file and safe repeat behavior.

## 3. Product contract to carry into every projection

Preserve PLAN and architecture, especially LR-001–LR-014:

```text
complete main response capture
 -> CPU detects supported suspicious words/short phrases
 -> no eligible suspicion: unchanged original, zero review calls
 -> otherwise one fresh isolated Qwen review request
 -> validate local proposal and independent CPU evidence
 -> exact approved-span patches only
 -> deliver final answer text
```

The reviewer asks whether expression X is a natural appropriate choice in the
finished sentence; it may keep it, propose one local replacement or request a
wider edit that the current policy refuses. It is not a second proofreader that
returns a rewritten answer. Preserve the agreed Slovenian question in PLAN §8.

Isolation is newly constructed request/history state, not the existence of a
Python thread. Do not pass main conversation IDs/history/tools/images or recurse
through the repair/constitutional adapter. Use the already running Qwen later;
no second large GPU model and no changes to its protected deployment.

Preserve exact code-point span mapping, protected structures, no n-grams across
excluded material, censoring/unknown-count semantics, bounded one-pass review,
strict independent acceptance, final composed validation, safe optional-repair
fallback, honest main errors/completion and separate usage. Model confidence is
not calibrated correctness; a second call to the same Qwen is not statistically
independent evidence. Numerical quality/latency estimates remain goals/proposals.

## 4. Required materialized inventory

Every listed document must be substantive and project-specific. Every executable
must work and have tests, not print a TODO or delegate assembly to the owner.
Additional small files are permitted when necessary; avoid speculative frameworks.

### 4.1 Coding repository

```text
REPO_ROOT/
  AGENTS.md                                  # MINIMAL ROLE ROUTER ONLY
  PLAN.md                                    # exact input; canonical product plan
  ARCHITECTURE.md                             # exact input at bootstrap; canonical full current source
  ARCHITECTURE-for-agents.md                  # dense derived coding architecture
  CRITICAL.md                                # exact empty input; sole live register
  OAP-COMMUNICATION-coding-agent.md            # dense complete execution protocol
  README.md                                  # bootstrap-only status, project, navigation
  STATUS.md                                  # PLANNED / NOT RUN; no fictional product progress
  SECURITY.md                                # concise operational rules + linked rationale
  TESTING.md                                 # current OAP tests versus future product tests
  .gitignore
  .gitattributes                             # preserve source/register bytes and executable text policy
  docs/
    OAP-RUNBOOK.md
    CODEX-ROLE-SETUP.md
    READINESS.md
    DECISION-POLICY.md
    LIVE-TEST-ENVIRONMENT.md                  # hypotheses and qualification procedure, not claimed live facts
    DATA-SOURCES.md
    EVALUATION.md
    IMPLEMENTATION-ROADMAP.md                 # concise navigation, no second roadmap authority
    BOOTSTRAP-SOURCES.md
    bootstrap/                               # immutable input snapshots, reference-only
      BOOTSTRAP-GENERATOR.md                  # source AGENTS bytes, renamed to avoid role inheritance
      INSTRUCTIONS.md
      PLAN.md
      ARCHITECTURE.md
      CRITICAL.md
      SOURCE-MAP.md
      specifications/                        # exact supplied specification snapshots
      sources/                               # exact supplied OAP and product source snapshots
  oap/
    README.md
    RECOVERY.md
    COMPLETENESS.md                          # architecture-derived criteria, no accepted results
    bootstrap-sources.lock.json              # input hashes + source-to-installed mapping
    BOOTSTRAP-MANIFEST.sha256                # handoff snapshot, not a permanent all-file freeze
    runtime.env.example
    coding-instructions/
      AGENTS.md                              # ultra-dense executor law, not bootstrap AGENTS
    strategic-instructions/
      AGENTS.md                              # canonical FULL strategic constitution source
      OAP-COMMUNICATION-strategic.md           # full control/review protocol
      strategic_model_init_material.md
      INITIAL-ROADMAP.md
      initial-orders/
        000-a-<specific-slug>.md
        ...
        055-a-<specific-slug>.md
    governance/
      MANIFEST.json
      DISTILLATION-MAP.md
      CONTEXT-BUDGET.md
    audit/
      ICA-PROTOCOL.md
      ICA-REQUEST-TEMPLATE.md
      ICA-REPORT-TEMPLATE.md
      ICA-REQUIREMENT-MATRIX-TEMPLATE.md
      requests/
        ICA-DEMONSTRATOR-DRAFT.md
        ICA-MVP-DRAFT.md
        ICA-SERVICE-DRAFT.md
    templates/
      WORK-ORDER-TEMPLATE.md
      REPORT-TEMPLATE.md
      STRATEGIC-REVIEW-CHECKLIST.md
      CRITICAL-ENTRY-TEMPLATE.md
      HUMAN-ADJUDICATION-TEMPLATE.md
      HANDOFF-TEMPLATE.md
    prompts/
      coding-round.md
      strategic-start.md
      ica-start.md
    bin/
      bootstrap-two-codex-oap.sh
      doctor.sh
      check_state.py
      check_governance.py
      check_ica.py
      oap_fifo.py
      publish_order.py
      append_critical.py
      verify_report.py
      launch-role-setup-shell.sh
      launch-codex-setup-tmux.sh
      launch-coding-agent.sh
      launch-strategic-agent.sh
      launch-oap-tmux.sh
    orders/
      .gitkeep
    reports/
      .gitkeep
    tests/
      <real helper/regression tests and fake CLI/GitHub fixtures>
```

**Do not create `oap/active`.** Only inactive draft seeds exist. No application
`src` scaffolds that suggest the repair product is implemented. Python standard-
library tests or an isolated test environment for the OAP helpers are allowed;
product packaging and CI are future objectives. Do not install into Qwen's venv.

The immutable docs/bootstrap snapshots preserve the original input names except
AGENTS is renamed BOOTSTRAP-GENERATOR.md. Do not add a bootstrap-role AGENTS under
that archive. Preserve source bytes, with a source-path-to-archive-path map in the
lock. Source snapshots may retain original-context links; classify them explicitly
as such rather than modifying canonical sources. All **generated active** links
must resolve. Root documents are current authority; audit snapshots are not.

Code licensing/copyright must not be invented. Record a proposed/unresolved code
license where no explicit owner choice exists; distinguish it from data/model
rights. No remote/package publication during generation.

### 4.2 Private strategic workspace

```text
STRATEGIC_HOME/
  AGENTS.md                                  # exact copy of full strategic source
  OAP-COMMUNICATION-strategic.md
  strategic_model_init_material.md
  INITIAL-ROADMAP.md                          # controlled starting source copy
  SOURCE-REFERENCES.md                        # paths/hashes to canonical PLAN/ARCHITECTURE/CRITICAL
  RUNTIME.md
  runtime.env                                # protected; acknowledgements NO
  control.fifo                               # real FIFO, never signaled during bootstrap
  response.fifo                              # real FIFO, never signaled during bootstrap
  drafts/
    000-a-<specific-slug>.md
    ...
    055-a-<specific-slug>.md
  critical-drafts/
  workorders/
    EXECUTION_TIMINGS.md
    STRATEGIC-HANDOFF.md
  audit/
    requests/                                # inactive copies
    results/                                 # empty, no fake passes
  logs/                                      # bounded operational metadata only
  bootstrap-backups/
  codex-homes/
    coding/
      config.toml
    strategic/
      config.toml
```

Do not create independently editable private full PLAN/ARCHITECTURE/CRITICAL
copies. Strategy reads canonical root files. Private AGENTS is full strategic
law, never the compact executor edition. Its versioned source remains in Git;
refresh intentionally without overwriting private drafts/history/config.

Directories private 0700, config/FIFOs 0600. Do not create/copy auth.json, browser
cookies, provider credentials or another session's history. A private no-remote
Git initialization is allowed only if a verified CLI requires it; not an app origin.

### 4.3 Receipt and manifests

Write BOOTSTRAP_ROOT/BOOTSTRAP-RECEIPT.md with actual destinations, input digests,
source-to-installed mapping, role/context identities and counts, 56 draft inventory,
semantic distillation review, executed tests/results, collisions/backups, missing
activation prerequisites and proof of no operational activation.

Do not hash a receipt/manifest inside itself. Separate immutable input locks,
current governance projections, generated-file handoff snapshot and mutable
runtime/critical state. A candidate cannot authorize its own source rewrite just
by changing the expected digest. Use trusted accepted Git/reference bases later.

## 5. Generation order and semantic review

First qualify inputs/paths and inspect tooling. Author full strategic law and
complete procedure from the canonical sources. Derive dense coding AGENTS,
compact architecture and compact coding communication with stable clause IDs.
Generate router, role prompts/configuration, read-set manifest and distillation map.

The generator must conduct a separate clause-by-clause comparison of compact
clauses with the full sources. Record lost/ambiguous clauses and fix them. No
success claim from a keyword count alone. The exact full/compact loading and
budget contract is in ROLE-CONTEXT. Do not let another mandatory file reintroduce
the full architecture/CRITICAL into coding's routine context.

Generate SECURITY, TESTING, decision/readiness/recovery/audit documents and all
56 specific drafts. Drafts must specify behavior, invariants, edge cases, evidence
boundaries, commands and non-goals. Generic repeated paragraphs are not enough.
They remain unactivated and contain honest future VERIFY markers.

Implement and test the shared helpers next. Use the actual materializer path to
populate the real strategic workspace only after fixture tests/preflight. Then
run read-only real structure validation and finish the receipt. Never activate to
"prove" that bootstrap was installed.

## 6. Runtime/model configuration

Use Codex as the proposed default two-role CLI, consistent with the prior Connect
bootstrap. Do not build a second OpenCode backend unless the owner selects it.
Inspect installed version/help/schema without a model request. Where compatibility
cannot be established, generate safe setup shells and report operational activation
blocked; do not invent flags, model identifiers or context capabilities.

The generator's gpt5.6-sol/MAX or gpt6-astra selection does not select the two
operational models. Prefer explicitly provided profile/model values. Otherwise
leave them unresolved and require a deliberate owner selection before operational
launch. Context sizes and reasoning labels from previous examples are not verified
current model facts; do not copy MAX as a CLI enum or silently fall back to a default.

Private runtime schema must cover:

```text
OAP_REPO_ROOT, OAP_STRATEGIC_HOME, OAP_GITHUB_REPOSITORY
CODEX_BIN
CODING_CODEX_HOME, STRATEGIC_CODEX_HOME
CODING_CODEX_PROFILE or explicit CODING_CODEX_MODEL
STRATEGIC_CODEX_PROFILE or explicit STRATEGIC_CODEX_MODEL
role-specific reasoning/context/compaction overrides (only when verified)
OAP_ACK_DANGER_FULL_ACCESS=NO
OAP_ACK_START_LOOP=NO
OAP_SETUP_TMUX_SESSION, OAP_RUN_TMUX_SESSION
REPAIR_QWEN_BASE_URL, REPAIR_QWEN_MODEL, REPAIR_QWEN_API_KEY_ENV (unresolved)
REPAIR_ALLOW_LIVE_TESTS=NO
REPAIR_API_HOST=127.0.0.1
REPAIR_API_PORT (unresolved)
```

Generate syntactically valid actual paths and empty unresolved settings, not angle-
bracket pseudovalues in executable env/TOML. Omit unsupported/unset optional keys.
No secrets. Use one clear configuration authority with conflict detection. Parse
allowlisted values without execution where possible; shell sourcing means trusted
executable configuration and requires explicit ownership/mode/symlink checks.
Use argument arrays; never eval model names or interpolate untrusted shell text.

The model role is a routing/configuration fact, not a capability claim. Coding
may have less context; measure intended read-set budget against the selected
profile once supported. Do not require Qwen/data to start ordinary offline OAP work.

## 7. Launch behavior

**Setup mode:** two side-by-side tmux panes, coding left/strategy right; correct
cwd and separate CODEX_HOME per role. No task prompt, automatic order, FIFO read/
signal or forced dangerous mode. Missing auth/model values must not block opening
the shell. A later owner-started interactive CLI may configure/login; exiting
returns to a live shell in the same role directory/home. Never restart it on quit.

**Operational mode:** deliberate acknowledgements plus validated role config,
remote baseline and governance. Coding wrapper waits at OS level on control FIFO;
no model spends tokens while idle. It consumes one signal, checks no-replay state,
sets OAP_ROLE=coding and starts one fresh bounded invocation. Strategy starts with
OAP_ROLE=strategic from STRATEGIC_HOME in its own persistent context. Its logical
continuity survives process replacement.

Tiny launch prompts route to correct files; they do not duplicate full law or
load both branches. The coding model does not wait for the already consumed OK.
Completed final reports are verified before another model is launched. Nonzero exit,
publication failure or inconsistent state enters explicit recovery, not automatic
replay. A deliberate stop returns to a shell. Per-role locks prevent duplicates.

Both tmux launchers support a real --print-only mode. Reinvocation may attach to
an identified matching project session, never kill/repurpose an unrelated one.
Cleanup targets only owned children. No daemon/cron/autostart installation.

## 8. Helper implementation contract

The detailed semantics are in OAP-PROTOCOL. At minimum:

| Helper | Mandatory behavior |
|---|---|
| bootstrap-two-codex-oap.sh | No-write dry run; safe real materialization; identical reruns no-op; explicit scoped backup-preserving refresh. |
| doctor.sh | Read-only source/role/context/tooling/permissions/FIFO/state check; no repair/install/signal side effects. |
| check_governance.py | Shared canonical/projection/read-set/critical/base validation; bootstrap, accepted-runtime and candidate-review modes. |
| check_state.py | Exact active/order/report/recovery state plus relevant DHA status; absent active is inactive, malformed is invalid. |
| publish_order.py | Final metadata/provenance/DHA/transition validation; immutable order then active pointer; crash recovery; no implicit signal. |
| append_critical.py | Exact active strategic payload only, trusted prior bytes, lock/recovery; no agent human dispositions. |
| verify_report.py | Exact identity, implementation parent, report-only SELF path/content and actual remote verification where configured. |
| oap_fifo.py | Exactly two bytes OK, real FIFOs, fragmented I/O and invalid framing tests, interruptible external waiting. |
| check_ica.py | Schema/milestone/source/main revision/requirement coverage/freshness checks; no invented semantic or independence guarantee. |
| launchers | Role/cwd/home/read-set selection, idle outside model, setup/operation separation, no resurrection. |

All have useful help, deterministic nonzero failures, bounded redacted diagnostics,
tests and safe quoting/path handling. Reuse common validation rather than reimplement
slightly different critical/order grammars in each script.

Doctor distinguishes four independent statuses:

```text
bootstrap structure and fake-helper tests: valid / invalid / unproven
operational activation: ready / blocked with precise prerequisites
live repair testing: disabled / unverified / separately qualified
product/milestone acceptance: NOT ASSESSED BY BOOTSTRAP
```

Do not recreate a missing live register from the seed. Runtime governance compares
current accepted source/projections. Candidate-review mode checks ordered revisions
against accepted base while the private strategy copy stays accepted-old. Refresh
private governance at a quiescent post-merge checkpoint before its next use; no
unaccepted source or mutable private draft overwrites.

## 9. Immutable source installation

Copy root PLAN/ARCHITECTURE/CRITICAL bytes exactly and archive input snapshots.
Full architecture has one live current source. Root CRITICAL equals its seed at
bootstrap and grows by exact appends later. Do not keep testing whole-file seed
equality after legitimate appends. Maintain both seed-prefix and full-prior-history
integrity checks. No agent-approved human disposition can clear a gate.

Preserve bundled OAP chapters unedited. Their examples/statistics/manual URLs are
source material, not fresh verified product claims. There is no remaining need to
retrieve the formerly unavailable site before bootstrap generation. Optional
outside verification can be recorded separately, not substituted for these sources.

## 10. Final validation and handoff

Execute the complete BOOTSTRAP-ACCEPTANCE matrix. This package contains instructions
and templates, not pre-tested implementations; the generator must actually build
and test the helpers. Use disposable Git repos, temporary FIFOs and fake CLIs/
GitHub transports. No actual model session or protected-service mutation.

Check all source hashes again; all required files and active links; role projection
coverage and budgets; every detailed initial draft and dependency map; exact
critical schema/four disposition values; valid z-to-aa same-PR continuity; report
SELF provenance; crash/no-replay paths; quiescent source refresh; ICA gates and
no fabricated evidence. Verify real target paths are inactive after tests.

Do not equate an untested installed-client assumption with fake-test success.
Record NOT RUN/BLOCKED accurately. Fix safe scaffold defects; report genuine missing
external authority without making the owner do ordinary implementation work.

The receipt ends with exact owner actions: review generated scope/diff/manifest;
select/configure an authorized remote and baseline publication; configure/login
separate runtime roles; run doctor; deliberately enable permitted activation;
launch the two-pane operational mode. Provide manifest-scoped review commands,
not blind git add -A over an existing checkout. The bootstrap does not perform
these human approval actions for the owner.

**Stop after materializing and verifying the inactive bootstrap.**
