# Work report 008-h - Composition-contract fix, documented dialect-limitation policy classes, and final objective-008 blind acceptance on a new sealed v3 hidden set

```oap-report
{
 "id": "008-h",
 "result": "COMPLETE",
 "order_path": "oap/orders/008-h-composition-contract-fix-and-final-blind-acceptance.md",
 "order_sha256": "782c6811e1963fc07779d4209461ff6ad1057eec62ffa652b5916e6c6fa73342",
 "governance": {
  "AGENTS.md": "118be17b2e9e5b50d628c796b136ee1b8154ebf844af0007839f65848fd69f09",
  "ARCHITECTURE-for-agents.md": "e83545d648b32263110f532d9425c785abdcd96be5e37d0ce055f57cc2ec9491",
  "ARCHITECTURE.md": "a16e0f87bdb21f6920aa63119cf5be148b6c8b68bf1337c44006d54b7a7261f7",
  "OAP-COMMUNICATION-coding-agent.md": "6355623830eb5ef47523d04b1a6bf958685f4debd16b62f9f46c3f14de01020b",
  "PLAN.md": "d2aa1d98cc5177ac6093ab3aeb79903b192780910ef2d1712839b474fb5adbf0",
  "SECURITY.md": "0424b58bdaae1d4379364e3470b7cb4b2b729a20456d30e5c1ea59a3edbed7a6",
  "TESTING.md": "68a3307289f684910281b9d4947336f3a390924e229e7aafce7212b4a0fcc5d1",
  "oap/coding-instructions/AGENTS.md": "55bc5d72200cd3e25e1d8decd629d902ffb732c8808bae8ef36a6081b4416514",
  "oap/governance/DISTILLATION-MAP.md": "a03d4be8d70101dc2d038bc5e8bd98e18fcaf3806539651b37d0e85f2558c2d7",
  "oap/governance/WORKSPACE-LAYOUT.json": "cba4ae2226038a44d74bff2eb727bc79e930b34f8f657b1e14c411a4ca09d254",
  "oap/prompts/coding-round.md": "fa94f21c065209d284978f7f731b95600cbab87949d0c8ae2c226a0923552611",
  "oap/prompts/ica-start.md": "2311778a8c2f8cbfa24bbc9be1eed30d14289f3d82c2b5e8da70d806b12d70f4",
  "oap/prompts/strategic-start.md": "40ccb00e48c9b1b6ae93d5e5a334322b8a249d655341e47a123e5321b65b6e9b",
  "oap/strategic-instructions/AGENTS.md": "ac05494856ac8c85c31b77225ac96abe9596f0d0ae50d96e338da7370669fa5c",
  "oap/strategic-instructions/OAP-COMMUNICATION-strategic.md": "6ba11ddcde24ed3d8777f305951d706d3fc4a1869470be9669a9853d3ce15cbf",
  "oap/strategic-instructions/strategic_model_init_material.md": "813edbc94f0a991abda046a544beb22510a83c9b45dc5282f046dd71a324465d"
 },
 "publication_commit": "SELF",
 "implementation_head": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
 "publication_verified": false,
 "pr_mode": "AMEND_EXISTING_PR",
 "pr": 9,
 "pr_url": "https://github.com/ulfe-lmi/llm-slovenian-repair/pull/9",
 "pr_state": "open",
 "branch": "oap/008-prose-boundary-qualification",
 "base_sha": "fca215cfd9fe84a6dd7b73a12db538c2ecca040e",
 "starting_remote_sha": "345f34bc0657ce5b7d74b877c69649e35052abc8",
 "no_merge": true,
 "checks": [
  {
   "command": "Reconciliation before mutation (requirement 1c): git ls-remote origin refs/heads/main and refs/heads/oap/008-prose-boundary-qualification, gh pr view 9 --json number,state,mergedAt,mergeable,autoMergeRequest,baseRefName,headRefName,headRefOid, python3 oap/bin/check_state.py --repo-root ., python3 oap/bin/check_governance.py --repo-root . --mode candidate-review --accepted-ref 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9 (executed at this session's start, 2026-09-20T21:20Z, on the local implementation head; remote re-observed in this session)",
   "result": "PASSED",
   "details": "Remote main 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9 unchanged (equals OAP_ACCEPTED_REF). Remote branch head 345f34bc0657ce5b7d74b877c69649e35052abc8 (the activation commit; the remote head at round start) while the local branch carries the four 008-h implementation commits over it (1f35f09e3d88d10d30011f86c333711a0481c3e6, 4033d26f5ef87c9828f730ccec2c326daec7546a, 7abb0ef2a98168f4558f183ce154402f1907ca65, 6e2352948e8e748f1f942709ae8daa55cfdd382c) unpushed. PR #9 state open, mergedAt null, mergeable MERGEABLE, autoMergeRequest null, base main, head oap/008-prose-boundary-qualification. oap/active = exactly 008-h plus one LF (6 bytes). workorders/consumed.json = 008-h / consumed / recovery:true / pr 9 (the wrapper consumed control once before this session; this session is the same-ID/branch/PR resume of the consumed round after context loss, with the implementation already committed at 6e2352948e8e748f1f942709ae8daa55cfdd382c and the report-only commit outstanding). check_state returned RECOVERY_REQUIRED for 008-h - the expected pre-report state (no 008-h report exists until the report-only commit; control consumed). check_governance candidate-review: structure valid, exit 0, all sixteen governance hashes matching the order metadata. No other concurrent mutation of the branch.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Frozen-surface byte-identity and in-scope diff (requirement 1a): git diff --name-status fca215cfd9fe84a6dd7b73a12db538c2ecca040e..6e2352948e8e748f1f942709ae8daa55cfdd382c plus per-blob comparison of the preserve-list surfaces against the 008-g final head, in this session",
   "result": "PASSED",
   "details": "The full diff base..head is exactly 88 files: 79 additions and 9 modifications (STATUS.md; oap/GENERATED-FILES.json; oap/active; research/RESEARCH-STATE.md; research/curated/prose_boundary.py; research/prose-boundary/corpus/manifests/corpus-census.json; research/prose-boundary/tools/prose_boundary_builder.py; research/registry/experiments.json; research/tables/experiment-summary.csv), every one on the order Files-and-boundaries modify list; zero out-of-scope paths. Every surface in the local_work preserve list is byte-identical to base (absent from the diff): the frozen 007-m linguistic surfaces (config, prompt, final-root manifest - the machine-block hashes re-asserted by the 007-o consistency suite); the entire 008-a through 008-g round (every order and report byte, registry entries, tools, results, REPORT-008G.md, the hidden_acceptance.py 008-f harness at sha256 22ae618bd1be66bfd93c79a10e97066eef5dcd05213f851e7b19f5a12e8771fd); the v1 hidden manifest (sha256 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18) and v2 hidden manifest (sha256 cf37654a45b10c024525fc334f368d4b20956619e2568542544c855d93a85ec1); structural-policy-v3.json (sha256 7ff6646e3c11d1241cb1c85bd96f08e3f0db08e73d4eb4a4e97fac0c17e1828f, byte-identical in-tree at every 008-h head); the annotation guide; every pre-existing test file (the only test-file change is the new additive file); all workflows; the 008-d quarantine record; protected.py at sha256 27f22eaee129190b880851b958a8aa5a38d357cb4e711d70e762b2353d32e78f (byte-identical to the 008-g final head). The 008-g report bytes re-verified in this session (sha256 c6b814484483c3a864e3231c16ba8f34c6517e1861acb7407b6e5176ec9edc73) and the 008-g registry entry byte-identical to the base blob (31 to 32 entries, the 008-g entry unchanged). The untracked local residue (root corpus/, .research-test-scratch/, .rclone-speed-test/) was preserved untouched and never committed; the session's own __pycache__ cache artifacts were removed before the publication-guard scan (session-owned caches only).",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Activation commit byte-identity: git show --stat 345f34bc0657ce5b7d74b877c69649e35052abc8 plus sha256sum of the order bytes from the committed blob via git cat-file, in this session",
   "result": "PASSED",
   "details": "The activation commit 345f34bc0657ce5b7d74b877c69649e35052abc8 has sole parent fca215cfd9fe84a6dd7b73a12db538c2ecca040e and carries exactly two files: oap/orders/008-h-composition-contract-fix-and-final-blind-acceptance.md (new; 65,031 B; committed-blob sha256 782c6811e1963fc07779d4209461ff6ad1057eec62ffa652b5916e6c6fa73342, re-verified equal to the worktree bytes in this session) plus oap/active = the exact ID 008-h plus one LF (6 bytes; blob re-verified by od in this session at the activation commit and at the worktree).",
   "sha": "345f34bc0657ce5b7d74b877c69649e35052abc8",
   "publication_head_claim": false
  },
  {
   "command": "Data-free seal re-hash of ALL THREE private roots against the committed manifests at the implementation head (requirements 1b and 9; pre- and post-evaluation round re-hashes are recorded in the committed aggregates and strategy receipts): round-owned script sealing every manifest file (hash and size only, zero content reads into the model context), manifests read from the committed git blobs at 6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "result": "PASSED",
   "details": "This session (post-evaluation re-hash): v1 4091/4091 files sha256+size verified, 0 missing, 0 mismatch, committed manifest sha256 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18 unchanged, seal copy (hidden-manifest-seal.json) byte-identical to the manifest; v2 4076/4076 verified, 0 missing, 0 mismatch, manifest sha256 cf37654a45b10c024525fc334f368d4b20956619e2568542544c855d93a85ec1 unchanged, seal copy byte-identical; v3 4076/4076 verified, 0 missing, 0 mismatch, manifest sha256 ee503bd7c09f26a1b9c171319cc82f8ec692918d0d7d18f11d528a6f18c54f36 unchanged, seal copy byte-identical. Round record (committed): pre-evaluation re-hashes of all three seals plus the strategy independent pre-evaluation data-free re-verification (receipt sha256 46756a8ab18509a949dd5b1cf9cb611f166e5465b15c9a04692540c8208a360b, 3,623 B, cites the v3 manifest sha256, recorded in the committed evaluation aggregate) and the strategy post-evaluation all-seals re-verification (receipt sha256 ecee7e285d073e69f6160ef63a2e3726c9c4db8ddbc26af9c3c5a855199f814a; both receipts live in the supervision tree and are not committed). No v1 re-run and no v2 acceptance use this round: the v1 and v2 sets remain single-use spent.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Policy v4 and builder composition-contract identity (requirement 3): structural-policy-v4.json, structural-policy-v3.json byte-identity in-tree, experiment-008h.json (v3-to-v4 sha256 pair, frozen protection identity census, trigger decision), prose_boundary_builder.py (R1/R2/R3, label_source recording, no-parser-participation assertion), in this session at the implementation head",
   "result": "PASSED",
   "details": "structural-policy-v4.json sha256 f564d9f87ef01a893d6cd6bb36f6cf99a0d353a4dde4e4c4c024942eb969e76f committed: the v3 policy plus exactly (1) the new policy-exposed dialect-ambiguity classes with shape rules and measured v1/v2/008-g evidence - html-closing-tag-adjacent-fence (the 35 B v2 shape), keyvalue-list-item-quoted-emphasis (the 16 B v2 shape), yaml-hash-comment-heading (the 348 B plus 5 B v1 sub-shape, with the recorded non-viability reason) plus the four naturalistic dialect-limitation classes (markdown-table-dialect-boundary 615 B, list-item-candidate-prose 250 B, indented-code-lazy-continuation 45 B, residual-token-line-glue 17 B over the 008-g 70-case aggregate); the xml-diacritic-tag-name class is NOT added (the EXTEND trigger decision covers it); (2) determinism and scope clauses carried unchanged; (3) additive changelog. structural-policy-v3.json stays byte-identical in-tree (sha256 7ff6646e3c11d1241cb1c85bd96f08e3f0db08e73d4eb4a4e97fac0c17e1828f re-verified); the v3-to-v4 sha256 pair is recorded in experiment-008h.json (sha256 c7169caef94df666fb321653d49be9355211c615e9db895f725d64bb99133f88), which also records the new frozen protection identity census (research/curated/prose_boundary.py a89f17f8ef7a8e8651f79a39cd8209911d7592317f5f1fed8f09e5edb9acd87e; protected.py 27f22eae...; structural-policy-v3.json 7ff6646e...; structural-policy-v4.json f564d9f8...). Builder sha256 647ecf1ff3649f22867d124f70201135c267f3aa08328d900186371168fa5451: R1 (a fenced or indented code component is never composed on the line immediately after an HTML closing-tag line; exactly one blank line, template-owned NEUTRAL delimiter, recorded in the composition provenance; closing-tag-scoped predicate per the order), R2 (hash-only admission predicate; excluded count 0 for the v3 pool, recorded in the generation identity), R3 (defined and unit-tested, INACTIVE under the EXTEND decision); the no-parser-participation assertion is retained; the label oracle is unchanged in meaning; the label_source split (construction vs oracle-refined) is recorded per labeled region in the v3 labels and manifest (census: construction-labeled PROTECTED 232,816 B / 2,482 regions; oracle-refined PROTECTED 65,414 B / 1,462 regions; POLICY_EXPOSED 1,688 B / 331 regions). No Markdown-delimiter regexes were added; the residual layer still operates only on parser-approved candidate-prose spans.",
   "sha": "1f35f09e3d88d10d30011f86c333711a0481c3e6",
   "publication_head_claim": false
  },
  {
   "command": "Single predeclared xml-fragment trigger decision (requirement 4): the curated-surface diff against the 008-g final head plus the decision record in experiment-008h.json, in this session",
   "result": "PASSED",
   "details": "Decision EXTEND, recorded in experiment-008h.json BEFORE v3 generation (decided after the dev regression gate, per the order): the trigger name pattern RE_XML_NAME is widened from ASCII letters to Unicode letters ((?!\\d)\\w[\\w.:-]* - a leading Unicode letter/underscore via a negative digit lookahead, then letters/digits/underscore/dots/colons/hyphens) with the shape guards unchanged (letter/underscore immediately after an opening bracket then letters/digits/hyphens plus the v3 dots/colons; same-line closing or self-closing or attribute space; 2000 cp bound; never on comparison operators; never on an opening bracket followed by digits; never on a lone bracket). RE_XML_DIALECT_NAME (the ASCII dialect vocabulary) and the abstention logic are unchanged, so recognized-HTML behaviour is byte-identical to v3. git diff of research/curated/ against the 008-g final head is EXACTLY this single change (strategy-verified and re-verified in this session: 15 insertions / 8 deletions in the one file; the strategy 2026-09-20 final-head review open item 1 (resolved before publication)). The predeclared FALLBACK branch (revert + R3 admission + xml-diacritic-tag-name policy-exposed class) was available and NOT triggered: the dev regression showed 0 new safety exposure and plus 0.021674 pp over-suppression (within the 1.0 pp bound), none attributable to the extended trigger. The item-4 negative controls (Slovenian diacritic word in angle brackets that is not tag-shaped stays candidate prose; inequality angle brackets stay exposed; a lone angle bracket stays exposed; recognized-HTML behaviour unchanged) are in the dev-edge batch (e18-e23) and in the focused test file.",
   "sha": "1f35f09e3d88d10d30011f86c333711a0481c3e6",
   "publication_head_claim": false
  },
  {
   "command": "Visible dev dialect-edge batch (requirement 5): research/prose-boundary/corpus/defect-dev-008h/ (documents + machine-known labels) plus the additive corpus-census.json update, in this session",
   "result": "PASSED",
   "details": "32 documents + 32 machine-known labels committed (order target 30; plus 2 disclosed: e31/e32 are the end-of-input HTML-block-tail boundary variants needed to pin the closing-tag-without-separator shape at document end; the disclosure is in the report, the registry entry, and RESEARCH-STATE section 20). The batch covers: the 35 B v2 shape WITH the R1 separator (labeled protected) and WITHOUT it (labeled policy-exposed per the v4 html-closing-tag-adjacent-fence class); the 16 B keyvalue-list-item-quoted-emphasis shape (policy-exposed per v4); the yaml-hash-comment-heading shape (policy-exposed per v4); the xml-diacritic-tag-name shape (PROTECTED under the EXTEND decision); the item-4 negative controls; and genuine prose controls (Slovenian sentence with parentheses and a colon; a time expression; a quoted Slovenian phrase). corpus-census.json (sha256 5d6a4171c290fc79bc609e3fa74e85e2e1a509c80861fc897f2e8ffedb784cbe) updated additively. The batch is visible to the implementation agent per the order; it pins the deterministic policy-exposed and protected behaviour on the exact v2 shapes.",
   "sha": "1f35f09e3d88d10d30011f86c333711a0481c3e6",
   "publication_head_claim": false
  },
  {
   "command": "Dev regression gate committed BEFORE any v3 generation (requirement 6): research/prose-boundary/results/hidden-acceptance-v3/dev-regression-008h.json plus the commit-order proof (the fix commit precedes the generation commit in history)",
   "result": "PASSED",
   "details": "Committed in the fix commit 1f35f09e3d88d10d30011f86c333711a0481c3e6, which precedes the v3 generation commit 4033d26f5ef87c9828f730ccec2c326daec7546a in the branch history (ordering requirement SATISFIED). Committed aggregate sha256 721a76b7f82ddb9cd12e44623858385441c438ef2a31613758747b64e57cd6a9, status PASS, over the four-file frozen protection identity census: (i) frozen 3,000-document dev corpus - new safety exposure 0 of 526,315 protected bytes; (ii) 50-document 008-g dev batch - safety exposure 0; (iii) 32-document dev-edge batch - protected-shape safety 0. Prose over-suppression on the dev corpus: plus 0.021674473490918 percentage points versus the 008-g dev baseline (plus 0.032982894442701305 pp versus the 008-c baseline; both far inside the predeclared 1.0 pp bound; the suppression rate 1.5702 percent versus the 008-g baseline 1.5485 percent). All four predeclared mechanical stop conditions: NOT fired (the driver asserts and would halt the round in a designed BLOCKED state). Actual losses by container reported regardless of outcome (S.Emph 145, S.Head 436, S.Item 3,727, S.Para 20,620, S.TableCell 625, unknown 4,439).",
   "sha": "1f35f09e3d88d10d30011f86c333711a0481c3e6",
   "publication_head_claim": false
  },
  {
   "command": "New hidden acceptance set v3: generation identity, content-free manifest, seal verification, strategy pre-evaluation re-verification, bounded zero-overlap recovery (requirement 7): generation-identity-008h.json, hidden-manifest-v3.json, seal-verification-v3.json, in this session",
   "result": "PASSED",
   "details": "Generation identity sha256 62b452269aa79171e1af42e46dae7ba653a1a0049bc6bef7c0e8cd858b5a695f: model qwen3.8-27b (A100-FP8 007-lineage deployment record; profile sha256 df556922f9264f4b685c19e9e138f86c577897bb703ae83cbb35847d71d564de; endpoint value omitted per the 007 convention; authorized test-data generation, never objective-009 material); the committed 008-c generation driver (sha256 7050b2254ca14ffbfbe70ca06bd0674724a867debea124966cd56e834ed9cd45) imported unmodified and the frozen prompt library (sha256 904dd36b9891c567f250bf8b42b374d0b6557aa79ca3fcaa3c8824682fcc8fef) verbatim; batch plan: all 76 families, base 4 per family, densified 16 each for structured-html, structured-keyvalue, structured-xml, malformed-partial-html (nominal 352 slots); call budget: 87 component calls + 1 retry + 4 bounded recovery calls, including both preflights, total 89 HTTP attempts (the initial math-align-env failed batch refilled by the single permitted retry; no other model calls in this round). 345 final components across all 76 families; 2,000 deterministic mashup documents (PRNG seed dd1a19a598160b36; 1,635,779 bytes); R1 blank-line insertions 0 (the pool contains no closing-tag-line/fence adjacency by construction; recorded per document in the composition provenance); ground truth by construction with the no-parser-participation builder assertion; the label_source split recorded per region. Bounded recovery (disclosed; 008-g precedent): the initial stream produced 11 exact cross-pool duplicates in four model-saturated machine families (machine-filenames 4 dev, machine-identifiers 3 dev+v1, machine-windows-paths 3 dev+v1, machine-posix-paths 1 v2; machine-posix-paths is not on the predeclared known-saturated list but exhibits the same saturation - disclosed); one bounded K=8 regeneration round per family with new recorded seeds (17 overlap-discarded; 3 disclosed non-fatal shortfalls: filenames retained 1/4, identifiers 2/4, windows-paths 2/4); the final pool is 345 unique components with zero exact overlap asserted and disclosed against the dev pool (641), the v1 pool (588), and the v2 pool (340) by hash-only comparison; strategy independently re-verified the instance-level overlap data-free (recomputed fragment hashes, 0/0/0). Seal: the v3 private root sealed 4076/4076 (76 component + 2,000 document + 2,000 label files) with the byte-identical seal copy; out-of-manifest at seal = 87 generation receipts only. Content-free manifest sha256 ee503bd7c09f26a1b9c171319cc82f8ec692918d0d7d18f11d528a6f18c54f36 and seal verification sha256 b57125bb2e14250397f8b1d15c39afa875b544930f33549f04cd3fafadcc92d9 committed (counts, hashes, sizes, family/template census, seed identity, label-oracle identity, label-source census; no content, no private paths). Strategy independently re-verified the v3 seal data-free BEFORE the evaluation phase (order requirement 7 hard gate; the evaluation wrapper fail-closes without the strategy receipt): 4076/4076 re-hashed from disk against the committed git-blob manifest, manifest sha256 ee503bd7... recomputed equal, seal copy byte-identical - VERIFIED; the receipt identity (sha256 46756a8ab18509a949dd5b1cf9cb611f166e5465b15c9a04692540c8208a360b, 3,623 B, cites the v3 manifest sha256) is recorded in the committed evaluation aggregate.",
   "sha": "4033d26f5ef87c9828f730ccec2c326daec7546a",
   "publication_head_claim": false
  },
  {
   "command": "Data-restricted shape analysis (requirement 2): research/prose-boundary/results/hidden-acceptance-v3/shape-analysis-008h.json, in this session",
   "result": "PASSED",
   "details": "Committed data-free shape summary sha256 7d04ea994ac43e8053b8e0a79f697b5f27aba4cb6ce77e46e053ee48a6c2dc39: Part A re-derived every exposed range of the 008-g naturalistic consistency aggregate (all 15 ranges - the 9 with exposed bytes greater than zero plus the non-material ranges) with the 008-g frozen implementation and asserted byte-identical to the committed aggregate; Part B re-derived and asserted the v1 diagnostic 458 B (labeled development diagnostic, never acceptance). Every range carries a candidate class and, finalized, the D1-4 resolution kind: all 15 ranges classify as DOCUMENTED-DIALECT-LIMITATION (parser-dialect shapes: recognized Markdown tables, list-item candidate prose, indented-code lazy continuation, residual-token line glue); none falls in a 2026-09-17 human-named machine-significant category with a viable bounded recognizer, so no CLOSED-RESIDUAL extension was chosen from this analysis (no residual-recognizer change beyond the predeclared item-4 trigger decision; the residual layer is unchanged in the implementation). The two v1 diagnostic sub-shapes are classified: yaml-hash-comment-heading (348 B plus 5 B; a hash-plus-text line is contextually indistinguishable from a Markdown ATX heading - the recorded non-viability reason) is documented as a v4 dialect-ambiguity class; xml-diacritic-tag-name (110 B under the ASCII-only trigger) is covered by the EXTEND decision, so its policy-exposed class is not added. No response content, no private paths in the committed output.",
   "sha": "1f35f09e3d88d10d30011f86c333711a0481c3e6",
   "publication_head_claim": false
  },
  {
   "command": "v3 hidden evaluation: the full 008-c item-15 metrics set and e2e invariants 1-7 on the 2,000-document v3 set through the committed thin wrapper (research/prose-boundary/tools/hidden_acceptance_v3.py, sha256 9047b5b99bde86c2d4be335aeb6a6e12929b40ff833a30a26a3ebb64ba1f7e7d) importing the frozen 008-f harness unmodified (byte-identity against the committed blob asserted fail-closed at run start), zero network, zero new linguistic model calls (requirement 9)",
   "result": "PASSED",
   "details": "Committed data-free aggregates: mashup-metrics-v3.json sha256 05a82c2c4551fd11112d6f2f69d4ba866402d043866a27fa4851e51773728c0e and e2e-invariants-hidden-v3.json sha256 ba24c5e443fec938a94a8e3561b673f9dccb42dc2af3fca697977423980ed4d1, over 2,000/2,000 documents, evaluated at the frozen protection identity (re-asserted by the wrapper against experiment-008h.json). Label-source split (the 008-h D1-4 methodology): HARD safety axis - construction-labeled protected bytes: 232,816 total, 364 exposed (0.156 percent; target 0; NOT MET - the verdict input); oracle-refined protected census: 65,414 total, 950 exposed (24 documents; policy-coverage census with the CIRCULARITY LIMITATION DISCLOSED - the label oracle is the implementation's own residual function; it does not gate the verdict); cross-check against the raw frozen-harness record reconciles exactly (298,230 protected bytes, 1,314 raw-exposed = 364 + 950; 0 fallback documents). The 364 B hard-axis exposure is 5 of 2,000 documents, three named bounded mechanism shapes, all template T10-randomized: (1) 2 x 139 B (v3h-000713, v3h-001274; code-cpp) - an HTML OPENING-tag line (last line of an adjacent structured-html component) opens a CommonMark type-6 HTML block that absorbs the code fence plus the first code lines to the internal blank line; the remaining code lines parse as paragraph and are exposed (machine tokens inside stay covered by the residual classes); the released R1 predicate is closing-tag-scoped, so this complement shape is not covered and R1 correctly did not fire (v3 R1 insertions 0) - a NEW bounded composition shape surfaced by the fresh pool, addressable at the builder level; (2) 2 x 30 B (v3h-000625, v3h-001241; structured-xml) - a single-line multi-tag XML line in which one tag carries a non-ASCII (Slovenian) attribute name the dialect's ASCII-only inline-HTML rule does not recognise; the frozen xml-fragment class abstains because the tag NAME is in the ASCII dialect vocabulary, so the tag bytes are exposed (18 + 10 + 2 B) while interior machine tokens stay covered - a frozen class-boundary behaviour the tag-name-based trigger cannot see; (3) 1 x 26 B (v3h-001307; math-display-dollar) - an adjacent malformed-incomplete-display-dollar component makes the parser pair its unterminated opener with the complete component's opening marker; the TeX body parses as paragraph where the frozen classes cover numbers, identifier runs, and brace groups but not TeX command names or the equals sign (14 + 12 B) - dialect pairing plus frozen class boundary. PRE-EXISTENCE: reverting the single 008-h curated change (verified by git diff to be the only curated delta versus the 008-g final head) and re-running the frozen protection on the five documents yields BYTE-IDENTICAL exposure (139/139/30/30/26 = 364 B): a pre-existing architecture gap newly surfaced by the fresh pool, not a fix regression (the 008-g 51 B framing). The ordered closing-tag shape (the 35 B v2 shape) has ZERO v3 occurrences: the released R1 covers it by construction. Coordinates 0/0/0/0. Coverage 96.811 percent of expected prose bytes exposed (1,264,413 of 1,306,063; 600 of 8,400 regions completely available; actual losses by container S.Emph 49, S.Head 570, S.Item 2,495, S.Para 14,830, S.TableCell 256, unknown 23,450 - no arbitrary threshold). Malformed behaviour 0 violations over the 10 deterministic family rows; the policy-exposed row totals were independently recomputed from the sealed v3 labels (per-family first-contributing-document identity; the known 008-f first-document double-count carried as a disclosed frozen-harness defect): delta 0 satisfied for all families, raw rows and recomputed true values both committed, no frozen byte changed. E2E invariants 1-7 all PASS on 2,000/2,000 replays at the frozen deterministic stub boundary (the embedded raw frozen-harness output is explicitly labelled as such, not a development-run result). Determinism: sorted doc_id iteration; 50-case re-run byte-identical (v3h-000000 through v3h-000049). Performance: median 46.7 / 68.6 / 276.8 ms per document at 1/10/50 KB, peak RSS 72,732 KB (qualitative statement only). The evaluation exit code 1 is the wrapper's DESIGNED hard-fail signal on the hard axis (construction exposed not equal to 0) - the exit code is the verdict input, not an evaluation error; all aggregates were written and committed (008-g precedent wording).",
   "sha": "7abb0ef2a98168f4558f183ce154402f1907ca65",
   "publication_head_claim": false
  },
  {
   "command": "Naturalistic consistency re-run on the fixed 70-label population (requirement 8): the committed deterministic driver research/prose-boundary/tools/naturalistic_consistency_v3.py (sha256 6a3324556120c84a6e1381f8f7b3ef2d79e7fe2ebdf3304ee2527d7fd20bc67a) plus the committed data-free aggregate naturalistic-consistency-v3.json (sha256 79f98d8bab5acb1ca3a65c894ec83af311334439a0c1bd1c0ace341b74f3873c)",
   "result": "PASSED",
   "details": "The frozen 008-h implementation (prose_boundary.py a89f17f8...; pinned helper 5df9a11d...) re-run on the 70 naturalistic responses against the EXISTING completed v2 labels (harness-mediated private-root reads only; fixed population; no new naturalistic generation; no re-adjudication). Result: 5 material cases (identical to the 008-g five: nat-glossary-entry-hidden-03, nat-json-config-hidden-01, nat-math-answer-hidden-04, nat-qa-answer-hidden-04, nat-table-equations-hidden-03), 15 exposed ranges (6 material ranges under the carried 008-g predeclared rule: exposed span of 50 B or more, or 5 or more exposed ranges in one case), exposed byte counts byte-identical to the committed 008-g aggregate (the EXTEND trigger is monotonic - it only adds protection - and changed nothing on this population). Per-range resolution (D1-4, committed as the driver's embedded data-free map): 15/15 DOCUMENTED-DIALECT-LIMITATION with the v4 class references (markdown-table-dialect-boundary x9, list-item-candidate-prose x2, indented-code-lazy-continuation x1, residual-token-line-glue x3); 0 CLOSED-RESIDUAL (no shape falls in a named machine-significant category with a viable bounded recognizer); 0 AMBIGUOUS; 0 unresolved material range - verdict condition (vii) satisfied (no_unresolved_material_range true, material_cases_all_resolved true). Attribution (carried 008-g heuristic): parser-dialect 12, label-ambiguity 2, residual-class-gap 1. The narrow predeclared label-correction path was NOT triggered (label_corrections empty: no frozen label was revealed factually wrong about the source text; the path permits at most 5 cases under the frozen guide with the committed 008-g v2 instrument).",
   "sha": "7abb0ef2a98168f4558f183ce154402f1907ca65",
   "publication_head_claim": false
  },
  {
   "command": "Final objective-008 verdict and ledger/state bookkeeping (requirements 10-11): RESEARCH-STATE.md additive section 20 plus machine block, the registry entry, STATUS.md verdict sentences, scoped GENERATED-FILES.json pin, CSV rebuild, REPORT-008H.md, in this session",
   "result": "PASSED",
   "details": "Final objective-008 verdict CONDITIONAL (predeclared decision state; structural-safety evidence only), justified field-by-field against the committed new evidence in RESEARCH-STATE.md section 20, the registry entry, STATUS.md, REPORT-008H.md, and this report: (i) v3 construction-labeled protected bytes exposed = 0 - FAIL (364 B; 5 of 2,000 documents; three named bounded shapes; byte-identical under the 008-g implementation); (ii) coordinates 0/0/0/0 - PASS; (iii) malformed behaviour 0 violations - PASS; (iv) e2e invariants 1-7 all PASS on the v3 set - PASS (7/7); (v) determinism identical (50-case re-run) - PASS; (vi) dev regression PASS - PASS (0/0/0; plus 0.021674 pp within the 1.0 pp bound); (vii) every material naturalistic range resolved - PASS (15/15 DOCUMENTED-DIALECT-LIMITATION; 0 unresolved material range); (viii) zero model calls beyond the recorded v3 generation budget, zero linguistic-pipeline calls, zero objective-009 touches - PASS (89 generation HTTP attempts, 0 linguistic-pipeline, 0 objective-009). NOT FAIL: every exposed byte is bounded and its mechanism fully understood; the parser-first design detected all of it on the fresh pool (the set's defect-detection purpose) and the dev corpus plus the 008-g batch remain at zero exposure. The smallest corrective suffix is named (extend R1 from closing-tag lines to all HTML block-start lines for the 278 B absorption shape; a bounded class-boundary analysis of the 60 B xml-fragment non-ASCII-attribute shape and the 26 B display-dollar pairing shape; the T6 link-destination admission/label refinement as the disclosed pre-existing observation). Judgment debt: D1-3 CARRIED, D1-4 CARRIED, the carried 008-g D1-1 remains CARRIED (all clear only on a final objective-008 PASS); on the non-occurring PASS branch the objective-008 protection layer would be declared FROZEN at the 008-h census - that declaration is NOT made by this round. Sections 18 and 19 of RESEARCH-STATE.md and all prior records remain byte-identical. Bookkeeping: registry 32 entries (the 008-g entry byte-identical; the new entry kind corrective-evaluation, status CONDITIONAL, with the full data-free metrics summary, the v1/v2/v3 seal identities before and after, the naturalistic resolution summary, the dev regression summary, the new frozen protection identity, the label-source census, and pending_evidence naming objective 009 and the named corrective scope); machine block registry_entries 31 to 32 and oap_reports_reviewed 50 to 51 with every other identity field unchanged (including quarantined_007n and quarantined_008d); STATUS.md carries the objective-008 final verdict sentence(s) (structural-safety verdict; no linguistic acceptance; no merge, release, or deployment claim; objective 009 reserved); the scoped GENERATED-FILES.json pin covers exactly the STATUS.md entry; the committed research/tables/experiment-summary.csv is the deterministic rebuild (rebuild_tables --check PASSED in this session); REPORT-008H.md sha256 4ec3a088bae28925ac2b74b7daf70b780d94109a444710fcce344e183a1b117d.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Focused re-run at the implementation head (order Verification, Focused): research/tests/test_008h_dialect_edge_protection.py (sha256 a0f2cb563b138aae3bfc4f0e0c7d11a5bb8f1c0416f8daca79bd914fee7a1b43) plus the 007-o consistency file (test_research_state_consistency.py), umask 022 (CI parity), pinned helper (sha256 5df9a11d56b5bdec90c3ca09298802723975306f77e43e74ad1a72eb427c44bf, presence and hash re-verified in this session)",
   "result": "PASSED",
   "details": "54 tests: 53 passed, 1 failed in 8.63 s. The single failure is EXACTLY the designed pre-report count assertion (test_registry_and_report_counts, AssertionError 51 != 50: the machine block oap_reports_reviewed 51 versus the 50 report files in the tree before the report-only commit after the 007-o exclusion; the registry count assertion 32 == 32 passes) - the 008-e/008-f/008-g designed precedent; green at the final head by construction (this report adds the 51st counted file). Every positive and negative case of the focused file passes (R1 with/without separator, R2 shape, the yaml-hash-comment-heading shape, the xml-diacritic-tag-name shape protected under EXTEND, the item-4 negative controls, the prose controls, the dev-edge batch fail-closed validation). The observation is local evidence at the implementation head.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Full research suite with the pinned helper present, umask 022 (CI parity): python3 -m pytest -p no:cacheprovider --ignore=.venv -q research/tests at the implementation head (order Verification, Broader; the v3 results are data-free aggregates and the suite does not read either private root)",
   "result": "FAILED",
   "details": "rc=1, 2026-09-20T21:46:01Z to 21:54:32Z (pytest wall 512.19 s). Result line: 1 failed, 357 passed, 75 subtests passed in 512.19s (0:08:32). The single failure is EXACTLY the designed pre-report count assertion (research/tests/test_research_state_consistency.py ResearchStateConsistencyTest.test_registry_and_report_counts, AssertionError 51 != 50: the machine block oap_reports_reviewed 51 versus the 50 report files in the tree before the report-only commit after the 007-o exclusion; the registry count assertion 32 == 32 passes) - the 008-e/008-f/008-g designed precedent, green at the final head by construction (this report adds the 51st counted file). Run environment: umask 022 (CI parity), the pinned helper present (sha256 5df9a11d56b5bdec90c3ca09298802723975306f77e43e74ad1a72eb427c44bf, re-verified), the v3 results are data-free aggregates and the suite does not read either private root. The earlier full-suite run of this round under the session-default umask 077 additionally showed the umask-077-environmental contextual-validator mode test (negative_paths item 11); it does not occur in the umask 022 CI-parity run cited here. The observation is local evidence at the implementation head, never a CI observation.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "One full local Application-baseline driver run at the implementation head, umask 022 (CI parity): python3 -B scripts/verify_development_baseline.py with an owned persistent temp parent under the user's local share directory (never /tmp) and a local 1800 s command-timeout diagnostic parameter (the committed driver is byte-frozen at the 300.0 s per-command cap, so the local parameter changes no gate); per-stage results and wall times are local evidence only, never a CI observation (order Verification, Broader)",
   "result": "FAILED",
   "details": "Driver exit rc=1, 2026-09-20T21:54:32Z to 22:24:37Z (wall 1,805 s; per-stage results and wall times are local evidence only, never a CI observation). Stages: cleanup PASSED; uv lock --check PASSED (1,800 s cap); frozen dependency sync PASSED; focused contract tests PASSED; full pytest FAILED in a fresh temporary workspace copy of the repository with its own frozen environment (owned persistent temp parent under the user's local share directory, never /tmp; the 1,800 s local command-timeout diagnostic parameter changes no gate), reporting 2 failed, 738 passed, 5 skipped, 91 subtests passed in 1668.10s (0:27:48). Failure 1: ResearchStateConsistencyTest.test_registry_and_report_counts - exactly the designed pre-report count assertion (AssertionError 51 != 50, as in the research suite cited above; the registry count assertion 32 == 32 passes; green at the final head by construction). Failure 2: oap Acceptance.test_B18_real_fifo_framing - subprocess.TimeoutExpired of the test's own spawned oap_fifo.py wait reader after its 3 s startup budget (the test's own comment waits for the FIFO reader on slower source mounts); an environmental I/O-latency flake under full-suite load on the sync mount, not an implementation regression: the identical test PASSED in this round's earlier full driver run (the 21:11:11Z stage, where the full-pytest failures were the contextual-validator umask-077 test and the count assertion) and PASSED in this session's isolated re-run at the implementation head (1 passed in 90.16 s, umask 022, CI parity environment, owned temporary directory). The driver result FAILED at the implementation head is expected (the designed count assertion); the B18 flake is recorded verbatim as the second named failure together with its isolated pass.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "OAP suite at the implementation head: OAP_FORWARD_RECOVERY_HISTORY_SOURCE set to the repository worktree with an owned persistent TMPDIR, frozen local-disk project environment: python3 -B -m unittest discover -s oap/tests (order Verification, Broader; the B10 inventory-link, scoped inventory/drift, transcript and report-history suites at the pre-report state)",
   "result": "PASSED",
   "details": "rc=0, completed 2026-09-20T23:10:11Z: 'Ran 129 tests in 7230.085s' followed by OK, with zero error output in the suite section. OAP_FORWARD_RECOVERY_HISTORY_SOURCE set to the repository worktree; owned persistent TMPDIR under the user's local share directory; frozen local-disk project environment. The suite ran at the pre-report state (the report-only commit does not exist yet), the established 008-e/008-f/008-g precedent: the B10 inventory-link, scoped inventory/drift, transcript, and report-history suites all pass at the implementation head with the report absent, and those guards are re-run at the report-only state by the pre-push gate. The observation is local evidence at the implementation head, never a CI observation.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Bounded re-derivation of the committed v3 evaluation (order Verification, Focused: the label-source split re-derivation on a bounded v3 sample, the harness determinism re-run stability check, and the e2e invariant re-derivation on a bounded v3 sample): the committed wrapper re-run to an owned private scratch with --replay-count 200 --skip-performance against the sealed v3 private root and the strategy pre-evaluation receipt, at the implementation head",
   "result": "PASSED",
   "details": "The re-derivation succeeded and wrote all outputs to an owned private scratch (2026-09-20T21:45:39Z to 21:57:44Z, wall 725 s; --replay-count 200 --skip-performance). The driver exit code 1 is the wrapper's DESIGNED hard-fail signal on the hard axis (construction exposed 364, not 0), not an evaluation error - the 008-g precedent wording. Strategy pre-evaluation receipt re-verified: v3seal-result-pre-20260920-211915.json sha256 46756a8ab18509a949dd5b1cf9cb611f166e5465b15c9a04692540c8208a360b, 3,623 B, cites the v3 manifest sha256. Hidden-set identity re-asserted: manifest ee503bd7c09f26a1b9c171319cc82f8ec692918d0d7d18f11d528a6f18c54f36, 2,000 documents / 2,000 labels / 345 components. Wrapper self-test all true (cp round trip, out-of-order caught, truncation caught, no fail-open, patch-noop identity). Re-derived aggregates versus committed: every deterministic field byte-identical - safety 298,230 protected / 1,314 exposed / met false; construction_exposed 364; oracle_refined_exposed 950; coordinates 0/0/0/0 met true; coverage 0.9681102672688836; malformed_violations 0; recomputation_delta_0 true; determinism 50-case identical (v3h-000000 to v3h-000049); e2e invariants 1-7 all true, all_pass true; the label-source split (construction-labeled 232,816 total / 232,452 covered / 364 exposed, met false; oracle-refined 65,414 total / 64,464 covered / 950 exposed; cross-check 298,230 reconciled true). The only differences are the declared re-derivation parameters: the performance block absent (--skip-performance), evaluation wall seconds 192.02 to 193.99 (local), the derived output_sha256 field, and the e2e replay count 2,000 to 200 (the bounded sample). No private content in the model context; the scratch is session-owned and never committed.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Data-free privacy and whitespace enforcement at the implementation head (acceptance criterion 12): python3 -B -m research.tools.publication_guard --root research (zero hidden content in committed artifacts) plus python3 oap/bin/check_whitespace.py --base 2832fa1e51bdf3641aabbd81feab8ddb64a876da --revision HEAD and --base 82ea1e6f4173934fa47bb34ee6a6f78338d3603a --revision HEAD (the two authorized bases; the three frozen historical blank-at-EOF incidents are the only tolerated diagnostics), after removing the session's own __pycache__ cache artifacts",
   "result": "PASSED",
   "details": "All executed in this session at the implementation head, umask 022. publication_guard --root research: PASS (6,487 files) - zero hidden content in committed artifacts. check_whitespace at HEAD 6e2352948e8e748f1f942709ae8daa55cfdd382c against both authorized bases: base 2832fa1e51bdf3641aabbd81feab8ddb64a876da PASSED and base 82ea1e6f4173934fa47bb34ee6a6f78338d3603a PASSED, each with exactly the three frozen historical blank-at-EOF incident diagnostics (oap/orders/007-e-recover-forward-from-an-immutable-invalid-report.md, oap/orders/007-f-complete-faithful-research-archive-and-ci-portable-reproduction.md, oap/reports/007-d-complete-research-publication-and-reproduction-coverage.md) and no other diagnostics; one transient GIT_EXECUTION_FAILED on the first base-2832fa1e attempt in this session passed on the immediate retry (sync-mount I/O). research/tools/rebuild_tables.py --check: numeric table PASS (the committed research/tables/experiment-summary.csv is the deterministic rebuild). The session's own __pycache__ cache artifacts under research/ were absent at scan time (the pre-existing oap/bin cache is outside the guard root and was never touched).",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Pre-push report validity gate at the locally committed report-only state (requirement 12; the 008-e gate verbatim): (a) meaningful() scan of the full draft report text - every checks[] command string, every mandated string field, and the full document (no angle-bracket spans, no standalone uppercase placeholder-vocabulary tokens); (b) python3 oap/bin/verify_report.py --repo-root . --id 008-h (local-only; verifies the report-only commit invariants against the literal implementation head); (c) python3 oap/bin/check_transcript.py --repo-root . --revision HEAD --expected-id 008-h",
   "result": "PASSED",
   "details": "Gate (a): the meaningful() pattern scan of the full draft report text - every checks[] command string, every mandated string field, and the full document - for the meaningful() rejection patterns (any angle-bracket span and any standalone uppercase token from the four-word uppercase placeholder vocabulary of the meaningful() rejection regex) reported zero violations at build time (the builder enforces this scan and refuses to emit the draft otherwise). Gate (b): python3 oap/bin/verify_report.py --repo-root . --id 008-h at the locally committed report-only state returned verified (scope local only): the report-only commit has the literal implementation head 6e2352948e8e748f1f942709ae8daa55cfdd382c as sole parent and the report path as sole changed path, the report and order bytes match the committed blobs, every checks[] sha is an ancestor of the implementation head, the full report field validation passed (governance equal to the order metadata, order identity and sha256, pr_state open, pr_url exact, base_sha and starting_remote_sha, no_merge true, publication_commit SELF, publication_verified false, critical_action NONE, pr_observed_at not after report_written_at), and the report-history guard at the report commit is valid. Gate (c): python3 oap/bin/check_transcript.py --repo-root . --revision HEAD --expected-id 008-h at the same state returned valid: the worktree active pointer byte-matches the commit active pointer (008-h), every published report has its order, the 008-h report validates against its order and its commit re-verified as report-only, no unfinished non-current order, 008-h is the latest and active, and the report-history guard is valid. Stop-and-correct discipline (the 008-d lesson): if (b) or (c) fails, the report is corrected in place, committed with git commit --amend --no-edit keeping the sole parent and sole changed path, the gate re-run, and no push occurs until all three gates pass.",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "Report-only commit (sole parent = the literal implementation head 6e2352948e8e748f1f942709ae8daa55cfdd382c; sole changed path oap/reports/008-h-composition-contract-fix-and-final-blind-acceptance.md), push, python3 oap/bin/verify_report.py --repository ulfe-lmi/llm-slovenian-repair --id 008-h (independent remote head/branch/PR, report bytes, parent, and changed-path check), and the exact 2-byte OK response via the response FIFO (P-SELF-03; requirement 14)",
   "result": "PENDING",
   "details": "Executed immediately after this commit and recorded in the post-publication record and the strategic review (P-SELF-03): the report-only commit stages ONLY the report path with the literal implementation head as sole parent, pushes the branch (the four unpushed implementation commits plus the report commit), independently verifies the remote head bytes, parent, and sole changed path, and then sends the exact OK response frame. No push or mutation occurs after this report commit (C-REPORT-01).",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  },
  {
   "command": "All four required checks at the final head (this report commit): Application baseline, Research reproducibility, OAP bootstrap acceptance, OAP report history (requirement 13; the established bounded verbatim rerun observation policy for the zero-failure TIMEOUT class: up to 3 additional observations at the identical final head, a green identical-commit rerun is the current check state, disclosed verbatim)",
   "result": "PENDING",
   "details": "Executed after the push and recorded in the post-publication record and the strategic review (P-SELF-03). Predeclared rule: all four green (directly or via the bounded verbatim rerun observations) is the success state; any genuine test failure is a STOP/BLOCKED with the failing test named; no new suffix merely for waiting. Local CI-parity evidence at the implementation head (umask 022): the designed single count assertion (machine block 51 versus 50 report files before the report-only commit) is the only expected red and is green at the final head by construction (this report adds the 51st counted file); the umask-077-environmental contextual-validator mode test does not occur under CI's umask 022 (verified in this session: the test constructs a 0o755 directory and passes under umask 022, failing only under umask 077 where the directory is created 0700).",
   "sha": "6e2352948e8e748f1f942709ae8daa55cfdd382c",
   "publication_head_claim": false
  }
 ],
 "critical_action": "NONE",
 "implementation": "Five commits on oap/008-prose-boundary-qualification over base fca215cfd9fe84a6dd7b73a12db538c2ecca040e (the 008-g final head, equal to the order base_sha): (1) 345f34bc0657ce5b7d74b877c69649e35052abc8 activation: the exact order bytes (sha256 782c6811e1963fc07779d4209461ff6ad1057eec62ffa652b5916e6c6fa73342, 65,031 B, re-verified against the committed blob in this session) plus oap/active = 008-h plus one LF; (2) 1f35f09e3d88d10d30011f86c333711a0481c3e6: the composition-contract fix (structural-policy-v4.json sha256 f564d9f87ef01a893d6cd6bb36f6cf99a0d353a4dde4e4c4c024942eb969e76f with the additive dialect-ambiguity classes and changelog; v3 byte-identical in-tree; experiment-008h.json sha256 c7169caef94df666fb321653d49be9355211c615e9db895f725d64bb99133f88 carrying the v3-to-v4 sha256 pair, the frozen protection identity census, and the EXTEND trigger decision with the not-triggered fallback rule; the builder R1/R2/R3 composition contract and label_source recording; the single predeclared curated change - the xml-fragment trigger name pattern widened to Unicode letters; the 32-document visible dev dialect-edge batch; the committed data-free shape analysis; the focused test file; and the dev regression gate PASS committed BEFORE any v3 generation); (3) 4033d26f5ef87c9828f730ccec2c326daec7546a: the NEW v3 hidden acceptance set (345 components across all 76 families; 2,000 deterministic mashup documents, seed dd1a19a598160b36; the committed generation identity with the bounded zero-overlap recovery disclosure; the content-free manifest sha256 ee503bd7c09f26a1b9c171319cc82f8ec692918d0d7d18f11d528a6f18c54f36 and seal verification sha256 b57125bb2e14250397f8b1d15c39afa875b544930f33549f04cd3fafadcc92d9 with the seal 4076/4076 and the strategy pre-evaluation re-verification receipt recorded); (4) 7abb0ef2a98168f4558f183ce154402f1907ca65: the v3 blind evaluation (the committed data-free aggregates; the new thin wrapper hidden_acceptance_v3.py importing the frozen 008-f harness unmodified with byte-identity asserted fail-closed), the naturalistic consistency re-run (the new deterministic driver; the committed data-free aggregate with the per-range resolution), and REPORT-008H.md; (5) 6e2352948e8e748f1f942709ae8daa55cfdd382c (the literal implementation head): exactly the five bookkeeping paths (research/registry/experiments.json, research/RESEARCH-STATE.md, STATUS.md, oap/GENERATED-FILES.json, research/tables/experiment-summary.csv). Commit 6 (SELF, this report): oap/reports/008-h-composition-contract-fix-and-final-blind-acceptance.md only, with the literal implementation head as sole parent. No merge, no auto-merge, no force-push; main unchanged at 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9. The only model use of the round is the authorized, recorded, and classified: v3 component generation 89 HTTP attempts total (87 component calls + 1 retry + 4 bounded recovery calls, including both preflights; authorized test-data generation); zero linguistic-pipeline model calls; the narrow label-correction path was not triggered (0 calls); no other model calls of any kind.",
 "pr_observed_at": "2026-09-20T23:13:35Z",
 "report_written_at": "2026-09-20T23:16:00Z",
 "documentation": "(1) research/prose-boundary/REPORT-008H.md (sha256 4ec3a088bae28925ac2b74b7daf70b780d94109a444710fcce344e183a1b117d) - the round's data-free research report: the pre-work gates, the shape analysis, the composition-contract fix record (R1/R2/R3, the EXTEND trigger decision, policy v4), the v3 set identity (seal, strategy receipt, generation identity, the bounded zero-overlap recovery), the dev regression, the v3 acceptance metrics (the 364 B hard-axis attribution with the pre-existence proof, the label-source split, the frozen-harness recomputation disclosure), the naturalistic re-run with the per-range resolution, the final verdict field-by-field, and the limitations. (2) research/RESEARCH-STATE.md section 20 - the additive final objective-008 verdict (field-by-field; D1-3 CARRIED, D1-4 CARRIED, the carried 008-g D1-1 CARRIED; structural-safety-only classification; objective-009 reservation; the on-PASS frozen-protection declaration stated as NOT made) plus the machine block update (registry_entries 31 to 32, oap_reports_reviewed 50 to 51, every other identity field unchanged); sections 18 and 19 untouched. (3) STATUS.md - the objective-008 final verdict sentence(s) appended to the existing paragraph (structural-safety verdict; no linguistic acceptance; no merge, release, or deployment claim; objective 009 reserved). (4) research/registry/experiments.json - one 008-h entry (kind corrective-evaluation, status CONDITIONAL) with the full data-free metrics summary, the v1/v2/v3 seal identities before and after, the naturalistic resolution summary, the dev regression, the new frozen protection identity, the label-source census, and pending_evidence naming objective 009 (fresh human-labelled target-distribution linguistic confirmation - NOT this corpus) and the named corrective scope. (5) research/prose-boundary/results/hidden-acceptance-v3/ - the five data-free aggregate files (dev-regression-008h.json, shape-analysis-008h.json, mashup-metrics-v3.json, e2e-invariants-hidden-v3.json, naturalistic-consistency-v3.json) plus the content-free manifests under research/prose-boundary/corpus/manifests/ (hidden-manifest-v3.json, seal-verification-v3.json, the additive corpus-census.json). (6) This OAP report: the complete data-free evidence record.",
 "criteria": "Addressed against the order's twelve acceptance criteria with committed evidence: (1) MET - all pre-work gates recorded and passed (remote/PR/active reconciliation re-observed in this session; frozen-surface byte-identity with the in-scope diff of exactly 88 files; activation bytes; the session post-evaluation re-hash of all three seals plus the round pre/post re-hashes and the strategy pre and post receipts). (2) MET - the shape analysis committed (data-free; every exposed range classified into the D1-4 taxonomy; no response content, no private paths). (3) MET - policy v4 committed (v3 byte-identical in-tree; the dialect-ambiguity classes with shape rules and measured evidence; the changelog; the v3-to-v4 sha256 pair recorded in experiment-008h.json). (4) MET - the builder contract (R1/R2 with the provenance recording, R3 defined and inactive, the admission-rule recording, the no-parser-participation assertion retained; the label_source split recorded in the v3 labels and manifest); the focused test file green with the positive and negative cases (the designed pre-report count assertion is the single red, in the 007-o consistency file); the frozen 007-m surfaces byte-identical at the implementation head. (5) MET - the trigger decision recorded (EXTEND with the Unicode-letter pattern and unchanged shape guards; the predeclared fallback rule available and not triggered, with the dev regression evidence); the dev regression committed before any v3 generation: dev corpus new safety exposure 0 of 526,315; 008-g batch new safety exposure 0; dev-edge protected-shape safety 0; prose over-suppression plus 0.021674 pp within the predeclared 1.0 pp bound (actual losses reported by container regardless); no stop condition fired, so no BLOCKED state. (6) MET - the v3 hidden set: manifest plus seal verification committed; the strategy data-free re-verification recorded before the evaluation phase (the receipt cites the manifest sha256; the wrapper fail-closes on it); the generation identity committed (model, prompt library, frozen driver, seeds, call budget, admission rules, excluded count 0, the bounded recovery); the zero-exact-component-overlap assertion with the dev (641), v1 (588), and v2 (340) pools disclosed; GT by construction (no-parser-participation assertion); the label_source census recorded; 345 components / 76 families / 2,000 documents. (7) MET - the v3 evaluation artifacts committed, data-free, deterministic (50-case re-run identical; fixed sorted-doc_id iteration; the bounded re-derivation check recorded in the checks); the new frozen protection identity recorded; all three seals verified before and after the evaluation (manifest sha256s unchanged); no v2 or v1 acceptance use (both single-use spent; no re-run cited as acceptance anywhere). (8) MET - the naturalistic consistency re-run aggregate committed with the per-range resolution kinds (15/15 DOCUMENTED-DIALECT-LIMITATION; 0 unresolved material range); no label correction (the narrow path not triggered; recorded). (9) MET - the verdict is exactly one of the predeclared states, CONDITIONAL, justified field-by-field against the committed new evidence (NOT PASS: field (i) - 364 B above 0 on the hard axis; NOT FAIL: every exposed byte is bounded and its mechanism fully understood, the parser-first design detected all of it, and the dev corpus plus the 008-g batch remain at zero exposure); the structural-safety-only classification and the objective-009 reservation stated in this report, RESEARCH-STATE section 20, the registry entry, STATUS.md, and the research report; the 008-g artifacts untouched (report bytes re-verified; registry entry byte-identical). (10) MET - registry 32 entries; the machine block 32/51 consistent at the implementation head (the designed single-assertion red at the implementation head - oap_reports_reviewed 51 versus 50 report files - is green at the final head by construction, as this report adds the 51st counted file); the 008-g registry entry byte-identical. (11) PENDING inside this pre-push report per P-SELF-03 - the report-only commit invariants (parent equals the literal implementation head, sole changed path, exact bytes) are executed immediately after this commit via the remote verify_report; the pre-push local gate (meaningful() scan, local verify_report, local check_transcript) is recorded in the checks, and the 008-d defect class (angle-bracket placeholder tokens) does not recur. (12) PENDING inside this pre-push report per P-SELF-03 for the four required checks at the final head (the predeclared rule recorded in the checks, with the local CI-parity evidence); zero hidden content in committed artifacts (publication guard and whitespace scans recorded); zero 007-m changes; zero objective-009 touches; no merge.",
 "negative_paths": "Recorded and observed: (1) The predeclared dev-regression stop conditions did NOT fire (0 new safety on the dev corpus, the 008-g batch, and the dev-edge protected shapes; plus 0.021674 pp within the 1.0 pp bound); had any fired, the round would have been BLOCKED with the dev regression committed and NO v3 generation or hidden evaluation performed (the v3 generation commit exists only because the gate passed and was committed first). (2) The predeclared verdict cap DID fire, by design: the v3 hard safety axis is not met (364 B above 0), so the final verdict is at most CONDITIONAL; each of the three mechanism shapes is named, bounded, and attributed (pre-existing: byte-identical under the 008-g implementation). (3) The predeclared EXTEND fallback rule did NOT fire (no new safety exposure attributable to the extended trigger; over-suppression within bound); the fallback branch (revert + R3 + policy-exposed class) remains the recorded alternative. (4) Designed pre-report red at the implementation head: the single count assertion (machine block oap_reports_reviewed 51 versus 50 report files in the tree) is red in exactly one test (test_registry_and_report_counts) in the local research suite and in the driver's full-pytest stage, and the registry count assertion 32 == 32 passes; green at the final head by construction; any additional genuine failure at that head would be a STOP/BLOCKED event with the test named. (5) The zero-exact-component-overlap assertion initially FAILED on the first generation run (11 exact duplicates: dev 10, v1 6, v2 1, counted with overlap across pools, confined to four saturated machine families): the predeclared builder assertion acted as designed; the single bounded K=8 regeneration per family recovered the pool and the final 0/0/0 re-assertion is disclosed (3 non-fatal shortfalls disclosed); no component was silently reused; the strategy independently re-verified instance-level overlap data-free. (6) Seal mismatch: none found (v1 4091/4091, v2 4076/4076, v3 4076/4076 hash- and size-verified before and after the evaluation, re-verified in this session; manifest sha256s unchanged; seal copies byte-identical); a mismatch would have been STOP/BLOCKED with the seal voided and recorded. (7) Frozen-surface diff: none found (the full base-to-head diff is exactly 88 files, all on the modify list; every preserve-list surface byte-identical to base). (8) The evaluation wrapper exit code 1 is the designed hard-fail signal (construction-labeled exposed bytes 364, not equal to 0) - an expected data-free observation of the named defect, not a tooling failure; all aggregates were written and committed. (9) The label-correction path was NOT triggered (no frozen label factually wrong about the source text); had it triggered, at most 5 cases under the frozen guide with the committed 008-g v2 instrument, every correction recorded with rationale. (10) The initial math-align-env batch returned a malformed response (not a JSON array of exactly K strings); it was refilled by the single permitted retry and the failure is disclosed in the generation identity; no label or component was fabricated. (11) The umask-077-environmental contextual-validator mode test (test_private_directory_boundaries_reject_symlink_wrong_mode_and_escape) fails only under umask 077 (the session default: the 0o755 test directory is created 0700 and accepted by the validator) and passes under umask 022 (CI parity; the directory stays 0755 and is rejected) - the round's heavier local runs record which umask they used, and the CI-parity runs use umask 022.",
 "boundary_fidelity": "Exactly six commits, all in scope. Commits 1 to 5 (345f34bc0657ce5b7d74b877c69649e35052abc8 activation: order plus oap/active; 1f35f09e3d88d10d30011f86c333711a0481c3e6 the composition-contract fix plus dev-edge batch plus dev regression gate plus shape analysis plus focused test file; 4033d26f5ef87c9828f730ccec2c326daec7546a the v3 generation identity plus the two content-free manifests plus the seal verification; 7abb0ef2a98168f4558f183ce154402f1907ca65 the evaluation aggregates plus the two new tool files plus the naturalistic aggregate plus REPORT-008H.md; 6e2352948e8e748f1f942709ae8daa55cfdd382c the five bookkeeping paths) - every path on the order Files-and-boundaries modify list (88 files total over the base). Commit 6 (SELF, this report): oap/reports/008-h-composition-contract-fix-and-final-blind-acceptance.md only, with the literal implementation head as sole parent. No change to any 007-m linguistic surface, frozen corpus, v1 or v2 manifest or seal, 008-a through 008-g artifact, workflow, gate, or driver (the committed 008-f harness is byte-identical to base); no objective-009 data of any kind; no CRITICAL append or mitigation update; no merge, no auto-merge, no force-push, no reset/clean/stash; the untracked local residue (root corpus/, .research-test-scratch/, .rclone-speed-test/) preserved untouched and never committed; the private roots were accessed only through the committed harness, wrapper, and driver entry points plus the round-owned data-free re-hash (hash and size only; zero content reads into the model context). PR #9 amended, OPEN, and UNMERGED throughout; main unchanged at 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9.",
 "setup": "Reconciled before mutation (requirement 1): remote main 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9 unchanged; PR #9 open, mergedAt null, mergeable MERGEABLE, autoMergeRequest null, head oap/008-prose-boundary-qualification, base main; at round start (order publication) the remote head was 345f34bc0657ce5b7d74b877c69649e35052abc8 (the activation commit) and the local branch is 4 commits ahead of the remote (the four 008-h implementation commits, unpushed at reconciliation). oap/active carries 008-h. Environment: CPU-only; the frozen project environment (the committed uv.lock); the pinned helper binary prose-boundary-meas at the private research runtime root (sha256 5df9a11d56b5bdec90c3ca09298802723975306f77e43e74ad1a72eb427c44bf, presence and hash re-verified in this session) drives every protection call; the local driver invocation uses the 1800 s command-timeout diagnostic parameter with an owned persistent temp parent under the user's local share directory (never /tmp); the sync-mount git stat-cache blindness (content changes invisible to stat-based git status, observed on the oap/active pointer at activation) is worked around by content-hash comparison and od-level byte verification; background processes use setsid nohup with pgrep tracking (a plain nohup background job dies with the exec session); the heavy suite runs are sequential to avoid sync-mount I/O contention. This session is a same-ID resume after context loss (consumed.json recovery:true): the implementation was already committed at the literal implementation head 6e2352948e8e748f1f942709ae8daa55cfdd382c; this session re-verified the durable state (remote, seals, frozen surfaces, committed aggregates) before writing the report. The v1, v2, and v3 private roots were accessed only through committed entry points or the round-owned data-free re-hash; zero hidden content reads into the model context. The model endpoint is the authorized 007-lineage A100-FP8 deployment (profile sha256 df556922f9264f4b685c19e9e138f86c577897bb703ae83cbb35847d71d564de; endpoint value omitted per the 007 convention; credentials read as data and never printed, persisted, hashed, or committed).",
 "privacy": "Zero hidden content in any committed artifact, log, or report: the committed aggregates carry counts, categories, rates, hashes, document IDs (v3h-*), component IDs (comp-*-v3-*), case IDs (nat-*-hidden-*), container names, residual-class names, malformed family names, the deterministic performance-document hashes, and the seeds only; the publication-guard scan at the implementation head (recorded in the checks) and the mechanical privacy scan (private paths, endpoint values, credential shapes) found zero violations; no document text, no label content, no naturalistic response text, no raw model output, no private absolute paths, no endpoint value, and no bearer in any committed byte. The v3 set (4,076 files), the spent v1 set (4,091 files), and the spent v2 set (4,076 files), and all labels, remain in the private roots, referenced by committed content-free manifest hashes only; the generation receipts (87) remain in the private root only (out-of-manifest, disclosed at seal). The model identity is disclosed (qwen3.8-27b, A100-FP8 007-lineage record, profile sha256) with the endpoint value omitted per the 007 convention; the call ledger is complete and classified (v3 generation 89 HTTP attempts total; zero linguistic-pipeline calls; zero label-correction calls; zero objective-009 touches). The strategy seal-verification receipts are cited by sha256 and size only (they live in the supervision tree and are never committed).",
 "limits": "This round is structural-safety evidence ONLY: the CONDITIONAL verdict establishes no linguistic quality, no experimental-MVP acceptance, and no release or deployment authority; it is not an acceptance of the Slovenian repair system of any kind. The verdict applies to the fixed implementation frozen at the dev-regression gate (the four-file census in experiment-008h.json) and to the NEW sealed v3 set (single-use as acceptance; first and only use). The v1 and v2 sets remain single-use spent: no re-run on either is cited as acceptance anywhere in this round (the v1 458 B figure is the committed 008-g development diagnostic, re-derived and asserted in the shape analysis only). The naturalistic label evidence is complete (70/70 labels, frozen) with line-indexed granularity (disclosed); all 15 exposed ranges are resolved as DOCUMENTED-DIALECT-LIMITATION with the v4 class references, and 0 material range is unresolved. The 364 B hard-axis residual is the named bounded defect; the objective-008 protection layer is NOT declared frozen (that declaration is reserved for a final PASS). Objective 009 (fresh human-labelled linguistic confirmation of the complete frozen pipeline) remains the separate reserved next objective; its future sample must not contain any case generated or inspected for objective 008 (every v1, v2, and v3 component, mashup, naturalistic case, dev document, dev batch, and dev-edge document). PR #9 remains open and unmerged; merge disposition is strategy's separate act after independent review of this round's final head (and requires a PASS verdict). No milestone, release, deployment, or acceptance-claim language appears in any committed artifact of this round beyond these explicit denials.",
 "human_gates": "The human research decision of 2026-09-17 (objective-008 strengthening) defines this round's architecture: a material defect revealed by the hidden set uses normal Concentrated-OAP corrective sequencing with the hidden set's evidential status explicitly downgraded and a new independent hidden set generated and sealed after the fixes; the naturalistic population stays the 70 labeled cases with the consistency re-run against existing labels and re-adjudication only if a label must change (the narrow predeclared path, at most 5 cases); the predeclared PASS/CONDITIONAL/FAIL decision states govern the final objective-008 conclusion; the 2026-09-17 human architecture decision (parser first, then parser-approved spans, then the narrow residual recognizer on the named machine-significant categories only; no ever-growing regex grammar; prefer real syntax validators) is honored: the residual layer gained no new recognizer this round (the only implementation change is the predeclared single trigger decision, EXTEND). The human instruction of 2026-09-18/19 (keep the loop moving through implementation and independent structural acceptance; all four required checks genuinely green at the reviewed final head; no weakening, skipping, or redefining any check; no manufactured acceptance of any kind; the owner is not a terminal relay; D0/D1 resolved by strategy) is honored: the predeclared stop conditions were enforced by the drivers, the verdict follows the predeclared states on the actual data, and the named corrective scope is handed to strategy rather than self-assigned. No human gate was bypassed; D1-3 and D1-4 are registered in the order's Governance section with full S-DECIDE-02 records and remain CARRIED (they clear only on a final PASS); the carried 008-g D1-1 remains CARRIED; no CRIT admission (five conditions fail at condition 1).",
 "scope": "Execute and publish round 008-h exactly as ordered: the pre-work integrity gates (requirement 1); the data-restricted shape analysis (requirement 2); policy v4 and the bounded builder composition contract on the released surfaces only (requirement 3); the single predeclared xml-fragment trigger decision (requirement 4 - EXTEND, fallback not triggered); the visible dev dialect-edge batch (requirement 5); the dev regression gate committed before any v3 generation (requirement 6 - PASS, no stop condition fired); the new independent sealed v3 hidden acceptance set (requirement 7); the naturalistic consistency re-run on the fixed 70-label population (requirement 8); the implementation freeze and v3 blind evaluation (requirement 9); the final objective-008 verdict per the predeclared decision states (requirement 10 - CONDITIONAL, structural-safety only, the smallest corrective suffix named); the ledger and state bookkeeping (requirement 11); the report-only final commit with the 008-e pre-push validity gate verbatim (requirement 12); the final-head CI observation under the bounded verbatim rerun policy (requirement 13); the exact response OK after remote verification (requirement 14). Nothing beyond the order's Files-and-boundaries list is modified; the two NEW tool files (the thin evaluation wrapper, the naturalistic consistency driver) are committed so the evaluation is reproducible from the sealed private root; no objective 009; no merge.",
 "result_summary": "Round execution COMPLETE: all 14 requirements executed and the twelve acceptance criteria MET (criteria 11 and 12 PENDING inside this pre-push report per P-SELF-03, the predeclared rules recorded in the checks). Final objective-008 verdict: CONDITIONAL (predeclared decision state; structural-safety evidence only). On the NEW sealed v3 hidden acceptance set (2,000 documents, 345 components, 76 families; seal 4076/4076 verified data-free before and after evaluation, strategy pre- and post-evaluation receipts recorded), the composition-contract-fixed implementation (builder R1/R2, the EXTEND trigger decision, policy v4) shows exactly one material defect on the hard axis - 364 of 232,816 construction-labeled protected bytes exposed (0.156 percent; target 0) across 5 of the 2,000 documents, in three named bounded pre-existing mechanism shapes (278 B HTML-opening-tag-adjacent fence absorption, 60 B xml-fragment non-ASCII-attribute class boundary, 26 B display-dollar pairing across a malformed component), byte-identical under the 008-g implementation (a pre-existing architecture gap newly surfaced by the fresh pool, not a fix regression) - while every other axis is green (coordinates 0/0/0/0; malformed 0 violations over 10 deterministic rows with the policy-exposed recomputation delta 0; e2e invariants 1-7 PASS on 2,000/2,000 replays; determinism 50-case re-run identical; the oracle-refined policy-coverage census 950 B of 65,414 with the disclosed circularity limitation; the ordered closing-tag shape at zero v3 occurrences). The naturalistic consistency re-run on the fixed 70-label population: 5 material cases (identical to the 008-g five), 15 exposed ranges byte-identical to the 008-g aggregate, 15/15 resolved as DOCUMENTED-DIALECT-LIMITATION with v4 class references, 0 unresolved material range, 0 label corrections. The dev regression gate PASSED before any v3 generation (0/0/0 safety; plus 0.021674 pp within the 1.0 pp bound). D1-3 and D1-4 CARRIED; the carried 008-g D1-1 CARRIED (all clear only on a final objective-008 PASS); the objective-008 protection layer is not declared frozen. The smallest corrective suffix is named (R1 extension to all HTML block-start lines; bounded class-boundary analysis of the two remaining shapes; the T6 link-destination refinement). No merge, release, or deployment claim; PR #9 open and unmerged; objective 009 reserved."
}
```
