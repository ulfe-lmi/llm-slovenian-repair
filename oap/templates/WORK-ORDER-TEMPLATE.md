# Final order assembly template (inert)

Start from the specific numbered draft, reconcile every current fact and set
status FINAL in its one oap-metadata JSON fence. Preserve all 16 required readable
sections. Replace governance with exact identity map, base_sha with literal accepted
Git SHA, source repository/default/branch/PR and required CI names with observations.
Set D0/D1/D2 safe scope and concrete H/A/E/I/C provenance; no R marker.

Normal DHA is NONE. APPEND needs critical-entry fence with canonical complete
entry, actual PR/objective, length/hash and trusted entire prior register reference.
Mitigation NONE + UPDATE uses critical-update fence. Payload includes final LF;
fence closes only after exact bytes. No agent human-adjudication records.

Validate --dry-run before publication. A new a has pr null and CREATE_NEW_PR;
correction uses verified number/AMEND_EXISTING_PR. Only strategy publishes, no
implicit signal. Quoted synthetic TODO strings are data, not unresolved metadata.
