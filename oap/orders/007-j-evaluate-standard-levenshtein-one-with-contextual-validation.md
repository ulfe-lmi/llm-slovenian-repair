# Work order 007-j — Evaluate standard Levenshtein-one candidates with contextual validation

Status: FINAL

```oap-metadata
{
  "id": "007-j",
  "title": "Evaluate standard Levenshtein-one candidates with contextual validation",
  "objective": "007",
  "status": "FINAL",
  "phase": "DEMONSTRATOR",
  "repository": "ulfe-lmi/llm-slovenian-repair",
  "default_branch": "main",
  "base_sha": "ee2d1b479719009ff1d07829478f241e3f395f7c",
  "branch": "oap/007-concept-verification",
  "pr_mode": "AMEND_EXISTING_PR",
  "pr": 8,
  "dependencies": ["001", "002", "003", "004", "005"],
  "local_work": "Preserve the complete remotely published 007-i chain through report-only head a4d3592e25a792dd3f8d65a729c54a08032a88e3 and all native private evidence. Before mutation, coding must reconcile the clean objective branch after the observed Dropbox/rclone metadata stall; no conflicting local work is authorized for overwrite.",
  "prior_review": "007-i is remotely verified and scientifically complete: 735 immutable validator observations, two paired projections, zero uncertain deliveries, and final public/OAP records. PR #8 is OPEN/UNMERGED; main is ee2d1b4. Final-head OAP checks are green; Research reproducibility is red only at stale generated-table freshness after 133 tests passed; Application baseline retains its inherited failure.",
  "provenance": [
    {"kind":"H","reference":"Owner directs one paired experiment expanding only substitution-only candidate generation to standard Unicode-code-point Levenshtein distance 1, reusing exact 007-i validator requests, preserving identical validator/fallback, and stopping after permanent research/OAP publication without merge."},
    {"kind":"E","reference":"Verified read-only reconnaissance over the frozen 2,973-row population and 141,162-form unigram vocabulary found 1,035 new unique candidates: 542 request-identical reusable 007-i substitutions and 493 new insertion/deletion candidates; 193 old unique candidates become ambiguous."},
    {"kind":"I","reference":"STRATEGIC_HOME/workorders/007-j-levenshtein-one-reconnaissance.md SHA-256 09556f9d17ac5f54654837a366e2378eb57e2a054e3ddc5b45d1f9cd5b8ee9d0 fixes candidate, transition, reuse, live, projection, privacy and failure boundaries before implementation/model calls."},
    {"kind":"A","reference":"S-ORDER-03 requires suffix 007-j on the same objective branch/PR; LR-002/003/007/008/012/013/014 preserve selection, evidence, exact patching, protected environment and evidence-layer distinctions."}
  ],
  "governance": {
    "AGENTS.md": "118be17b2e9e5b50d628c796b136ee1b8154ebf844af0007839f65848fd69f09",
    "ARCHITECTURE-for-agents.md": "e83545d648b32263110f532d9425c785abdcd96be5e37d0ce055f57cc2ec9491",
    "ARCHITECTURE.md": "a16e0f87bdb21f6920aa63119cf5be148b6c8b68bf1337c44006d54b7a7261f7",
    "OAP-COMMUNICATION-coding-agent.md": "6355623830eb5ef47523d04b1a6bf958685f4debd16b62f9f46c3f14de01020b",
    "PLAN.md": "d2aa1d98cc5177ac6093ab3aeb79903b192780910ef2d1712839b474fb5adbf0",
    "SECURITY.md": "0424b58bdaae1d4379364e3470b7cb4b2b729a20456d30e5c1ea59a3edbed7a6",
    "TESTING.md": "68a3307289f684910281b9d4947336f3a390924e229e7aafce7212b4a0fcc5d1",
    "oap/coding-instructions/AGENTS.md": "55bc5d72200cd3e25e1d8decd629d902ffb732c8808bae8ef36a6081b4416514",
    "oap/governance/DISTILLATION-MAP.md": "a03d4be8d70101dc2d038bc5e8bd98e18fcaf3806539651b37d0e85f2558c2d7",
    "oap/governance/WORKSPACE-LAYOUT.json": "cba4ae2226038a44d74bff2eb727bc79e930b34f8f657b1e14c411a4ca09d254",
    "oap/prompts/coding-round.md": "fa94f21c065209d284978f7f731b95600cbab87949d0c8ae2c226a0923552611",
    "oap/prompts/ica-start.md": "2311778a8c2f8cbfa24bbc9be1eed30d14289f3d82c2b5e8da70d806b12d70f4",
    "oap/prompts/strategic-start.md": "40ccb00e48c9b1b6ae93d5e5a334322b8a249d655341e47a123e5321b65b6e9b",
    "oap/strategic-instructions/AGENTS.md": "ac05494856ac8c85c31b77225ac96abe9596f0d0ae50d96e338da7370669fa5c",
    "oap/strategic-instructions/OAP-COMMUNICATION-strategic.md": "6ba11ddcde24ed3d8777f305951d706d3fc4a1869470be9669a9853d3ce15cbf",
    "oap/strategic-instructions/strategic_model_init_material.md": "813edbc94f0a991abda046a544beb22510a83c9b45dc5282f046dd71a324465d"
  },
  "lr": ["LR-002", "LR-003", "LR-007", "LR-008", "LR-012", "LR-013", "LR-014"],
  "relevant_gates": [],
  "required_checks": ["Research reproducibility", "OAP bootstrap acceptance", "OAP report history", "Application baseline"],
  "decision_class": "D0"
}
```

## Identity

Immediate human-authorized experimental suffix after immutable 007-i, on the SAME
objective-007 branch and PR #8. The sole scientific variable is the candidate
neighborhood: replace 007-i's one-alphabetic-substitution set with the complete
standard Levenshtein-distance-one set over the same casefolded Unicode code-point
lookup forms and same frozen EXACT unigram vocabulary.

Mandatory private source, read in full:

- `STRATEGIC_HOME/workorders/007-j-levenshtein-one-reconnaissance.md`, SHA-256
  `09556f9d17ac5f54654837a366e2378eb57e2a054e3ddc5b45d1f9cd5b8ee9d0`.

Reuse the exact 007-i frozen validator prompt from code and its public/private
identity; do not copy it into a newly worded strategic prompt or modify its bytes.

## Provenance

The owner's current direction fixes the new candidate rule, reuse policy, required
transition/operation analysis, comparison baseline, exclusions and stop condition.
The mandatory reconnaissance provides the exact pre-live population and request
budget. Earlier generated roadmap seeds are not treated as authority to widen scope.

## Current verified state

Remote main is `ee2d1b479719009ff1d07829478f241e3f395f7c`; remote branch and
open/unmerged PR #8 head are
`a4d3592e25a792dd3f8d65a729c54a08032a88e3`, the immutable 007-i report
commit. Active/report are 007-i and CRITICAL has no admitted entries. OAP bootstrap
acceptance/report-history checks are green. Research reproducibility failed only at
the stale generated numeric table after its test step passed; Application baseline
has the inherited failure. The Dropbox/rclone workspace showed metadata stalls, so
coding must obtain an actual clean status and matching local/remote head before any
repository mutation; do not reset, clean, stash or overwrite around the stall.

## Governance

S-AUTH-01, S-PRODUCT-02/03/04, S-ORDER-01/03, S-EVIDENCE-01,
S-DIAGNOSE-01, S-PROTOCOL-01 and S-REVIEW-01 apply. Human authority covers the
bounded new validator calls against the existing A100-FP8 deployment. It does not
authorize service mutation, merge, release, deployment or production integration.

## Goal and dependencies

Determine whether expanding unique mechanical candidates from substitution-only to
standard Levenshtein distance 1 increases useful reference correction coverage after
the identical contextual validator and fallback, without losing the precision and
preservation advantage established by 007-i BEST.

## Scope

Add a small 007-j distance-one candidate/driver seam and focused offline tests under
`research/`; create one new native private experiment root outside Git and outside
system temporary storage; stage/hash exact required 007-i/frozen-baseline inputs;
freeze configuration and candidates before calls; reuse exact 007-i observations;
make calls only for the non-reusable population; aggregate the required views and
analyses; publish data-free configuration/result/report records and minimal additive
navigation/table updates; publish one immutable 007-j OAP report. Preserve every
prior public/private research and OAP record byte-for-byte.

## Non-goals

No detector, target population, English policy/threshold, corpus/vocabulary, initial
case, mechanical gate, exact-unigram gate, validator prompt/parser, model/deployment,
reasoning, inference field, timeout, worker policy, baseline reviewer/retry,
fallback, scoring, protected-span or patching change. No distance 2+, Damerau
transposition, frequency/context tie-break, morphology, optimized production index,
`--slovenize`, benchmark expansion, new semantic labels, Codex/proxy/SSE, second
deployment, unrelated CI repair, merge, release, deployment or next experiment.

## Files and boundaries

Allowed repository changes are new 007-j research code/tests, new data-free public
007-j config/result/report, deterministic generated numeric table and minimal
additive registry/navigation updates. Existing research implementations and records
may be imported but not semantically changed; a shared helper may change only if
strictly necessary and with explicit regression proof that historical behavior is
unchanged. OAP changes are only the exact 007-j order, active pointer and final
007-j report. Filled sentences/targets/candidates/prompts, raw responses/reasoning,
credentials, private endpoint/profile paths and row-level evidence remain in the new
native private root.

## Requirements

1. Reconcile exact main, branch, PR, active/report, prior hashes and clean local work
   at `a4d3592e25a792dd3f8d65a729c54a08032a88e3`. Preserve every earlier
   artifact. A FUSE stall is not permission to reset, replace or bypass repository
   history.
2. Create exactly one unique owner-controlled, non-symlink, mode-0700 007-j root
   under the existing native research-runtime parent. Never use `/tmp`. Stage only
   required immutable 007-i/frozen inputs with regular-file/owner/mode/size/hash
   checks; source bytes remain unchanged.
3. Verify exact frozen populations: 1,487 spelling rows, 1,486 preservation rows,
   75 initial-u/v spelling rows, 2,001 plus 920 admitted detector-selected Slovene
   OOV targets, and 141,162 frozen EXACT unigram forms. Detector outputs, English
   suppressions, selected coordinates and saved baseline decisions must match 007-i.
4. Implement standard Levenshtein distance over Python Unicode code points with unit
   substitution/insertion/deletion cost. Identity is distance zero; two substitutions,
   substitution+insertion and adjacent transposition are distance two. Do not perform
   Unicode normalization, Damerau transposition, splitting/joining or multiword edits.
5. Search the same complete frozen vocabulary. Let new C be the deduplicated union of
   all EXACT forms at standard distance one from the existing casefolded lookup form:
   C=0/C>1 fall through, C=1 alone reaches validation. Do not rank/tie-break. Label
   each unique relation exactly SUBSTITUTION, INSERTION or DELETION.
6. Recompute 007-h's old alphabetic substitution-only C solely as an identity and
   transition assertion. Freeze and report the full requested transition matrix,
   including zero-valued old-C=1/different and old-C>1/new-C=1 cells. Pre-live
   expected totals are 826 spelling plus 209 preservation new unique candidates;
   operations 542 substitution, 172 insertion and 321 deletion.
7. Verify expansion creates 493 old-C=0/new-C=1 candidates, preserves 542 old unique
   candidates unchanged, and makes 193 old unique candidates ambiguous. Any mismatch
   stops before live calls; do not tune or silently replace the expected population.
8. Run unchanged initial-case restoration, mechanical replacement checks and EXACT
   membership for the unique candidate. A failed local gate falls through and does
   not reach the validator. Preserve exact original coordinates and protected spans.
9. Freeze immutable private `CONFIGURATION.json` and ordered candidate manifest before
   live execution. Include all source/code/input, old/new population, transition,
   operation, prompt/request/parser/deployment, reuse, worker, persistence, failure,
   projection/scorer and privacy identities. Record private endpoint/profile values
   only privately and never the credential value.
10. For each of the 542 surviving same-candidate substitutions, reuse a 007-i
    observation only after exact source row/coordinate/sentence/original/candidate,
    canonical request-body SHA, prompt, model/profile/endpoint, protocol fields,
    effort, parser, limits and immutable source-record hashes match. Persist a
    provenance record that distinguishes reuse from fresh dispatch. Never reuse by
    target text or candidate alone.
11. A missing or mismatching reuse field requires a fresh call only if that candidate
    is in the frozen new-C=1 set and the maximum 493-call manifest is correspondingly
    frozen before dispatch. Do not borrow, repair or resample an old observation.
12. Make exactly one fresh call for each of the 493 new insertion/deletion candidates
    and no other model call. Use the same A100-FP8 `qwen3.8-27b` profile, exact frozen
    007-i prompt, Responses non-streaming, `store=false`, `include_reasoning=true`,
    explicit `reasoning.effort="low"`, no added sampling/max-output/stop fields,
    300-second timeout, 2,000,000-byte response bound and strict parser. Do not mutate
    the model server.
13. Use 8 deterministic disjoint workers over the stable ordered new-call keys,
    modulo 8, ascending within each worker, with one in-flight call per worker and at
    most 8 total. Prove no duplicate/missing assignment. Reused observations consume
    no worker dispatch.
14. Persist immutable request bytes before dispatch, dispatch marker, raw bounded
    response/timing before parsing, and parsed observation. Completed valid/failure
    observations are never overwritten. Request/dispatch without raw response becomes
    `INTERRUPTED_UNCERTAIN_DELIVERY_NO_RESAMPLE`, terminal failure, and is never called
    again. Resume only the same root/freeze.
15. Accept only the unchanged strict 007-i choices. `USE_CANDIDATE` validates the
    supplied unique candidate; `KEEP_ORIGINAL`, `UNCERTAIN` and protocol/transport
    failure reject it distinctly and execute the exact preserved baseline fallback.
    No validator-generated correction, output repair, retry or second judge exists.
16. For C=0/C>1 and local-gate rejection, use the exact preserved baseline result.
    Never resample baseline first/retry calls. Preserve baseline combined document
    failure semantics, exact right-to-left patching, overlap refusal and every
    untouched/protected slice.
17. Produce exactly the 007-i populations: DASSLE spelling all, exact frozen
    initial-u/v subset, spelling without initial-u/v, and corrected/reference
    preservation. Keep the name `initial-u/v`; do not redefine any subset.
18. Report the complete candidate transition matrix and per-operation counts. For
    SUBSTITUTION/INSERTION/DELETION separately report unique, reference-exact and
    non-reference candidates; validator USE/KEEP/UNCERTAIN/FAILURE; accepted/rejected
    reference-exact/non-reference/unresolved; and final TP/FP/FN contribution. Never
    infer semantic harm solely from non-reference status.
19. For new/changed candidates report exact-reference accepted/rejected,
    non-reference accepted/rejected, unresolved attribution, choice/failure counts,
    reference-exact sensitivity and reference precision among accepted mechanical
    proposals. Protocol failure remains separate from KEEP.
20. Compare primary NEW validated+fallback directly with frozen 007-i BEST
    validated+fallback for TP/FP/FN, precision/recall/F0.5, preservation changed cases
    and introduced edit units, protected/outside differences and operational failures,
    with exact deltas. Do not claim improvement from candidate/call count alone.
21. Record reused observations, fresh calls, total observations, baseline and fallback
    first/retry calls, projected total calls, new-call latency/reasoning/output-token
    statistics, candidate-search runtime and vocabulary-loading runtime. Scientific
    quality is primary; do not reject a better result because calls increased.
22. Add focused offline tests proving substitution/insertion/deletion distance one;
    identity excluded; two substitutions, substitution+insertion and transposition
    distance two; unique cross-operation candidate validation; cross-operation
    ambiguity fallback; English suppression before generation; exact-identity-only
    007-i reuse; new/different candidate fresh-call requirement; rejection/failure
    fallback; USE applies only supplied candidate; protected/outside integrity;
    deterministic worker partition/persistence/resume; no default network/live action.
    Encode no desired linguistic answers.
23. Before live calls, commit and push implementation/tests/order/active; verify remote
    head and clean state. Run focused tests, complete research tests, exact cached Ruff,
    compile/diff, publication/privacy, registry/table generation and transcript guards.
    The first live request must be the first frozen new candidate, not a probe.
24. Execute every independently runnable new-call candidate once. One candidate failure
    must not stop other candidates. A systemic service failure preserves evidence and
    permits offline reuse/fallback aggregation; do not fabricate missing decisions or
    resample. Aggregate once from durable observations without tuning.
25. Write private full configuration, manifests, reuse records, request/raw/observation
    records, case projections, RESULTS, REPORT and RUN-STATUS with owner-only modes.
    Public artifacts contain only data-free configuration, aggregate metrics, hashes,
    limitations and no row/sentence/target/candidate/response/reasoning body, endpoint,
    credential, profile path or private path.
26. Run the complete focused/research/full-pytest/OAP/native-baseline/transcript/
    governance/protected-source/prior-research/report-history/acquisition-history/
    whitespace/git-fsck checks and all final-head GitHub checks. Record PASSED, FAILED,
    BLOCKED and inherited results truthfully. Do not repair unrelated Application CI.
27. Perform the strongest-reason-not-to-publish review: incorrect Levenshtein semantics;
    transposition admitted; incomplete vocabulary; operation inversion; wrong old/new
    transition; uniqueness destroyed but candidate retained; false observation reuse;
    hidden baseline resampling; duplicate new call; uncertain-delivery resample; prompt/
    parser/inference drift; scorer/subset/fallback drift; denominator inflation; privacy
    leak; prior artifact mutation; or non-reference mislabeled harm. Correct only actual
    007-j defects without changing scientific settings.
28. Publish new data-free research config/result/report and minimal additive registry,
    numeric-table and navigation records; update PR #8 body through GitHub metadata
    only. Final report commit changes only
    `oap/reports/007-j-evaluate-standard-levenshtein-one-with-contextual-validation.md`,
    has the exact implementation/result head as sole parent, is pushed and remotely
    verified. Do not merge. Send exact `OK`, exit and STOP for human review.

## Acceptance criteria

- Frozen transition/population counts match requirements 3, 6 and 7 exactly.
- All 542 reused observations pass exact request/config/source identity checks; every
  other unique candidate has at most one durable fresh attempt and total new calls do
  not exceed 493.
- The unchanged validator/fallback yields complete operation, new-candidate and four-
  population metrics with exact 007-i deltas and protected/outside integrity evidence.
- Earlier public/private/OAP artifacts are byte-identical, private material remains
  private, PR #8 remains open/unmerged, and the immutable 007-j report is remotely
  verified.

## Verification

Run all checks named in requirements 22, 23, 26 and 27 at their exact relevant heads.
Offline tests must exercise actual candidate/reuse/persistence/projection boundaries,
with fake transport outside the strict wire/parser boundary. Final-head GitHub status
is evidence and cannot authorize unrelated repair or merge.

## Local setup and constraints

Use existing frozen dependencies, source rows/index/scorer and A100 profile. No source
acquisition or data redistribution. Native private evidence only; no system temporary
storage. Maximum 493 fresh validator requests, 8 in flight, one attempt each, 300
seconds each. Do not restart/reconfigure the Qwen service or shared Dropbox mount.

## Documentation

Publish a concise data-free report/config/result with the transition matrix, operation
analysis, new/changed-candidate validator behavior, all required population metrics,
exact 007-i deltas, reuse/new-call/runtime accounting, limitations and skeptical
conclusion. Add only minimal archive navigation and deterministic numeric-table output.

## Git and report publication

Same branch and PR #8. Implementation/tests and pre-live freeze-capable code are
committed/pushed before live execution. Public results/navigation/table precede the
final immutable report-only commit. No amendment of earlier order/report/research
records, no merge, and no next experiment.

## Decision classification

D0. Human direction resolves the scientific choice. The indexed exact-distance
implementation and strict reuse mapping are bounded reversible experimental mechanics.

## Deferred human adjudication

- Decision: NONE
