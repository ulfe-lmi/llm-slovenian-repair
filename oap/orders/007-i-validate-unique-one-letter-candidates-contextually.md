# Work order 007-i — Validate unique one-letter candidates contextually

Status: FINAL

```oap-metadata
{
  "id": "007-i",
  "title": "Validate unique one-letter candidates contextually",
  "objective": "007",
  "status": "FINAL",
  "phase": "DEMONSTRATOR",
  "repository": "ulfe-lmi/llm-slovenian-repair",
  "default_branch": "main",
  "base_sha": "ee2d1b479719009ff1d07829478f241e3f395f7c",
  "branch": "oap/007-concept-verification",
  "pr_mode": "AMEND_EXISTING_PR",
  "pr": 8,
  "dependencies": ["001", "002", "003", "004", "005"],
  "local_work": "Clean local/remote objective-007 branch at strict-valid 007-h report commit e916a2d9cafd171fbb71bfc70ac2c21222118f6e. Preserve all prior repository/private research evidence byte-for-byte. No 007-i implementation or validator call has run.",
  "prior_review": "007-h is strict-remote-verified with report-only head e916a2d and artifact parent 608f73b. PR #8 is OPEN/UNMERGED, main remains ee2d1b4, CRITICAL is empty and branch protection remains disabled. 007-h produced 571 spelling plus 164 preservation unique eligible candidates. Research reproducibility and report history are green; inherited/static Application and OAP bootstrap failures are durably classified and explicitly outside this round.",
  "provenance": [
    {"kind":"H","reference":"Owner directs one new paired contextual binary-validator experiment over every 007-h unique candidate, exact frozen prompt/model/low effort, shared responses for VALIDATED+FALLBACK and VALIDATED-ONLY, no existing-response resampling, no unrelated CI repair, no merge and STOP after permanent reporting."},
    {"kind":"E","reference":"Verified 007-h private cases contain 1,487 spelling plus 1,486 preservation rows and exactly 571 plus 164 unique mechanically eligible candidate targets; frozen private aggregate/config/input identities and exact candidate source hashes are recorded in the mandatory reconnaissance."},
    {"kind":"I","reference":"Strategic bounded reconnaissance f58a58249a5c40e9b94e33bc669b189ef9ccbc20475b17317a12ccd704d2088b fixes exact live request/response, persistence, eight-worker scheduling, attribution, projection, failure and call-accounting semantics before any model call."},
    {"kind":"A","reference":"S-ORDER-03 permits suffix 007-i on the same objective branch/PR after remotely verified 007-h; explicit human direction authorizes the bounded A100 live validator calls while preserving merge/release/deployment and unrelated-CI boundaries."}
  ],
  "governance": {
    "AGENTS.md": "118be17b2e9e5b50d628c796b136ee1b8154ebf844af0007839f65848fd69f09",
    "ARCHITECTURE-for-agents.md": "e83545d648b32263110f532d9425c785abdcd96be5e37d0ce055f57cc2ec9491",
    "ARCHITECTURE.md": "a16e0f87bdb21f6920aa63119cf5be148b6c8b68bf1337c44006d54b7a7261f7",
    "OAP-COMMUNICATION-coding-agent.md": "6355623830eb5ef47523d04b1a6bf958685f4debd16b62f9f46c3f14de01020b",
    "PLAN.md": "d2aa1d98cc5177ac6093ab3aeb79903b192780910ef2d1712839b474fb5adbf0",
    "SECURITY.md": "0424b58bdaae1d4379364e3470b7cb4b2b729a20456d30e5c1ea59a3edbed7a6",
    "TESTING.md": "68a3307289f684910281b9d4947336f3a390924e229e7aafce7212b4a0fcc5d1",
    "oap/coding-instructions/AGENTS.md": "55bc5d72200cd3e25e1d8decd629d902ffb732c8808bae8ef36a6081b4416514",
    "oap/governance/DISTILLATION-MAP.md": "a03d4be8d70101dc2d038bc5e8bd98e18fcaf3806539651b37d0e85f2558c2d7",
    "oap/governance/WORKSPACE-LAYOUT.json": "cba4ae2226038a44d74bff2eb727bc79e930b34f8f657b1e14c411a4ca09d254",
    "oap/prompts/coding-round.md": "fa94f21c065209d284978f7f731b95600cbab87949d0c8ae2c226a0923552611",
    "oap/prompts/ica-start.md": "2311778a8c2f8cbfa24bbc9be1eed30d14289f3d82c2b5e8da70d806b12d70f4",
    "oap/prompts/strategic-start.md": "40ccb00e48c9b1b6ae93d5e5a334322b8a249d655341e47a123e5321b65b6e9b",
    "oap/strategic-instructions/AGENTS.md": "ac05494856ac8c85c31b77225ac96abe9596f0d0ae50d96e338da7370669fa5c",
    "oap/strategic-instructions/OAP-COMMUNICATION-strategic.md": "6ba11ddcde24ed3d8777f305951d706d3fc4a1869470be9669a9853d3ce15cbf",
    "oap/strategic-instructions/strategic_model_init_material.md": "813edbc94f0a991abda046a544beb22510a83c9b45dc5282f046dd71a324465d"
  },
  "lr": ["LR-002", "LR-003", "LR-007", "LR-008", "LR-012", "LR-013", "LR-014"],
  "relevant_gates": [],
  "required_checks": ["Research reproducibility", "OAP bootstrap acceptance", "OAP report history", "Application baseline"],
  "decision_class": "D0"
}
```

## Identity

Immediate human-authorized experimental suffix after immutable 007-h, on the SAME
objective-007 branch and PR #8. This order introduces exactly one scientific
variable: one contextual binary validator call for every 007-h unique eligible
mechanical candidate. It neither merges nor repairs unrelated CI.

Mandatory private sources, read in full:

- `STRATEGIC_HOME/workorders/007-i-contextual-validator-reconnaissance.md`, SHA-256
  `f58a58249a5c40e9b94e33bc669b189ef9ccbc20475b17317a12ccd704d2088b`;
- `STRATEGIC_HOME/workorders/007-i-validator-prompt.txt`, SHA-256
  `572cf2fb4864e66e38a500465e1089062d58aeed4721063fb0c1e466e424230d`.

The second source is the exact frozen prompt. Do not reword, wrap, translate,
append examples, add JSON instructions or tune it.

## Provenance

The owner's current direction is the sole authority for the new scientific
variable, prompt, projections, exclusions and stop condition. The mandatory
reconnaissance establishes exact repository/runtime facts and closes ordinary
execution choices before sampling. No generated roadmap item or prior experiment
is treated as human acceptance.

## Current verified state

Remote main is `ee2d1b479719009ff1d07829478f241e3f395f7c`. Local, remote
branch and PR #8 head are `e916a2d9cafd171fbb71bfc70ac2c21222118f6e`,
the immutable 007-h report-only commit with artifact parent `608f73b`. PR #8 is
OPEN and UNMERGED; CRITICAL is empty. The current state is REVIEW_READY for
007-h. Research reproducibility and report history are green; inherited/static
Application and OAP bootstrap failures are recorded and outside this round.

## Governance

S-AUTH-01, S-PRODUCT-02/03/04, S-ORDER-01/03, S-EVIDENCE-01,
S-DIAGNOSE-01, S-PROTOCOL-01 and S-REVIEW-01 apply. Explicit owner authority
covers this bounded live experiment against the existing model endpoint; it does
not authorize server mutation, merge, production integration, release or deployment.

## Goal and dependencies

Measure whether the same Qwen deployment can contextually accept useful 007-h
unique one-substitution candidates while rejecting preservation/non-reference
candidates strongly enough to recover precision, with and without frozen baseline
fallback. This is an experiment, not product integration or linguistic acceptance.

## Scope

Add a small 007-i validator/driver and focused tests under `research/`; create one
new owner-controlled native private experiment root outside Git; stage exact 007-h
inputs there; freeze configuration before live execution; make the exact bounded
validator calls; persist every call before aggregation; derive two projections;
publish data-free config/result/report artifacts plus the immutable OAP report.
Add only the minimal navigation/registry entries required by current research
archive conventions. Preserve all earlier records.

## Non-goals

No candidate-rule, detector, English policy, corpus/index, fallback reviewer,
retry, scoring, benchmark, model/deployment, effort, sampling, token limit, stop
condition, mechanical gate or patching change. No generated correction from the
validator, second judge, validator retry, resampling, prompt tuning, threshold,
frequency/context tie-break, insertion/deletion/transposition, morphology,
`--slovenize`, Codex/proxy/SSE, second deployment, production integration,
unrelated CI repair, merge, release or deployment.

## Files and boundaries

Allowed repository paths are the new 007-i validator/driver/focused-test files,
new data-free 007-i configuration/result/report records, and minimal additive
research navigation/registry entries. Existing research and production files may
be imported but not semantically changed; a shared helper may be touched only if
strictly required without changing historical behavior. Private filled prompts,
sentences, targets, candidates, raw responses, reasoning, credentials and endpoint/
profile paths remain only in the new native experiment root. The exact 007-i order,
active pointer and final report path are the only OAP mutations.

## Requirements

1. Start from exact local/remote head `e916a2d9cafd171fbb71bfc70ac2c21222118f6e`,
   main `ee2d1b479719009ff1d07829478f241e3f395f7c`, active 007-i after publication,
   branch `oap/007-concept-verification`, open/unmerged PR #8, and no unrelated
   work. Preserve every prior order/report/research/private artifact byte-for-byte.
2. Create a unique owned non-symlink private 007-i root under the existing native
   research-runtime parent. Never use the system temporary directory. Copy only
   exact required 007-h private inputs/cases and manifests, verify size/hash/regular
   file/ownership identity, and never modify their sources.
3. Verify the frozen 1,487 spelling and 1,486 preservation case populations and
   exact baseline replay before live execution. Derive exactly 571 spelling and
   164 preservation scheduled candidates from persisted 007-h targets whose
   cardinality is one and existing mechanical gate is accepted. Verify their
   stable identities and existing exact target/candidate coordinates. Any mismatch
   stops before a model call.
4. Reuse the exact 007-h candidate rule and persisted candidate: English suppression
   first; same frozen unigram vocabulary; original/candidate same Unicode-code-point
   length; exactly one differing alphabetic position; all others identical; exactly
   one EXACT-unigram candidate; no rank/tie/frequency/context heuristic. Recompute
   only as an integrity assertion, never to alter the population.
5. Freeze immutable `CONFIGURATION.json` before live execution. Include all input,
   population, 007-h, implementation, prompt, profile, endpoint/model, scheduling,
   parser, projection, failure, scorer, privacy and zero-resampling identities.
   Record private endpoint/profile identities only privately. Freeze a stable
   ordered 735-candidate manifest and its SHA-256.
6. Use the same verified A100-FP8 Qwen deployment/profile and model
   `qwen3.8-27b`. Read its bearer into the live process only without logging or
   copying it. Use Responses, non-streaming, `store=false`, `include_reasoning=true`,
   explicit `reasoning.effort="low"`, no added sampling/max-output/stop fields,
   300-second timeout and 2,000,000-byte response bound. Do not mutate the server.
7. For every scheduled target make exactly one fresh call containing only the
   complete original case sentence, exact original target text and exact existing
   mechanical candidate in the frozen prompt. The call asks only for validation;
   the response cannot introduce a replacement. No existing baseline Qwen response
   is resampled.
8. Accept a response only when HTTP/protocol/model/completion/low-reasoning evidence
   verifies and exactly one assistant output-text field, after surrounding-whitespace
   framing removal, equals `USE_CANDIDATE`, `KEEP_ORIGINAL` or `UNCERTAIN`.
   Additional text, markup, fields, wrong effort/model, missing token accounting,
   timeout or malformed transport is a named protocol/operational failure.
9. Use 8 deterministic disjoint workers partitioned by the stable ordered key
   `(phase, case index, target start, target end, target ordinal)`, modulo 8,
   ascending per worker. Each worker is synchronous with at most one in-flight call.
   Prove no duplicate/missing assignment and at most 8 concurrent requests.
10. Persist immutable request bytes before dispatch, raw bounded response/timing
    before parsing, then parsed observation. Completed observations are never
    overwritten/resampled. A request lacking raw response on restart becomes
    `INTERRUPTED_UNCERTAIN_DELIVERY_NO_RESAMPLE`; count it as failure and never call
    it again. Checkpoint progress and support resume by exact freeze/key hashes.
11. Validator failure and `UNCERTAIN` conservatively reject the candidate but are
    distinct from model `KEEP_ORIGINAL`. Do not repair output syntax or make a
    follow-up call. Actual new model-call count must equal the number of scheduled
    candidates with a newly attempted or durably uncertain request, never exceed 735,
    and contain validator calls only.
12. Derive `VALIDATED+FALLBACK` from each same response: `USE_CANDIDATE` applies
    the exact 007-h mechanical edit; every other decision/failure rejects it and
    executes the exact persisted baseline target result. Preserve the 007-h combined
    document-level fail-closed behavior for fallback operational failures. No
    baseline call is made in this experiment.
13. Derive `VALIDATED-ONLY` without a model call: `USE_CANDIDATE` applies the exact
    mechanical edit; all other decisions/failures leave that target original. No
    frozen baseline output or failure affects this projection. Apply compatible
    edits right-to-left and preserve every other original slice exactly.
14. Compute isolated reference attribution for all 735 mechanical candidates with
    the unchanged 007-h scorer, including 20 candidates previously unlabelled due
    document rollback. Separate exact-reference, non-reference and unresolved.
    Never label a non-reference edit harmful without direct ground truth.
15. Produce complete spelling, exact frozen 75-case initial-u/v, spelling-minus-u/v,
    and corrected/reference preservation views for both projections. Use the same
    row identities, mapping and token-coordinate scorer as 007-h; do not tune from
    any outcome.
16. Report scheduled/actual calls, protocol failures, decision counts, unconditional
    and valid-response acceptance rates; accepted and rejected exact-reference/
    non-reference/unresolved candidate counts; known-good reference-exact sensitivity
    with explicit denominators; preservation-candidate rejection rate; TP/FP/FN,
    precision/recall/F0.5; changed preservation cases/edit units; protected/outside
    differences; operational failures; and direct comparisons to frozen baseline
    and unrestricted 007-h.
17. For each projection report projected baseline first/retry calls retained,
    ordinary first/retry calls avoided, 735 validator calls added, net projected
    calls and delta versus baseline/007-h. Report actual validator wall/HTTP time,
    median/p95/max latency, reasoning and output-token distributions, worker runtime
    and throughput. Keep actual calls separate from counterfactual projected calls.
18. Add focused offline tests for exact prompt/body/hash; parser positive choices
    and rejection of extra/JSON/duplicate/wrong fields; exact low-reasoning/model
    verification; candidate population equality with 007-h; deterministic disjoint
    partitions; immutable persistence/resume/uncertain-delivery; one-call maximum;
    no baseline-call path; both response-shared projections; failure semantics;
    isolated attribution; document rollback distinction; exact patching; protected/
    outside integrity; metric/call accounting; no default live/network access.
    Use fakes outside the actual transport/parser boundary and encode no desired
    linguistic answers.
19. Before live calls, commit/push code/tests and verify remote/clean. Run focused
    tests, complete research tests, staged publication/privacy guard, exact cached
    Ruff and relevant static checks. Do not repair unrelated historical/Application
    findings; record them separately. The first live request is a scheduled candidate,
    not an exploratory probe.
20. After all independently executable scheduled calls, aggregate exactly once from
    persisted evidence. A candidate failure does not stop unrelated candidates.
    If the service becomes systemically unavailable, retain all evidence and complete
    every offline projection/report possible without fabricating missing decisions.
21. Write private full configuration, candidate manifest, requests/raw responses,
    parsed observations, per-case projections, RESULTS, REPORT, MANIFEST and RUN-STATUS.
    Keep owner-only modes. Public artifacts contain only prompt template/hash,
    deployment/model class, aggregate metrics, stable hashes and limitations—no
    sentence, target, candidate, response/reasoning body, credential, endpoint,
    profile path, dataset row or private path.
22. Run the complete focused/research/full-pytest/OAP/native-baseline/transcript/
    governance/protected-source/prior-research/report-history/acquisition-history/
    whitespace/git-fsck checks and final-head GitHub checks. Preserve PASSED, FAILED,
    BLOCKED and inherited classifications. Unrelated CI failures do not authorize
    repair, test weakening or false green claims.
23. Perform a strongest-reason-not-to-publish review: wrong 735 population;
    candidate-generation drift; original/candidate inversion; prompt drift; hidden
    fallback resampling; duplicate validator call; uncertain-delivery resample;
    parser permissiveness; response reuse divergence between projections; scorer or
    document-failure drift; raw text/privacy leak; denominator inflation; confusing
    non-reference with harm; or silent prior-artifact mutation. Resolve only actual
    007-i defects without tuning linguistic behavior.
24. Publish new data-free research config/result/report records and minimal navigation
    additively; update PR #8 body through metadata only. Final report commit changes
    only `oap/reports/007-i-validate-unique-one-letter-candidates-contextually.md`,
    has the exact implementation/result head as sole parent, is pushed and remotely
    verified. Do not merge. Send exact `OK`, exit and STOP for human review.

## Acceptance criteria

- The candidate manifest is exactly the 735 unique eligible 007-h targets and the
  candidate-generation integrity check shows zero drift.
- Every candidate has at most one durable new validator attempt; all other model
  decisions are frozen reuse, and both projections share the same validator record.
- Prompt/request/model/effort/parser and eight-worker scheduling match this order.
- Both projections and all four required views have complete, denominator-explicit
  metrics, call accounting and protected/outside integrity evidence.
- Earlier research/OAP bytes are unchanged, private material remains private,
  PR #8 remains open/unmerged, and the final immutable report is remotely verified.

## Verification

Run every named check in requirements 18, 19, 22 and 23 at the exact relevant
implementation/result head. Focused tests must exercise the real prompt/parser,
persistence, partitioning, projection and scorer boundary with fake transport only
before live execution. Final-head GitHub checks are evidence, not permission to
repair excluded inherited CI or merge.

## Local setup and constraints

Use the existing owner-controlled native research runtime, exact cached dependencies,
verified 007-h cases/index/scorer and existing A100 profile. No new corpus acquisition,
package/data redistribution, service mutation or system-temporary storage. Maximum
live budget is 735 validator requests, 8 in flight, 300 seconds each, one attempt per
candidate. Preserve raw traces privately for scientific audit.

## Documentation

Publish a concise data-free report/config/result with exact denominators, both
projection metrics, call/latency accounting, direct frozen-baseline and 007-h
comparisons, limitations and a skeptical conclusion. Add minimal navigation so the
record is discoverable. Full raw evidence and executable resume instructions stay
private and hash-linked.

## Git and report publication

SAME branch and PR #8. Implementation/test commit precedes the private freeze/live
run. Public aggregate artifacts and additive navigation precede the final OAP report.
The OAP report is immutable/report-path-only with its actual implementation/result
parent. No merge, release, deployment or next experiment.

## Decision classification

D0. Human authority fixes the experiment. Deterministic partitioning, strict parsing,
conservative failure and paired projection accounting are reversible bounded
implementation choices with no remaining D1/D2 dilemma.

## Deferred human adjudication

- Decision: NONE
