# Work order 008-f — Blind hidden acceptance of the frozen parser-first protection layer and final objective-008 scientific conclusion

Status: FINAL

Finalized by strategic review of 008-e (final head
ac6315bdef2e65448bcf0eb1f6653b80741c164d, report-only commit over implementation head
4dfbdac6848d6b4abd930550088fd6b67be7a396, base c0786f9898d3a97aefe9d063bcd43210c0d7d2f1) after the 008-e
forward-recovery quarantine of the machine-invalid 008-d report and
restoration of transcript coherence. This round is the round the 008-c
design pre-allocated (as '008-d', renumbered through 008-e) for the
blind hidden acceptance of the frozen parser-first structural protection
implementation, the independent naturalistic label adjudication, the
end-to-end preservation verification on the hidden set, and the final
objective-008 scientific conclusion (PASS / CONDITIONAL / FAIL per the
predeclared decision states). The implementation is evaluated EXACTLY as
frozen at the 008-c final head 4b6a90ab13c0c9a02e533a2673a084e26f841a5c
(byte-identity of every frozen surface is a hard pre-requirement; the
008-d/008-e rounds changed no implementation byte). The hidden
acceptance set (2,000 deterministic mashup documents with machine-known
ground truth plus 70 naturalistic whole-response outputs, 4,091 sealed
files, committed manifest
research/prose-boundary/corpus/manifests/hidden-manifest.json sha256
8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18) is
evaluated for the first and only time as an acceptance instrument in
this round; its contents and labels were unavailable to the
implementation agent by seal contract and remain private (counts,
categories, and hashes in public artifacts only). Any material defect
found here is RECORDED and dispositioned, never fixed in-round:
corrective work, if required, is a separate 008-g with the evidential
status of this hidden set explicitly downgraded (and a new independent
hidden set generated and sealed if necessary). No merge, no auto-merge,
no deployment, no release, no milestone claim in this round. No
objective-009 data of any kind is touched. No new generation or
linguistic-pipeline model call; the only model use in this round is the
authorized naturalistic adjudication process (classification and
counting recorded in the report).

```oap-metadata
{
  "id": "008-f",
  "title": "Blind hidden acceptance of the frozen parser-first protection layer and final objective-008 scientific conclusion",
  "objective": "008",
  "status": "FINAL",
  "phase": "DEMONSTRATOR",
  "repository": "ulfe-lmi/llm-slovenian-repair",
  "default_branch": "main",
  "base_sha": "ac6315bdef2e65448bcf0eb1f6653b80741c164d",
  "branch": "oap/008-prose-boundary-qualification",
  "pr_mode": "AMEND_EXISTING_PR",
  "pr": 9,
  "dependencies": ["008"],
  "local_work": "Preserve byte-for-byte: every merged 000-007 seam on main, the entire research/ tree at the 008-e final head, ALL frozen 008-c implementation surfaces (research/curated/prose_boundary.py, research/curated/protected.py, research/prose-boundary/config/ structural-policy-v2.json and experiment-008c.json, research/prose-boundary/adapter/, research/prose-boundary/identity/, research/tests/test_parser_first_protection.py), the 008-a frozen baseline, the committed dev corpus and labels, the hidden manifest, every test file, scripts/verify_development_baseline.py, all workflows, every order and report byte, and any unrelated local work (untracked residue never committed).",
  "prior_review": "Strategy independent final-head review of 008-e (2026-09-18/19, private workorders/008-e-final-head-review-20260918.md): report-only commit ac6315bdef2e65448bcf0eb1f6653b80741c164d (sole parent 4dfbdac6848d6b4abd930550088fd6b67be7a396, the corrected implementation head; sole changed path oap/reports/008-e-report-republication-and-transcript-recovery.md, 77,271 B, content sha256 c57fcf598bb1eb85fe21287527fe9e13fa92dfc6d4ab6ab2a113542a2301aeb9, git blob a7266e6ba27556d30d5a5a6dabcada9306665b39); 17-check census clean (zero angle-bracket spans and zero standalone uppercase placeholder-vocabulary tokens; the 008-d defect class absent); strategy remote verify_report = verified and check_transcript at the final head = valid (008-d quarantined with corrective_id 008-e; 49 orders / 46 valid reports / report_count 49; the two frozen 006-a/006-c incidents intact); the disclosed in-round ledger correction 4dfbdac (63-hex truncation of the 008-e order sha256 in the 008-e registry entry, caught by the pre-push gate before publication; corrected value b8c8b11f49634fa5cd3f81dce64952d206aecbab2250cedd6efd6f8c76ef80f0; canonical-form self-citation 638333a7b085c05a36a10f29522acdccbfab9dd43e1408cc79b8fb8ba1d2339b round-trip verified) independently verified; 008-d order/report bytes identical at base and final head; full diff base..final = exactly the 8 in-scope files; hidden seal strategy-re-verified data-free 4091/4091 (manifest sha256 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18 unchanged); response OK frame received; round ACCEPTED as published; transcript coherence RESTORED at the final head.",
  "provenance": [
    {"kind": "H", "reference": "Human research decision 2026-09-17 (objective-008 strengthening): the hidden acceptance set must be evaluated blind against the frozen implementation; naturalistic labels require independent adjudication from a frozen annotation guide without consulting the implementation under test, with ambiguity labelled explicitly and independent-review reconciliation; failure decomposition and the predeclared PASS/CONDITIONAL/FAIL decision states govern the final objective-008 conclusion; 008-f (renumbered from the 008-c design's '008-d') is that round. Human instruction 2026-09-18 (current): keep the loop moving; all four required checks genuinely green at the reviewed final head; no weakening/skipping/redefining any check; no manufactured acceptance of any kind; the owner is not a terminal relay; D0/D1 resolved by strategy."},
    {"kind": "A", "reference": "The 008-c order scope items 8 (sealed hidden set), 15 (hidden-acceptance metrics protocol: safety, coverage, coordinates, residual-class breakdown, malformed-behaviour table, performance), 16 (frozen annotation guide: category vocabulary, implementation-free decision procedure, AMBIGUOUS labelling rule, reconciliation rule); S-REVIEW-01, S-MERGE-01 (merge is a separate post-review strategic act, not part of this round), S-EVIDENCE-01 (data-free public artifacts; distinct honest recording), S-ORDER-02/03 (corrective/final suffix on the same branch and PR), S-ICA-01/02 (evidence-layer separation: structural-safety evidence is NOT linguistic acceptance), the 008-c recorded D1 (hidden-set mechanics: independent generation streams, private root, seal before freeze, strategy control)."},
    {"kind": "E", "reference": "008-e final-head CI truth at ac6315bdef2e65448bcf0eb1f6653b80741c164d, independently re-verified via the GitHub API (2026-09-19): Research reproducibility run 35402539050 SUCCESS (28s, the designed pre-report count assertion mechanically green: 49 report files - 1 exclusion = 48 = oap_reports_reviewed); OAP bootstrap run 35402539062 acceptance SUCCESS (5m43s) + report history SUCCESS (7s) - the deterministic RECOVERY_LINKAGE_MISSING cleared by the committed prior_report_recovery linkage, the 008-e round's purpose; Application baseline run 35402539059 SUCCESS (7m42s) DIRECT green, all 13 driver stages PASSED including full pytest at the frozen 300.0s per-command cap (no rerun needed). Hidden seal state at the 008-e final head: strategy independent data-free re-hash of the private root = 4091/4091 files sha256+size verified, 0 missing, 0 mismatch, 0 unexpected files within the sealed tree (documents 2000 / labels 2000 / naturalistic 15 / components 76); outside the sealed tree only the disclosed hidden-manifest-seal.json copy and 386 pre-seal generation receipts (the same disclosed finding as 008-c/008-d); committed manifest sha256 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18 (strategy-recomputed MATCH); zero content reads. Frozen-surface byte-identity proof against 4b6a90a: the full diff c0786f9898d3a97aefe9d063bcd43210c0d7d2f1..ac6315bdef2e65448bcf0eb1f6653b80741c164d is exactly the 8 in-scope 008-e files (STATUS.md, oap/GENERATED-FILES.json, oap/active, the 008-e order, the 008-e report, research/RESEARCH-STATE.md, research/registry/experiments.json, research/tables/experiment-summary.csv) - every frozen 008-a/008-c surface, the 007-m frozen artifacts, all test files, workflows, and every historical order/report byte-identical (strategy spot checks incl. research/curated/prose_boundary.py, research/curated/protected.py, hidden-manifest.json, the 008-c order and report: IDENTICAL)."},
    {"kind": "I", "reference": "Strategy independent final-head reviews of 008-e, 008-d, 008-c (private workorders/); the 008-c order (design authority for this round); the committed frozen annotation guide research/prose-boundary/config/annotation-guide.json and the committed hidden manifest."}
  ],
  "governance": {"AGENTS.md": "118be17b2e9e5b50d628c796b136ee1b8154ebf844af0007839f65848fd69f09", "ARCHITECTURE-for-agents.md": "e83545d648b32263110f532d9425c785abdcd96be5e37d0ce055f57cc2ec9491", "ARCHITECTURE.md": "a16e0f87bdb21f6920aa63119cf5be148b6c8b68bf1337c44006d54b7a7261f7", "OAP-COMMUNICATION-coding-agent.md": "6355623830eb5ef47523d04b1a6bf958685f4debd16b62f9f46c3f14de01020b", "PLAN.md": "d2aa1d98cc5177ac6093ab3aeb79903b192780910ef2d1712839b474fb5adbf0", "SECURITY.md": "0424b58bdaae1d4379364e3470b7cb4b2b729a20456d30e5c1ea59a3edbed7a6", "TESTING.md": "68a3307289f684910281b9d4947336f3a390924e229e7aafce7212b4a0fcc5d1", "oap/coding-instructions/AGENTS.md": "55bc5d72200cd3e25e1d8decd629d902ffb732c8808bae8ef36a6081b4416514", "oap/governance/DISTILLATION-MAP.md": "a03d4be8d70101dc2d038bc5e8bd98e18fcaf3806539651b37d0e85f2558c2d7", "oap/governance/WORKSPACE-LAYOUT.json": "cba4ae2226038a44d74bff2eb727bc79e930b34f8f657b1e14c411a4ca09d254", "oap/prompts/coding-round.md": "fa94f21c065209d284978f7f731b95600cbab87949d0c8ae2c226a0923552611", "oap/prompts/ica-start.md": "2311778a8c2f8cbfa24bbc9be1eed30d14289f3d82c2b5e8da70d806b12d70f4", "oap/prompts/strategic-start.md": "40ccb00e48c9b1b6ae93d5e5a334322b8a249d655341e47a123e5321b65b6e9b", "oap/strategic-instructions/AGENTS.md": "ac05494856ac8c85c31b77225ac96abe9596f0d0ae50d96e338da7370669fa5c", "oap/strategic-instructions/OAP-COMMUNICATION-strategic.md": "6ba11ddcde24ed3d8777f305951d706d3fc4a1869470be9669a9853d3ce15cbf", "oap/strategic-instructions/strategic_model_init_material.md": "813edbc94f0a991abda046a544beb22510a83c9b45dc5282f046dd71a324465d"},
  "lr": ["LR-001", "LR-003", "LR-007", "LR-013", "LR-014"],
  "relevant_gates": [],
  "required_checks": ["Application baseline", "Research reproducibility", "OAP bootstrap acceptance", "OAP report history"],
  "decision_class": "D0"
}
```

## Identity

Final evaluation round of objective 008 (further suffixes only for
correction of this round's own publication or a post-verdict corrective
path under strategy). 008-a qualified pulldown-cmark 0.13.4 (GO,
supporting evidence only); 008-b restored research-state identities;
008-c implemented and FROZE the parser-first protection layer, built
and sealed the generated structural evaluation program (1,234
components / 76 families; 5,000 mashup documents: 3,000 development
committed with machine-known ground truth, 2,000 hidden sealed; 150
naturalistic whole-response outputs: 80 development / 70 hidden);
008-d observed the application-baseline pool regime and registered the
ledger (report later quarantined); 008-e quarantined the 008-d report
by forward recovery and restored transcript coherence. This round
executes the blind hidden acceptance and issues the final objective-008
scientific conclusion. It evaluates, it does not implement: no frozen
surface changes, no corpus changes, no gate changes.

## Provenance

- H: as metadata provenance H.
- A: as metadata provenance A.
- E: as metadata provenance E (to be completed from the 008-e
  final-head review before publication).
- I: as metadata provenance I.

## Current verified state

main = 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9 (unchanged). PR #9 OPEN / UNMERGED / MERGEABLE / auto-merge DISABLED, base main, head ac6315bdef2e65448bcf0eb1f6653b80741c164d (the 008-e final head); all four required checks green at that head (runs 35402539050 / 35402539062 acceptance + history / 35402539059). oap/active = 008-f (set by this publication; was 008-e); workorders/consumed.json = 008-e / consumed / recovery:false / pr 9. Hidden seal re-verification at publication: strategy independent data-free re-hash 4091/4091, manifest sha256 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18 unchanged, zero content reads. Frozen-surface byte-identity vs 4b6a90ab13c0c9a02e533a2673a084e26f841a5c: verified by the full base..final diff above (only the 8 in-scope 008-e files differ from base; no frozen-surface byte differs at ac6315b). Local worktree: the 008-e round's untracked residue preserved and never committed (.research-test-scratch/, corpus/); no other dirty tracked file at publication.

## Governance

S-REVIEW-01, S-MERGE-01, S-ORDER-02/03, S-EVIDENCE-01, S-ICA-01/02,
S-DECIDE-01 (D0: bounded evaluation and bookkeeping inside the risk
budget). No D1 is created in this round (the hidden-set mechanics D1
was recorded and accepted in 008-c; this round executes it). No CRIT
admission (five conditions fail at condition 1 - the human decision of
2026-09-17 defines the evaluation and the decision states).

## Goal and dependencies

Evaluate the frozen parser-first protection implementation against the
sealed hidden acceptance set exactly once, as an acceptance instrument:
safety, prose coverage, coordinate integrity, residual-recognizer
performance, malformed-input determinism, and performance on the
2,000 hidden mashup documents; independently adjudicate the 70 hidden
naturalistic whole-response outputs per the frozen annotation guide
(implementation-free; ambiguity explicit; independent-review
reconciliation); verify end-to-end preservation invariants 1-7 on the
hidden set; apply the predeclared decision states to issue the final
objective-008 scientific conclusion (PASS / CONDITIONAL / FAIL);
register the round in the ledger and machine block; publish the
data-free report. The four required checks must be genuinely green at
this round's final head under the established bounded
zero-failure-TIMEOUT observation policy for the Application baseline
(predeclared BLOCKED permitted; no gate change).

Dependencies: 008-c (frozen implementation + sealed hidden set +
annotation guide + metrics protocol); 008-e (coherent transcript
state; consistent ledger). No other dependencies.

## Scope

1. Pre-evaluation integrity gates (requirement 1):
   (a) frozen-surface byte-identity: git diff of every frozen 008-c
       implementation surface (local_work list) against the 008-c final
       head 4b6a90ab13c0c9a02e533a2673a084e26f841a5c must be EMPTY;
   (b) hidden seal: re-hash the private-root hidden content against the
       committed hidden-manifest.json (expect 4091/4091; manifest
       sha256 8233bdcf... unchanged; counts/hashes only, zero content
       reads outside the evaluation harness);
   (c) remote reconciliation: PR #9 open/unmerged, head == the 008-e
       final head, main unchanged; oap/active == 008-f after
       activation.
   Any failure: STOP, report BLOCKED with the exact mismatch.
2. Hidden mashup evaluation (requirement 2): run the frozen
   implementation over all 2,000 hidden documents via a new committed
   deterministic evaluation harness
   (research/prose-boundary/tools/hidden_acceptance.py, NEW in this
   round; evaluation tooling only - it CALLS the frozen implementation
   through its public entry points, changes no frozen surface, reads
   the private root, and emits data-free aggregates only; committed so
   the evaluation is reproducible from the sealed private root).
   Compute the full 008-c item-15 metrics set: safety (protected bytes
   exposed as prose - target 0; protected regions with any overlap into
   candidate prose - 0; machine-significant component exposure rate;
   malformed-structure unsafe exposure rate with the frozen
   policy-decided-suppression distinction), coverage (expected prose
   bytes exposed; expected prose regions completely available;
   unnecessary suppression by container type - actual losses, no
   arbitrary threshold), coordinates (UTF-8 boundary violations - 0;
   byte/code-point conversion mismatches - 0; source slice mismatches -
   0; patch-preservation violations - 0), residual-recognizer
   performance broken down by class (TeX; JSON/config; shell; paths;
   identifiers; numbers/units; URLs; other), malformed-input
   deterministic behaviour table, performance (parser + residual
   latency at ~1KB/~10KB/~50KB representative sizes, ms/document and
   peak RSS; qualitative cheapness statement only). Commit ONE
   data-free aggregate JSON under
   research/prose-boundary/results/hidden-acceptance/mashup-metrics.json
   (counts, categories, rates, hashes; NO case content, NO label
   content, NO private paths). The harness and its output must be
   deterministic (fixed iteration order; re-run stability check on a
   bounded sample of e.g. 50 cases, recorded).
3. Naturalistic adjudication (requirement 3): the 70 hidden
   naturalistic whole-response outputs are labelled INDEPENDENTLY of
   the implementation under test, per the committed frozen annotation
   guide (research/prose-boundary/config/annotation-guide.json),
   executed by a new committed adjudication driver
   (research/prose-boundary/tools/naturalistic_adjudication.py, NEW in
   this round; it fixes the adjudication prompt - the frozen guide
   plus one response - and records model identity, call count, and
   outputs): fresh adjudication context(s) given ONLY the guide plus
   each response
   (never the implementation source, never the implementation's output
   on the response, never development labels); category vocabulary
   per the guide (genuine prose; structural protect; machine-
   significant residual; delimiter/whitespace neutral; AMBIGUOUS with
   explicit labelling, no forced certainty); the guide's independent-
   review reconciliation rule applied (second pass over all
   AMBIGUOUS and a deterministic sample, e.g. 20 percent, of the rest;
   disagreements recorded with the reconciliation outcome). Adjudicated
   label files are written ONLY to the private root and sealed by a
   NEW committed content-free manifest (case IDs + sha256/size;
   research/prose-boundary/corpus/manifests/hidden-annotations-
   manifest.json); the report records: category distribution,
   AMBIGUOUS count, reconciliation statistics, the adjudication model
   identity and call count (authorized evaluation-process use,
   classified and counted, distinct from generation or linguistic-
   pipeline calls), and the guide identity.
4. End-to-end preservation invariants on the hidden set (requirement
   4): invariants 1-7 (candidate detector input cannot include
   protected source ranges; local n-gram/context windows do not bridge
   across protected spans; reviewer targets cannot originate inside
   protected spans; accepted repair patches cannot overlap protected
   spans; every protected original substring remains byte/code-point
   identical after the complete repair path; prose immediately
   adjacent to protected content remains repairable; zero eligible
   prose yields zero linguistic-review work) verified on the hidden
   documents through the ACTUAL pipeline entry points (frozen 007-m
   linguistic pipeline invoked only as the frozen downstream consumer;
   no linguistic tuning, no new linguistic model calls beyond what the
   frozen e2e harness performs deterministically - if the harness's
   e2e step would require fresh model calls, use the committed
   deterministic stub boundary established in 008-c and say so);
   commit ONE data-free JSON
   research/prose-boundary/results/hidden-acceptance/
   e2e-invariants-hidden.json (per-invariant PASS/FAIL + counts).
5. Final objective-008 scientific conclusion (requirement 5): apply
   the predeclared decision states to the committed evidence:
   PASS = no material safety violation, exact source integrity,
   acceptable prose coverage, bounded/deterministic malformed-input
   behaviour; CONDITIONAL = only a bounded, clearly understood defect
   remains (name it, bound it, and name the smallest corrective 008-g
   scope); FAIL = machine-significant material is exposed materially
   enough that the parser-first design cannot be trusted (name the
   failing layer; if genuinely parser-level, record the predefined
   markdown-rs challenger trigger - NOT executed in this round). The
   conclusion is written additively into RESEARCH-STATE.md as the
   objective-008 final section and into the report. The conclusion is
   structural-safety evidence ONLY: it establishes no linguistic
   quality, no experimental-MVP acceptance, no release/deployment
   authority; objective 009 (fresh human-labelled linguistic
   confirmation of the complete frozen pipeline) remains the separate
   reserved next objective and its future sample must not contain any
   case generated or inspected for objective 008.
6. Ledger and state bookkeeping (requirement 6): (a) one 008-f registry
   entry (kind evaluation, status per the conclusion) with the full
   data-free metrics summary, the seal identities before/after, the
   adjudication statistics, the e2e invariant results, and
   pending_evidence naming objective 009 (fresh human-labelled
   target-distribution linguistic confirmation - NOT this corpus);
   (b) machine block: registry_entries 29 -> 30, oap_reports_reviewed
   48 -> 49, every identity field otherwise unchanged (including
   quarantined_007n and quarantined_008d), plus the objective-008
   conclusion note; (c) STATUS.md objective-008 paragraph: the final
   conclusion sentence(s) (structural safety verdict + the explicit
   no-linguistic-acceptance + no-merge wording); scoped
   GENERATED-FILES.json pin updated in the same commit (scoped
   inventory/drift green); (d) CSV rebuild (--check).
7. Report-only final commit (requirement 7): sole parent = literal
   implementation head; sole changed path =
   oap/reports/008-f-blind-hidden-acceptance.md; the pre-push report
   validity gate of 008-e applies verbatim (meaningful() scan of every
   checks[] command string; verify_report --id 008-f = verified;
   check_transcript --revision HEAD --expected-id 008-f = pass;
   outputs recorded verbatim).
8. Final-head CI (requirement 8): all four required checks green at
   the final head, with the established bounded verbatim rerun
   observation for the zero-failure TIMEOUT class (up to 3 additional
   observations at the identical final head; a green identical-commit
   rerun is the current check state, disclosed verbatim); predeclared
   BLOCKED stop after the budget; any genuine test failure is a
   STOP/BLOCKED with the failing test named.

## Non-goals

- NO change to any frozen 008-c implementation surface, config,
  corpus, manifest, test file, workflow, or gate (the evaluation runs
  the frozen implementation as-is; any defect is recorded and
  dispositioned, never fixed in-round).
- NO new corpus generation, NO new components, NO new mashups, NO new
  naturalistic responses; the generation program is closed (414
  disclosed attempts).
- NO hidden-set content in any committed artifact, log, or report
  (counts/categories/hashes only); NO private paths.
- NO linguistic-method change of any kind; the frozen 007-m system is
  untouched; the e2e harness uses the 008-c deterministic boundary.
- NO new scientific or generation model calls; the ONLY model use is
  the authorized naturalistic adjudication (recorded and counted).
- NO objective-009 data of any kind; 009 remains reserved and its
  future sample excludes every objective-008 generated or inspected
  case.
- NO merge, NO auto-merge, NO force-push, NO rewrite of any immutable
  order or report; merge disposition is strategy's separate act after
  independent review of this round's final head.
- NO release, deployment, or milestone claim of any kind.
- No CRITICAL append or mitigation update.

## Files and boundaries

Inspect (read-only): all frozen 008-c surfaces; the committed hidden
manifest and annotation guide; the private root 008c-hidden/ (through
the evaluation harness and the seal re-hash only - no ad-hoc content
reads into the model context); the 008-d/008-e orders and reports
(immutable references); private workorders/008-e-final-head-review-
20260918.md (review basis); workorders/consumed.json and FIFO state.

Modify (only these): oap/orders/008-f-blind-hidden-acceptance.md (new,
activation commit); oap/active (-> 008-f);
research/prose-boundary/tools/hidden_acceptance.py (NEW,
deterministic evaluation harness, evaluation-only);
research/prose-boundary/tools/naturalistic_adjudication.py (NEW,
adjudication driver, evaluation-only);
research/prose-boundary/results/hidden-acceptance/mashup-metrics.json
(new, data-free);
research/prose-boundary/results/hidden-acceptance/
e2e-invariants-hidden.json (new, data-free);
research/prose-boundary/corpus/manifests/hidden-annotations-
manifest.json (new, content-free);
research/registry/experiments.json (append exactly one 008-f entry);
research/RESEARCH-STATE.md (machine block 29->30 and 48->49 plus the
objective-008 final conclusion section); STATUS.md (the objective-008
conclusion sentence(s)); oap/GENERATED-FILES.json (the STATUS.md pin
only); research/tables/experiment-summary.csv (rebuild output);
oap/reports/008-f-blind-hidden-acceptance.md (new, report-only
commit).

## Requirements

1. Pre-evaluation integrity gates per scope item 1 (frozen-surface
   byte-identity vs 4b6a90a; hidden seal 4091/4091; remote and
   protocol reconciliation); any failure STOP/BLOCKED.
2. Hidden mashup evaluation per scope item 2 (full item-15 metrics
   set; deterministic harness; one committed data-free aggregate
   JSON; bounded re-run stability check recorded).
3. Naturalistic adjudication per scope item 3 (frozen guide;
   implementation-free; AMBIGUOUS explicit; independent-review
   reconciliation; private-root label files; new content-free
   committed manifest; statistics and model-use count in the report).
4. End-to-end invariants 1-7 on the hidden set per scope item 4
   (actual pipeline entry points; frozen deterministic boundary; one
   committed data-free JSON).
5. Final objective-008 conclusion per scope item 5 (predeclared states
   only; structural-safety-only classification; objective 009
   reservation stated).
6. Bookkeeping per scope item 6 (registry 29->30; machine block 48->49
   with conclusion note; STATUS.md; scoped pin; CSV --check).
7. Report-only commit per scope item 7 with the 008-e pre-push
   validity gate verbatim (no REPORT_CHECK recurrence); the report
   contains every mandatory field and the data-free evidence record,
   with publication_verified false before its own push (P-SELF-03).
8. Final-head CI per scope item 8 (four green or predeclared BLOCKED;
   bounded verbatim rerun policy; genuine test failure = STOP).
9. Send exact response OK after remote verification and stop. PR #9
   remains open and unmerged.

## Acceptance criteria

1. All three data-free evidence artifacts committed (mashup metrics,
   e2e invariants, annotations manifest) with the seal identities
   before/after in the report (hidden manifest sha256 unchanged; 4091/
   4091 both times).
2. The objective-008 conclusion is exactly one of PASS / CONDITIONAL /
   FAIL, justified field-by-field against the committed evidence, with
   the explicit structural-safety-only classification and the
   objective-009 reservation.
3. Registry 30 entries; machine block 30/49 consistent at the final
   head; the 007-o consistency test green at the final head (designed
   single-assertion red at the implementation head).
4. Report-only commit invariants verified remotely; verify_report
   remote=verified; check_transcript green at the final head.
5. Four required checks green at the final head (or predeclared
   BLOCKED with verbatim observations); zero frozen-surface changes
   (git diff vs 4b6a90a over the frozen list empty at the final head).
6. Zero hidden content in committed artifacts (whitespace/privacy scan
   of the diff before push, recorded); zero linguistic changes; zero
   objective-009 touches; no merge.

## Verification

- Focused: the 007-o consistency test (designed red/green states), the
  seal re-hash (pre and post evaluation), the harness determinism
  re-run stability check, the e2e invariant re-derivation on a bounded
  hidden sample, verify_report and check_transcript (pre-push and
  remote), report history, scoped inventory/drift, rebuild_tables
  --check.
- Broader: full research suite with the pinned helper (the hidden-
  acceptance results are data-free aggregates - the suite does not
  read the private root), the OAP suite, one full local Application-
  baseline driver run (per-stage durations, local evidence only), the
  full CI battery at the implementation head and the final head.

## Local setup and constraints

- CPU-only plus the pinned Rust helper and frozen uv environment; the
  private research-runtime root for the hidden set (harness access
  only); no GPU, no new dependencies, no service touches.
- The naturalistic adjudication contexts are authorized evaluation-
  process model use: fixed guide identity, recorded model identity and
  call count, no access to implementation source or development
  labels, no access to objective-009 material.
- Privacy: no private text/paths in committed artifacts or logs.

## Documentation

- RESEARCH-STATE.md: the objective-008 final conclusion section
  (additive; the 008-c/008-d/008-e records untouched) plus the machine
  block update.
- STATUS.md: the objective-008 conclusion sentence(s) (structural
  safety verdict; no linguistic acceptance; no merge).
- The 008-f OAP report: the complete data-free evidence record.
- No rewrite of any historical report or of EXPERIMENT-HISTORY.md.

## Git and report publication

- Activation commit (order + oap/active) per the round mechanics;
  bookkeeping/evaluation commit(s) per scope items 2-6 (the
  evaluation artifacts may be one commit); report-only commit per
  scope item 7 (sole parent = literal implementation head).
- Push semantics per the OAP communication profile; no force-push; no
  push after the report-only commit (C-REPORT-01); exact response OK
  after remote verification.

## Decision classification

D0. Bounded evaluation and bookkeeping inside the risk budget,
executing the 008-c-frozen design and the 2026-09-17 human decision.
No D1 created; no CRIT admission (five conditions fail at condition 1).

## Deferred human adjudication

- Decision: NONE
