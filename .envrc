# Optional direnv hook: activates the machine-local development environment.
# Without direnv, run: source scripts/project_env.sh   (once per shell).
# direnv runs this file with PWD set to this file's directory.
. "$PWD/scripts/project_env.sh"
