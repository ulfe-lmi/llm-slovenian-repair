{
 "schema": "008c-structural-policy/2",
 "order": "008-c",
 "frozen_note": "Frozen by strategy (D0) before any 008-c implementation code depends on this file (order requirement 2). Corrects 008-a finding B: the D0 autolink-destination rule is codified in literal rule text here; experiment-008a.json remains byte-identical and is never edited.",
 "profile": {
  "id": "P2",
  "options": [
   "ENABLE_TABLES",
   "ENABLE_STRIKETHROUGH",
   "ENABLE_TASKLISTS",
   "ENABLE_MATH",
   "ENABLE_YAML_STYLE_METADATA_BLOCKS"
  ],
  "note": "Identical to the 008-a P2 profile (frozen structural policy)."
 },
 "candidate_rule": {
  "statement": "A Text event is candidate prose iff its open-tag ancestor set intersects PROSE_CONTAINERS and does not intersect NON_PROSE_CONTAINERS, with the two 008-a implementation rules carried unchanged into the runtime: D1 token-family matching and the D0 autolink-destination rule.",
  "PROSE_CONTAINERS": [
   "S.Para",
   "S.Head",
   "S.Item",
   "S.Quote",
   "S.TableCell",
   "S.Link",
   "S.Emph",
   "S.Strong"
  ],
  "NON_PROSE_CONTAINERS": [
   "S.CodeBlock:Fenced",
   "S.CodeBlock:Indented",
   "S.HtmlBlock",
   "S.Image",
   "S.Meta"
  ],
  "d1_token_family_matching": "A family entry matches its own token or any level-suffixed token of the form '<entry>:<suffix>' (e.g. S.Head matches S.Head:1..S.Head:6; S.List:ol:2 matches no entry). Carried unchanged from the 008-a evaluator.",
  "d0_autolink_destination": "The only S.Link whose original source slice starts with byte '<' is a CommonMark autolink; its inner Text leaf is the link destination and is PROTECTED. Deterministic source-byte test, no content parsing. Codifies the 008-a documented D0 evaluator rule (finding B resolution)."
 },
 "policy_decisions": [
  {
   "id": "PD-1",
   "decision": "Strikethrough/superscript/subscript content REMAINS EXPOSED.",
   "rationale": "Open 008-a policy item resolved: these wrap prose, not machine content; the mechanism (ancestors via the enclosing prose container) already exposes them and that behaviour is now the intended policy."
  },
  {
   "id": "PD-2",
   "decision": "Images (including alt text) REMAIN PROTECTED.",
   "rationale": "Open 008-a policy item resolved: conservative default; revisit only with future evidence."
  }
 ],
 "residual_layer": {
  "scope": "A narrow content-level recognizer operating ONLY on parser-approved candidate-prose spans, in code-point coordinates. It must NOT reparse Markdown structure, code, parser-recognized math, HTML, or metadata, and must not contain Markdown-delimiter regexes (it is not a second Markdown parser).",
  "classes": [
   {
    "id": "tex-paren",
    "description": "\\( ... \\) inline TeX: pair without newline, nonempty, span <= 200 code points; unpaired '\\'(' with math-like remainder to end of span (<= 200 cp) is also protected."
   },
   {
    "id": "tex-bracket",
    "description": "\\[ ... \\] display TeX: pair, span <= 500 code points (may span lines); unpaired '\\'[' with math-like remainder to end of span (<= 500 cp) is also protected."
   },
   {
    "id": "tex-env",
    "description": "Selected TeX environments: begin/end pairs for equation, equation*, align, align*, aligned, gather, gather*, gathered, eqnarray, matrix, pmatrix, bmatrix, vmatrix, cases, split; span <= 1000 code points; unbalanced begin (no matching end) protects begin-to-end-of-span for the same bound."
   },
   {
    "id": "bare-json",
    "description": "Bare JSON: maximal run starting at '{' or '[' that is JSON-like (balanced or truncated), no Markdown structure, <= 2000 code points; balanced pairs protected, truncated (unbalanced) protects to end of the containing prose span line."
   },
   {
    "id": "yaml-toml-config",
    "description": "YAML/TOML/INI/config-like text: runs of >= 2 consecutive 'key: value' / 'key = value' style lines, or a '[section]' header followed by such lines; <= 2000 code points."
   },
   {
    "id": "shell",
    "description": "Shell/terminal commands and flags: a line starting with a shell prompt ('$ ', '# ', '> ') or a bounded command vocabulary (sudo python python3 uv git curl npm npx pip pipx pytest ruff mypy cargo make gcc g++ cc rustc ls cat grep awk sed echo export cd mv cp rm mkdir touch tar zip) followed by arguments; command-line flag tokens (--word, -single-letter) and their attached values; whole affected line(s) protected."
   },
   {
    "id": "path",
    "description": "POSIX paths (rooted '/' with >= 1 segment, or relative a/b with >= 2 path segments and a dotless final segment) and Windows paths (drive ':' + backslash/slash, or UNC \\\\host\\share)."
   },
   {
    "id": "env-var",
    "description": "Environment variables: '${NAME}' or '$NAME' where NAME is [A-Za-z_][A-Za-z0-9_]* and (NAME contains '_' or NAME is all-uppercase with >= 2 chars)."
   },
   {
    "id": "identifier",
    "description": "Identifiers: underscored identifiers or identifiers containing a digit, [A-Za-z_][A-Za-z0-9_]* (carried semantics of the 008-a legacy _IDENTIFIER rule), plus ALL_CAPS identifiers of >= 2 letters with no digit-only runs (carried semantics of _UPPER_IDENTIFIER)."
   },
   {
    "id": "number-unit",
    "description": "Machine-significant numbers with short units: signed decimal numbers (., or , decimals) with an optional immediately-adjacent short unit (1-8 letters or %); carried semantics of the 008-a legacy _NUMBER rule."
   },
   {
    "id": "url",
    "description": "Bare URLs missed by the parser dialect: http:// or https:// (case-insensitive) followed by a non-space, non-'<' run; carried semantics of the 008-a legacy _URL rule."
   }
  ],
  "excluded_from_residual": "Markdown delimiters (backticks, brackets/parens as link syntax, #, *, -, ~, |), code, parser-recognized math, HTML, metadata, and list/heading markers are NOT residual classes; exposure of bare delimiters is neutral by policy (see malformed class map)."
 },
 "coordinate_contract": {
  "unit": "code-point coordinates into the unmodified original for all public intervals; byte coordinates only at the parser protocol boundary",
  "mapping": "cp = len(bytes[:s].decode('utf-8')); byte = len(text[:cp].encode('utf-8'))",
  "invariants": [
   "0 <= s <= e for every interval; intervals lie in [0, len(text)]",
   "every parser byte range lies on UTF-8 code-point boundaries (no bisection)",
   "original_bytes[start_byte:end_byte) is the exact intended source slice",
   "byte<->code-point mapping is deterministic and exactly reversible"
  ],
  "fail_closed": "Any bounds or UTF-8 boundary failure is rejected fail-closed (legacy fallback), never repaired by guessing."
 },
 "fail_closed_fallback": {
  "triggers": [
   "helper unavailable or not a regular file",
   "helper identity mismatch (sha256 != pinned)",
   "helper non-zero exit",
   "helper timeout",
   "helper stdout not valid UTF-8 JSONL",
   "missing or malformed event lines",
   "missing eof marker (truncated protocol)",
   "protocol event out of order or unbalanced",
   "any coordinate-contract violation"
  ],
  "behaviour": "legacy full-regex protection (the 008-a protected.py rule set, code-point coordinates) with the fallback reason recorded in the result; never fail open (an unprotected result is never returned)",
  "legacy_rules": "fenced-code, inline-code, markdown-link, url, path, relative-path, shell, number, identifier, upper-identifier, structured, tool-argument"
 },
 "determinism": "Identical input bytes -> identical protected-interval bytes (helper is deterministic for pinned pulldown-cmark 0.13.4; residual recognizers are pure functions of the candidate-prose text).",
 "helper": {
  "binary": "prose-boundary-meas",
  "invocation": "<helper> P2 < document",
  "protocol": "one JSON line per event {\"i\":<idx>,\"k\":\"<kind token>\",\"s\":<start_byte>,\"e\":<end_byte>} followed by {\"eof\":true}",
  "exit_codes": {
   "0": "ok",
   "3": "stdin not valid UTF-8",
   "5": "usage or write error"
  },
  "pinned_identity": {
   "crate": "pulldown-cmark =0.13.4",
   "registry_checksum_sha256": "e9f068eba8e7071c5f9511831b44f32c740d5adf574e990f946ddb53db2f314e"
  }
 },
 "malformed_class_map": {
  "note": "Frozen per-malformed-class expected outcome for the dev-corpus safety metric. 'protected' = the byte must be covered by the final protected set (structural parser interval or residual recognizer); 'policy-exposed' = policy-decided exposure, recorded but not a safety violation (008-a GO residual bound: delimiter-only residue is a bounded second-stage abstention, not a failure).",
  "classes": {
   "unclosed-fence": {
    "expected": "protected",
    "mechanism": "structural: pulldown-cmark extends the fenced code block to end of input."
   },
   "unmatched-backtick": {
    "expected": "policy-exposed",
    "mechanism": "a single backtick is a literal character (not a delimiter pair); backtick delimiters are outside residual authority (no Markdown-delimiter regexes). Machine sub-tokens inside (urls/paths/identifiers/numbers) are still protected by their own residual classes."
   },
   "unmatched-dollar": {
    "expected": "policy-exposed",
    "mechanism": "a lone '$' is ambiguous between currency and math; the parser does not pair it and the residual layer abstains (currency dollars must stay exposed as prose)."
   },
   "incomplete-display-dollar": {
    "expected": "policy-exposed",
    "mechanism": "an unbalanced '$$' is not display math to the parser; TeX-like content between markers is not a declared residual class (only \\( \\[ environments are); currency interpretation remains the conservative abstention."
   },
   "incomplete-paren-bracket": {
    "expected": "protected",
    "mechanism": "residual tex-paren / tex-bracket classes cover unpaired '\\(' and '\\[' with math-like remainder."
   },
   "unclosed-tex-env": {
    "expected": "protected",
    "mechanism": "residual tex-env class protects an unbalanced begin to end of span."
   },
   "broken-link": {
    "expected": "protected",
    "mechanism": "the URL sub-token is protected by the residual url class; the bracket/paren delimiter residue is neutral (policy-exposed delimiter, machine content protected)."
   },
   "partial-html": {
    "expected": "protected",
    "mechanism": "structural: HTML tags are Html/InlineHtml events, never Text, so tag bytes are never candidate prose."
   },
   "truncated-json": {
    "expected": "protected",
    "mechanism": "residual bare-json class protects the truncated JSON run."
   },
   "malformed-nesting": {
    "expected": "policy-exposed",
    "mechanism": "unbalanced Markdown emphasis delimiters are literal text/delimiters; prose inside remains prose; no residual class owns Markdown delimiters."
   }
  }
 },
 "metrics": {
  "safety": {
   "protected_bytes_exposed": "bytes of corpus regions labelled expected='protected' that are NOT covered by the final protected set; target 0",
   "protected_regions_overlapping_prose": "protected regions with any overlap into candidate prose beyond the residual layer; target 0 (residual overlap is by design and not counted)",
   "machine_significant_exposure_rate": "exposed bytes over structurally-known machine regions (expected='protected'); target 0"
  },
  "coverage": {
   "expected_prose_bytes_exposed": "exposed bytes over regions labelled expected='prose'; target 100% of construction-labeled prose",
   "expected_prose_regions_completely_available": "fraction of expected-prose regions with zero protected bytes",
   "unnecessary_suppression_by_container": "actual prose bytes suppressed per container type, reported (no arbitrary threshold)"
  },
  "coordinates": {
   "utf8_boundary_violations": "parser ranges bisecting code points; target 0",
   "byte_cp_conversion_mismatches": "mapping round-trip failures; target 0",
   "source_slice_mismatches": "byte slices not equal to intended source bytes; target 0"
  }
 },
 "rule_classification": {
  "note": "Every 008-a protected.py rule classified 4-way (1 = Markdown syntax replaced by the parser; 2 = semantic second-stage survivor; 3 = redundant/obsolete; 4 = purpose unclear). No rule silently deleted; survivors keep unchanged semantics and run only on candidate-prose spans in code-point coordinates.",
  "rules": [
   {
    "rule": "_FENCE (fenced-code)",
    "regex": "(?s)(?:^|\\n)(```+|~~~+)[^\\n]*\\n.*?\\n\\1(?=\\s|$)",
    "disposition": "1",
    "parser_replacement": "S.CodeBlock:Fenced / S.CodeBlock:Indented intervals"
   },
   {
    "rule": "_INLINE (inline-code)",
    "regex": "(?<!`)`[^`\\n]+`(?!`)",
    "disposition": "1",
    "parser_replacement": "Code event intervals"
   },
   {
    "rule": "_LINK (markdown-link)",
    "regex": "!?(?:\\[[^\\]\\n]*\\]\\([^)\\n]*\\)|<https?://[^>]+>)",
    "disposition": "1",
    "parser_replacement": "S.Link/S.Image structure (destinations protected by construction; autolink destinations via the D0 rule); link labels remain exposed prose"
   },
   {
    "rule": "_URL (url)",
    "regex": "https?://[^\\s<>]+",
    "disposition": "2",
    "residual_class": "url"
   },
   {
    "rule": "_PATH (path)",
    "regex": "(?<!\\w)(?:\\.?/|~/|[A-Za-z]:[\\\\/])[^\\s`<>]+",
    "disposition": "2",
    "residual_class": "path"
   },
   {
    "rule": "_RELATIVE_PATH (relative-path)",
    "regex": "(?<!\\w)[A-Za-z0-9_.-]+(?:/[A-Za-z0-9_.-]+)+(?!\\w)",
    "disposition": "2",
    "residual_class": "path"
   },
   {
    "rule": "_SHELL (shell)",
    "regex": "(?m)^\\s*(?:[$#>]\\s+|(?:sudo\\s+)?(?:python|python3|uv|git|curl|npm|pip|pytest|ruff|mypy)\\s+)\\S[^\\n]*$",
    "disposition": "2",
    "residual_class": "shell"
   },
   {
    "rule": "_NUMBER (number)",
    "regex": "(?<!\\w)[+-]?(?:\\d+(?:[.,]\\d+)?)(?:\\s?(?:%|[A-Za-z]{1,8}))?(?!\\w)",
    "disposition": "2",
    "residual_class": "number-unit"
   },
   {
    "rule": "_IDENTIFIER (identifier)",
    "regex": "(?<!\\w)(?=[A-Za-z_]*\\d|[A-Za-z_]*_[A-Za-z_])[A-Za-z_][A-Za-z0-9_]*(?!\\w)",
    "disposition": "2",
    "residual_class": "identifier"
   },
   {
    "rule": "_UPPER_IDENTIFIER (upper-identifier)",
    "regex": "(?<!\\w)[A-Z][A-Z0-9_]{1,}(?!\\w)",
    "disposition": "2",
    "residual_class": "identifier"
   },
   {
    "rule": "_JSON_XML (structured)",
    "regex": "(?:\\{[^\\n{}]{0,2000}\\}|\\[[^\\n\\[\\]]{0,2000}\\]|</?[A-Za-z][^>]*>)",
    "disposition": "2",
    "residual_class": "bare-json"
   },
   {
    "rule": "tool-argument",
    "regex": "n/a (API parameter)",
    "disposition": "2",
    "residual_class": "tool-argument (API level; applies to any source substring named by the caller, independent of parsing)"
   }
  ],
  "counts": {
   "1": 3,
   "2": 9,
   "3": 0,
   "4": 0
  }
 }
}
