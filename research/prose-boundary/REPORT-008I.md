# 008-i research report — opening-tag composition extension, xml-fragment class-boundary decision (D1-5), display-dollar adjacency guard, T6 link-destination admission refinement, and final objective-008 blind acceptance on a new sealed v4 hidden set

Order 008-i (corrective round 3 of objective 008, executing the 008-h named
smallest corrective suffix). Research-only record; no production readiness,
no release, no deployment, and no linguistic-quality claim. The final
objective-008 verdict recorded here is STRUCTURAL-SAFETY EVIDENCE ONLY.
Objective 009 remains reserved and untouched; its future sample must not
contain any case generated or inspected for objective 008 (every v1, v2, v3,
and v4 component, mashup, naturalistic case, dev document, dev batch, and
dev-edge document). Every claim below cites a committed record (path +
SHA-256 where material). No raw private LLM text, no private-root content,
and no private paths appear in this subtree; the v4 hidden set, the spent
v1/v2/v3 sets, and all labels are referenced by committed content-free
manifest hashes only.

**Outcome: PASS** (predeclared decision state; structural-safety evidence only). The 008-h named corrective suffix is executed to closure - R1 extended to all HTML block-start lines, the D1-5 xml-fragment class-boundary decision (PRIMARY bounded trigger refinement, committed with its evidence), the display-dollar adjacency guard, and the T6 clean-URL admission refinement (structural policy v5) - and the FINAL objective-008 verdict on the NEW sealed v4 hidden set (2,000 documents, 373 components, 76 families; seal 4,076/4,076 verified data-free before and after evaluation; the strategy pre-evaluation receipt gates the evaluation) is PASS: all eight predeclared fields pass (construction-labeled protected bytes exposed 0 of 266,807; coordinates 0/0/0/0; malformed 0 violations with recomputation delta 0; e2e invariants 1-7 on 2,000/2,000 replays; determinism 50-case identical; dev regression PASS, +0.0 pp vs the 008-h baseline within the 1.0 pp bound; 15/15 naturalistic ranges resolved DOCUMENTED-DIALECT-LIMITATION, 0 UNRESOLVED; 87/96 authorized model attempts, zero linguistic-pipeline calls, zero objective-009 touches), with the 62 B oracle-refined policy-coverage census (one documented pre-existing class - yaml-hash-comment-heading - across 8 documents, sealed and evaluated as-is under the predeclared rule) disclosed and non-gating. On the predeclared PASS branch the objective-008 protection layer is declared FROZEN at the 008-i census (four-file census in experiment-008i.json; policy of record v5). D1-1, D1-3, D1-4, and D1-5 - each predeclared to clear only on a final objective-008 PASS on the v4 set - are cleared by that verdict. No merge, release, or deployment claim; PR #9 stays open and unmerged; objective 009 remains reserved.

## Committed records cited in this report

| Record | SHA-256 |
| --- | --- |
| oap/orders/008-i-corrective-scope-and-v4-final-blind-acceptance.md (order) | 8e1e5a5baf993353a85e63e2717e18ebc17639d544797d26a2e019c1b84d2150 |
| research/prose-boundary/config/structural-policy-v3.json (frozen; byte-identical in-tree) | 7ff6646e3c11d1241cb1c85bd96f08e3f0db08e73d4eb4a4e97fac0c17e1828f |
| research/prose-boundary/config/structural-policy-v4.json (in-tree, byte-identical to the 008-h head) | f564d9f87ef01a893d6cd6bb36f6cf99a0d353a4dde4e4c4c024942eb969e76f |
| research/prose-boundary/config/structural-policy-v5.json (NEW policy of record: guards (a)-(c), D1-5 outcome, T6 observation, frozen protection identity) | 915f70d34ecc69ab6fea0b273f24ca20f251b6e181bd5e9281c5c012de2fe542 |
| research/prose-boundary/config/experiment-008i.json (round identity; four-file frozen protection census; D1-5 decision record) | 24e25edafbbf0b1d67dc405440ea78c0b76a9011c00d5e53c82fe3c00fef6685 |
| research/curated/prose_boundary.py (frozen 008-i implementation; single change vs 008-h = the D1-5 trigger condition) | c57e2901e52af962546ee83651850e22c91655a8cc822f2c43b1284da16b8c50 |
| research/curated/protected.py (byte-identical to the 008-g/008-h identity) | 27f22eaee129190b880851b958a8aa5a38d357cb4e711d70e762b2353d32e78f |
| research/prose-boundary/tools/prose_boundary_builder.py (released 008-i builder: R1 extended to all HTML block-start lines, display-dollar adjacency guard, T6 clean-URL admission, guard/provenance recording) | ccbec1c9f8c4636d125f86aac231c9bb016d917fa4b7ddd5854055d9158fa187 |
| research/tests/test_008i_composition_and_class_boundary.py (new focused file; 38/38 green at the implementation head) | a85608f33ede6f4bf8d72ea7d73a6b07a60f7e104130ade59ad039430905e8fd |
| research/prose-boundary/corpus/defect-dev-008i/ (dev dialect-edge batch: machine-known labels for the four fixed/refined shapes; content-free manifest) | committed in the first implementation commit; census additive in corpus-census.json |
| research/prose-boundary/corpus/manifests/corpus-census.json (additive defect-dev-008i section) | 5b6fc6e5c83852db5b70e7ec1a815010ec4c519d0b9b55e60dda6925746586fe |
| research/prose-boundary/results/hidden-acceptance-v4/dev-regression-008i.json (committed BEFORE any v4 generation; runs A+B) | f7959a7fda2adc33b8f95887dcfe45325ddeccd0623caf2c17c8e3bcf9756d24 |
| research/prose-boundary/results/hidden-acceptance-v4/shape-analysis-008i.json (data-restricted shape analysis, requirement 2) | ff26d29da724a9e58a5c9740b2ec2075ecc499f4126152d1042db4f7a27f2be3 |
| research/prose-boundary/config/generation/generation-identity-008i.json (v4 generation identity incl. seeds, admission rules, budget, recovery, pre-seal diagnostic, interrupted-stream disclosure) | 84b55be651d46ace5287600a9e1fb21c73c9c74ea99f499dd810885db592e5f5 |
| research/prose-boundary/config/generation/prompt-families.json (frozen prompt library, verbatim) | 904dd36b9891c567f250bf8b42b374d0b6557aa79ca3fcaa3c8824682fcc8fef |
| research/prose-boundary/tools/generation_driver.py (frozen 008-c driver, verbatim) | 7050b2254ca14ffbfbe70ca06bd0674724a867debea124966cd56e834ed9cd45 |
| research/prose-boundary/corpus/manifests/hidden-manifest-v4.json (content-free v4 manifest; 76 component + 2,000 document + 2,000 label files) | a702babc239d24251a62f35c1e51f389843c7e7eea8547d64bb325cf16174696 |
| research/prose-boundary/corpus/manifests/seal-verification-v4.json (4,076/4,076 verified; out-of-manifest = generation receipts only) | 0c6fa26ed70a3e8642ce76d0a139e77336fc6c4ba0b10a200559f6eeec909dc0 |
| research/prose-boundary/tools/hidden_acceptance_v4.py (new thin wrapper; frozen 008-f harness imported unmodified) | 3f87c18d071174a252d41a7f4383c8929e51fad56aa836ce0240e2f3b577caa3 |
| research/prose-boundary/tools/naturalistic_consistency_v4.py (new deterministic driver; carried 15-range resolution map) | 37e10dfd5d601254b8df44cb3d229ffd9488fb1053514728780686a95041f46a |
| research/prose-boundary/results/hidden-acceptance-v4/mashup-metrics-v4.json (v4 acceptance evidence; label-source split) | 5be89d8291dd3d15c9e3379a059f1b15305acea29fd0d99762af04ad45d2ad65 |
| research/prose-boundary/results/hidden-acceptance-v4/e2e-invariants-hidden-v4.json (e2e invariants 1-7) | 8a16f362cca68821bd9f9d9dc04f14c13cef2353d2b529bb801a837915212a3e |
| research/prose-boundary/results/hidden-acceptance-v4/naturalistic-consistency-v4.json (70-case re-run + per-range resolution) | 6f4df79b01e69f41e6d23aabff8f2ee4649351dabfc509e3d2a019b087b6d50a |
| research/prose-boundary/tools/hidden_acceptance.py (frozen 008-f harness; imported unmodified) | 22ae618bd1be66bfd93c79a10e97066eef5dcd05213f851e7b19f5a12e8771fd |
| research/prose-boundary/corpus/manifests/hidden-manifest.json (v1, single-use spent) | 8233bdcf683df8a97d62a375a6cf60d93b64aa6b7ecf22489ce9986333436a18 |
| research/prose-boundary/corpus/manifests/hidden-manifest-v2.json (v2, single-use spent) | cf37654a45b10c024525fc334f368d4b20956619e2568542544c855d93a85ec1 |
| research/prose-boundary/corpus/manifests/hidden-manifest-v3.json (v3, single-use spent) | ee503bd7c09f26a1b9c171319cc82f8ec692918d0d7d18f11d528a6f18c54f36 |
| Pinned helper prose-boundary-meas (008-a recorded identity) | 5df9a11d56b5bdec90c3ca09298802723975306f77e43e74ad1a72eb427c44bf |
| Strategy pre-work all-seals re-verification (supervision tree; receipt strat-verify-tmp/v3seal-result-pre-20260921-083701.json at the 008-h final head; not committed) | 45a6086ec9f0758a9cb27a4d4967b36b91cd32073bcb64b4ff644209fdcf49f1 (10,469 B) |
| Strategy v4 seal pre-verification receipt (supervision tree, strat-verify-tmp v4seal result; not committed) | 0fba8fcf61924f94adcfb7388bb6b369f2d61b18594f4cfc32129a69b87dbce0 (v4seal-result-pre-20260923-011639.json, 3,620 B) |
| Strategy v4/v3/v2/v1 post-evaluation all-seals receipt (supervision tree; not committed) | 833e60b85d0e8e1863f8eb2c93ad76a93a2eeb15d8e51cfe79b7987fbc1fdf8d (v4seal-result-post-20260923-013553.json, 14,022 B) |

## 1. Pre-work integrity gates (requirement 1) — PASSED

- Remote and protocol reconciliation (at round start, re-observed in this
  session): PR #9 open/unmerged, head 954d758b0d6308ce1811bccb25ab7d347ad99b7c
  (the 008-h final head); main = 7d2cc9ee37c1fc8c2a3eba238f89bc9bb242f9d9
  (OAP_ACCEPTED_REF) unchanged; oap/active = exactly 008-i plus one LF
  (6 bytes) at the activation commit bd8d60e5a24c32f7f3a6257254bf611e1ff5ebc6;
  workorders/consumed.json = 008-i / consumed / recovery:false / pr 9
  (the wrapper consumed control once before model launch).
- Frozen-surface byte-identity: the in-scope diff of every commit of this
  round is limited to the released-from-freeze list; every preserve-list
  surface (the frozen 007-m linguistic surfaces, every 008-a through 008-h
  round artifact, all workflows except the single line-35 change pinned in
  the order, the 008-d quarantine record, the committed corpus, every test
  file, scripts/verify_development_baseline.py at sha256
  3604eb6dd92d2fb47f2993bd9e931a11b1701970867367170738ee6e6fe6c818, all
  orders/reports) is byte-identical at the implementation head. The CI-1
  single line at .github/workflows/application-baseline.yml line 35
  (' --command-timeout 600' appended to the driver invocation) is pinned at
  sha256 4490d0fc9e9fcd3eb697b1721b385f086f1a232372593618501dc0ed0918bc4c
  (git blob b2bf32dfb9f25f74761d5a6f1a33c44c48869789, 1,687 B) in the FIRST
  implementation commit (owner Option A1, recorded 2026-09-22 18:24 CEST).
- Strategy in-round seal re-verification of ALL THREE committed seals
  (data-free, before the round starts, at the 008-h final head
  954d758b...): v1 4091/4091, v2 4076/4076, v3 4076/4076, all VERIFIED,
  seal copies byte-identical to the committed manifests, manifest sha256s
  unchanged (8233bdcf.../cf37654a.../ee503bd7...); the re-hash is re-run
  again around the v4 evaluation (section 7; strategy post-evaluation
  all-seals receipt).
- Registry at 32 entries at round start (this round appends exactly one
  008-i entry -> 33, section 10). CRITICAL.md seed-identical
  (a9ea5fa5db2affabf0f85710f41e37e73b108c0236a58b7efb9c17cb36a07e9e), zero
  entries. No mismatch: no BLOCKED state.

## 2. Data-restricted shape analysis (requirement 2)

Committed BEFORE any implementation change (git ordering: the shape
analysis commit b16aa24... precedes the first implementation commit
3279df6...): shape-analysis-008i.json (data-free). It re-derives the 364 B
v3 construction-labeled protected exposure (five documents, three mechanism
shapes) and the bounded pre-existing T6 link-destination observation from
the committed 008-h aggregates (REPORT-008H.md section 6,
mashup-metrics-v3.json, the v3 manifest census) and states the per-shape
fix specification: (i) the 278 B shape = two 139 B documents
(v3h-000713, v3h-001274; code-cpp) - an HTML OPENING-tag line (the last
line of an adjacent structured-html component) opens a CommonMark type-6
HTML block that absorbs the following code fence and its first code lines
to the internal blank line; the released 008-h R1 predicate is
closing-tag-scoped, so the complement (opening-tag) shape is uncovered ->
R1 extension to all HTML block-start lines (scope 3a); (ii) the 60 B shape
= two 30 B documents (v3h-000625, v3h-001241; structured-xml) - a
single-line multi-tag XML line in which one tag carries a dialect-
vocabulary name with a non-ASCII (Slovenian) attribute name the dialect's
ASCII-only inline-HTML rule does not recognise, while the frozen
xml-fragment class abstains (18+10+2 B split per document) -> the D1-5
class-boundary decision (scope 3/4); (iii) the 26 B shape = one 26 B
document (v3h-001307; math-display-dollar) - an adjacent
malformed-incomplete-display-dollar component makes the parser pair its
unterminated opener with the complete component's opening marker (14+12 B)
-> the display-dollar adjacency guard (scope 3b); plus the T6 observation
(whole-region PROTECTED construction label vs a non-clean-URL link
destination; the v3 prescan confirmed it did not materialize in the v3
pool) -> the T6 clean-URL admission refinement (scope 3c).

Record-hygiene re-pin (stop-and-correct, applied in this round before
publication): the committed shape-analysis file initially carried a JSON
key named "reference" (the REPORT-008H.md section-6 citation value) - a
key name the committed publication guard (research/tools/
publication_guard.py) denies in committed JSON. The key was renamed to
"cited_record" (no other byte change) and the dependent record pointers
(the shape_analysis.file_sha256 in experiment-008i.json, the
experiment_config_sha256 embedded in the two v4 evaluation aggregates,
and the cited shas in this report, the registry entry, and
RESEARCH-STATE.md section 21) were re-pinned to the re-pinned bytes.
The four-file frozen protection census is byte-identical before and
after the re-pin, and no measured value in any re-pinned aggregate
changed.

## 3. Policy v5 and bounded implementation (requirements 3-4)

- structural-policy-v5.json (NEW; v3 and v4 stay byte-identical in-tree):
  (a) the R1 extension recorded as the composition contract (fenced or
  indented code components never composed on the line following an HTML
  block-start line - pulldown-cmark 0.13.4 HTML block types 1-7 - with the
  008-h test-pinned corner, a bare single complete opening tag at EOL,
  staying unbarred by the preserved 008-h contract); (b) the display-dollar
  adjacency guard (a malformed-incomplete-display-dollar component never
  composed immediately before a component whose first line opens a
  display-dollar expression; barrier = one NEUTRAL-labeled blank line, the
  predeclared mechanism, not substituted); (c) the T6 clean-URL admission
  (T6 link destinations clean URLs: https?:// with no whitespace or angle
  brackets; builder-asserted; the pre-existing whole-region PROTECTED
  observation documented as a measured observation with the v3 prescan
  cited, not as a class); (d) the D1-5 outcome (PRIMARY branch taken: the
  bounded trigger refinement, with the v3 evidence cited); (e) the frozen
  protection identity = the four-file census in experiment-008i.json.
- Builder (prose_boundary_builder.py, the released 008-i builder):
  - R1 EXTENDED: the composition predicate fires for ALL dialect HTML
    block-start lines (opening-tag lines included), not only closing-tag
    lines; on fire, exactly one blank line (template-owned NEUTRAL
    delimiter) is inserted exactly as the released closing-tag rule does
    and recorded in the document composition provenance
    (r1_blank_line_insertions + r1_contract); the builder asserts the
    invariant fail-closed.
  - DISPLAY-DOLLAR ADJACENCY GUARD: on fire, one NEUTRAL-labeled blank line
    is inserted and recorded (display_dollar_barrier_insertions +
    display_dollar_guard_contract); no new TeX-command residual class (the
    008-g spec stays: paren/bracket/env only).
  - T6 ADMISSION REFINEMENT: builder-asserted at composition (fail-closed
    LookupError on a non-admitted pool); recorded as the
    t6_link_destination_admission provenance field.
  - The no-parser-participation assertion is retained; the label oracle is
    unchanged in meaning; the label_source split (construction vs
    oracle-refined) is recorded per labeled region.
- Predeclared xml-fragment trigger decision D1-5 (requirement 4):
  **PRIMARY (bounded trigger refinement) committed WITH its evidence**
  after the dev regression gate (section 4), recorded in
  experiment-008i.json. The curated delta is exactly ONE trigger condition:
  `_tag_non_ascii_attr_name` (a dialect attribute-scan simulation of the
  pinned pulldown-cmark 0.13.4 `scan_attribute_name` + outer attribute
  loop: first byte ASCII alpha/_/:, continuation alnum/_.: -; `=` with a
  quoted value skipped opaquely; unquoted value by the dialect stop set),
  applied in the xml-fragment class's opening branch (fire when the tag
  name is dialect-vocabulary but the tag fails inline recognition solely
  because of a non-ASCII attribute name; the protected span is the tag
  token only - a matching vocabulary closing tag stays structural) and the
  standalone-closing branch (fire when the closing token carries a
  non-ASCII attribute name; span = the token). The v2-parity abstention is
  preserved (a non-vocabulary tag name with non-ASCII attributes still
  abstains). ASCII attribute behaviour is byte-identical to the 008-h
  implementation (proven by the run A/B dev-regression raw-output
  byte-identity, section 4). The predeclared FALLBACK branch (no curated
  change + documented boundary in policy v5 + e02 re-label) was available
  and NOT triggered: fallback_triggered = false.
- Dev dialect-edge batch (requirement 5): defect-dev-008i/ committed with
  the four fixed/refined shapes under machine-known labels (content-free
  manifest; raw content stays private): (i) an HTML opening-tag line
  followed by a fenced code component (the 278 B family; R1-extended
  NEUTRAL barrier; protected); (ii) a single-line multi-tag XML line with a
  dialect-vocabulary-name tag carrying a non-ASCII attribute name (the 60 B
  family; branch-dependent label: PROTECTED under the committed D1-5
  primary refinement; under the 008-h implementation the measured 21 B
  tag-token exposure is the predeclared gate-A attribution target); (iii) a
  malformed incomplete display dollar with the one-NEUTRAL-blank-line
  barrier before a complete display-dollar component (the 26 B family;
  protected; malformed $$ policy-exposed); (iv) a T6 link with a
  non-clean-URL destination (the disclosed observation; retained as a
  NEUTRAL-split label document - dev data stays frozen, no re-labeling of
  any committed dev document).
- Focused tests: test_008i_composition_and_class_boundary.py - 38/38 green
  at the implementation head (positive: the opening-tag-line + fenced-code
  composition fires R1 with the NEUTRAL barrier and zero exposed protected
  bytes on the shape fixture; the xml-fragment class fires on the measured
  non-ASCII-attribute shape (EXTEND branch); the display-dollar adjacency
  fires the guard with zero exposed TeX-command bytes; the T6 clean-URL
  admission assert passes. Negative: the 008-g/008-h closing-tag R1
  fixtures unchanged; R2/R3 predicates unchanged; the xml-fragment class
  still ABSTAINS on a non-vocabulary tag name with non-ASCII attributes
  (the v2-parity guard preserved); the guard does not fire for a complete
  display-dollar component not adjacent to a malformed opener, nor for a
  malformed opener not followed by a complete component; the 18/18 helper
  unit cases for the dialect attribute-scan simulation; no new
  over-suppression on any committed dev fixture - the corpus-level negative
  proof is the dev regression gate (section 4)).

## 4. Dev regression gate (requirement 6) — PASS, committed before any v4 generation

scripts/verify_development_baseline.py over the 3,000-document dev corpus +
the 008-g dev batch + the 008-h dev-edge batch + the new 008-i dev-edge
batch, committed as dev-regression-008i.json BEFORE any v4 generation
commit (git ordering: the dev-regression commit d40e441... precedes the
v4 generation/identity commit). Two runs (run A = the 008-h implementation
as the attribution baseline; run B = the 008-i final implementation):

- Run A: dev corpus 0/526,315 B; 008-g batch 0/702 B; 008-h batch 0/666 B;
  008-i batch 21/188 B - exactly the predeclared shape (ii) attribution
  (e02: the 29 B dialect-vocabulary tag token with the 8 B interior number
  + identifier class-covered) and nothing else.
- Run B (final): dev corpus 0/526,315 B; 008-g batch 0/702 B; 008-h batch
  0/666 B; 008-i batch 0/188 B - ZERO on all four sets; the dev corpus,
  008-g and 008-h raw outputs byte-identical between runs A and B (the
  delta fires only on the measured shape).
- Prose over-suppression: run B rate 0.01570195190675161 = the 008-h
  baseline; deltas +0.032983 pp vs 008-c, +0.021674 pp vs 008-g, +0.0 pp
  vs 008-h - within the predeclared 1.0 percentage-point bound.
- All dev invariants pass; no stop condition fired (i-iv); the fallback
  branch was NOT triggered. Gate status: PASS. This record is the D1-5
  evidence input (item 4) and the round's regression record.

## 5. The new sealed v4 hidden acceptance set (requirement 7)

The v4 set was generated with a FRESH independent stream (the disclosed
interrupted prior stream: section 10) on the 007-lineage qwen3.8-27b
deployment identity (endpoint/credential out-of-band; no endpoint value or
credential material in any committed artifact), the frozen 008-c driver
(imported unmodified) and the frozen prompt library verbatim, under the
008-i builder with all three guards active. Committed identity:
generation-identity-008i.json (sha256 84b55be651d46ace5287600a9e1fb21c73c9c74ea99f499dd810885db592e5f5,
42,769 B).

- Seeds: master 86c3094973fa343b; v4_mashup 6b390354646a9b25 - a fresh
  008-i seed stream, DIFFERENT from the v3 seed dd1a19a598160b36; the
  per-family seeds are recorded (for the recovered families, the RETAINED
  fragments' recovery seeds, with the initial seeds disclosed in the
  recovery records).
- Batch plan (the same 76 families and density plan as v3): base 4 per
  family; densified 16 each (8+8) for structured-html,
  structured-keyvalue, structured-xml, malformed-partial-html; nominal
  352 component slots.
- Budget: 87 total HTTP attempts (2 preflights - initial + recovery - +
  85 component calls - 80 base batches + 5 bounded K=8 recovery calls - +
  0 retries), within the authorized 96 (order requirement 7; 008-h
  precedent 89).
- Components: 352 initial -> 13 exact dev/v1/v2/v3-pool duplicates across
  five machine families (machine-filenames 2, machine-identifiers 1,
  machine-urls 4, machine-versions-numbers-units 3,
  machine-windows-paths 3) recovered by one bounded K=8 regeneration per
  family with fresh recorded recovery seeds (13 overlap-discarded, 34
  components added: machine-filenames 2->6, machine-identifiers 1->11,
  machine-urls 4->6, machine-versions-numbers-units 3->9,
  machine-windows-paths 3->9) -> 373 final components / 76 families. Zero
  exact component overlap asserted hash-only against the dev pool (641),
  the v1 pool (588), the v2 pool (340), and the v3 pool (345) and
  disclosed (final re-assertion 0/0/0/0 plus 0 intra-pool; strategy
  re-verified the v4 seal data-free, section 7).
- Documents: 2,000 deterministic mashup documents (1,705,961 B total; min
  340 B, max 2,349 B; template census in the identity) + 2,000
  construction labels with the label_source split (sealed census:
  construction-labeled PROTECTED 266,807 B / 2,738 regions;
  oracle-refined PROTECTED 59,085 B / 1,807 regions; POLICY_EXPOSED 862 B
  / 320 regions; ground truth by construction under the builder
  no-parser-participation assertion).
- Admission: R2 excluded 0 components; R3 NOT applied (the D1-5 PRIMARY
  refinement makes it unnecessary; R3 applies only in the not-taken
  FALLBACK branch); T6 clean-URL admission active (182 T6 documents).
- Composition provenance: R1 barrier insertions 4 (4 documents - the
  extended predicate fired on the composed pool); display-dollar barrier
  insertions 0 (the guarded adjacency never composed in the pool);
  fallback documents 0.
- Pre-seal dev diagnostic (the frozen 008-i protection run over all 2,000
  built documents BEFORE sealing, recorded in the identity): NONZERO - 62
  B of 325,892 protected bytes across 8 documents, ALL oracle-refined,
  one documented class (attribution: section 6). Predeclared as-is
  sealing rule APPLIED: the set was sealed and evaluated as-is (a
  mid-round implementation change would break the frozen protection
  identity with no ordered re-freeze path - the 008-h precedent); the
  occurrence is disclosed and is a verdict input, not a BLOCKED trigger.
- Seal: the v4 private root sealed with the committed content-free
  manifest (76 component + 2,000 document + 2,000 label files = 4,076
  files; section 7); strategy's independent data-free pre-evaluation seal
  verification (order requirement 7 hard gate) PASSED before the
  evaluation phase, and the evaluation harness took the strategy receipt
  as a fail-closed input (the citation is recorded in the committed
  evaluation aggregate).

## 6. v4 hidden acceptance metrics (requirement 9)

Committed: mashup-metrics-v4.json + e2e-invariants-hidden-v4.json
(data-free; the raw frozen-harness record is embedded and cross-checked).
Evaluated through the committed 008-f frozen harness (byte-identical to
base, imported unmodified with its identity globals re-pointed to the v4
manifest and the v5 policy of record - the disclosed thin wrapper).

- Hard safety axis (construction-labeled protected bytes exposed, target
  0): **0 of 266,807 B across 2,000/2,000 documents - MET**. Cross-check
  against the raw frozen-harness record reconciles exactly: 325,892
  protected bytes = 266,807 construction + 59,085 oracle-refined, and the
  raw 62 B exposed is entirely oracle-refined (attribution below).
- Oracle-refined protected census (POLICY-COVERAGE CENSUS with the
  DISCLOSED CIRCULARITY LIMITATION - the label oracle is the
  implementation's own residual function, so this axis measures what the
  policy claims to protect, not independent evidence; it does NOT gate
  the verdict): 62 B exposed of 59,085 across 8 documents,
  field-by-field attribution:
  - v4h-000295 6 B, v4h-000559 5 B, v4h-000669 7 B, v4h-000702 6 B,
    v4h-000955 14 B, v4h-001604 5 B, v4h-001626 14 B, v4h-001912 5 B
    (sum 62 B; per-region verification: every exposed byte
    label_source = ORACLE-REFINED; construction-labeled exposed = 0).
  - Mechanism: the shape is yaml-hash-comment-heading - Slovenian YAML
    `# heading` comment lines (e.g. `# naloge`, `# oseba`, `# meritve`,
    `# testni podatki`) that the pulldown-cmark 0.13.4 dialect parses as
    ATX headings, i.e. STRUCTURAL: the heading bytes are NOT candidate
    prose, while the oracle-refined label (the frozen residual spec)
    claims PROTECTED for them - so protection cannot cover the claimed
    bytes and the heading bytes surface as exposed.
  - Pre-existence: this class is DOCUMENTED SINCE v1 (the v1 diagnostic
    sub-shape 348 B + 5 B) and is a named policy-exposed
    dialect-ambiguity class in structural-policy-v4.json carried verbatim
    into v5; the v4 occurrence is the same measured class on the fresh
    pool, sealed and evaluated as-is under the predeclared rule. It is
    the oracle-refined census, not the construction axis: verdict field
    (i) is construction-labeled exposure = 0, MET.
  - Shape-check s2 (a dialect-vocabulary tag name with a non-ASCII
    attribute name - the 60 B v3 shape): 0 TRUE OCCURRENCES on the v4
    pool. The document-level s2 heuristic registered 10 witnesses, all
    DISCLOSED ARTIFACTS of the heuristic itself: angle-bracket email/URL
    tokens (e.g. the `<pošta@primer.si>` address in v4h-000669) where the
    dialect's attribute-scan simulation reports a non-ASCII attribute
    name, but the dialect does not treat the token as candidate prose at
    that position (an autolink-adjacent line; the token bytes are outside
    the parser-approved candidate-prose intersection), so the token bytes
    are fully unprotected in the final protection and sit in
    PROSE-labeled regions - zero exposure. D1-5 fires correctly where the
    measured condition genuinely holds (proven by the dev-edge e02: 0
    exposure under run B, section 4).
- Coordinates: 0/0/0/0 (UTF-8 boundary, byte/cp conversion, source slice,
  patch preservation) - MET.
- Malformed behaviour: 0 violations over the 10 deterministic rows; the
  policy-exposed row totals are independently recomputed from the sealed
  labels with delta 0 (the known frozen-harness first-document
  double-count is explained per row in the committed recomputation block).
- Prose coverage: 96.677 percent of expected prose bytes available (888 /
  8,889 regions completely available); actual losses reported by
  container (S.Emph 56, S.Head 230, S.Item 2,533, S.Para 13,855,
  S.TableCell 129, unknown 27,998) - no arbitrary threshold.
- E2E invariants 1-7: all PASS on 2,000/2,000 replays
  (e2e-invariants-hidden-v4.json, sha256
  8a16f362cca68821bd9f9d9dc04f14c13cef2353d2b529bb801a837915212a3e).
- Determinism: 50-case re-run (v4h-000000..v4h-000049) produced identical
  output.
- Fallback documents: 0.
- Performance (deterministic subset, local evidence only): median
  48.419 / 69.525 / 277.328 ms/document at ~1 KB / ~10 KB / ~50 KB, peak
  RSS 77,012 KB, mode parser-first.

## 7. Seal integrity around the evaluation (requirement 9)

- v4 seal (committed): hidden-manifest-v4.json (sha256
  a702babc239d24251a62f35c1e51f389843c7e7eea8547d64bb325cf16174696) +
  seal-verification-v4.json (sha256
  0c6fa26ed70a3e8642ce76d0a139e77336fc6c4ba0b10a200559f6eeec909dc0):
  4,076/4,076 files (76 component + 2,000 document + 2,000 label)
  hash- and size-verified against the private root; out-of-manifest at
  seal = the 87 pre-seal generation receipts only (disclosed); the
  private seal copy is byte-identical to the committed manifest.
- Strategy INDEPENDENT data-free re-verification BEFORE the evaluation
  phase (order requirement 7 hard gate): VERIFIED - 4,076/4,076 re-hashed
  from disk against the COMMITTED git-blob manifest, manifest sha256
  recomputed equal, seal copy byte-identical (receipt
  strat-verify-tmp/v4seal-result-pre-20260923-011639.json, sha256
  0fba8fcf61924f94adcfb7388bb6b369f2d61b18594f4cfc32129a69b87dbce0, 3,620
  B, cites the v4 manifest sha256; supervision tree, not committed). The
  evaluation wrapper takes this receipt as a fail-closed input and
  refuses to run without it; the citation is recorded in the committed
  evaluation aggregate (v4_identity).
- v1/v2/v3 seals re-hashed around the evaluation (data-free; NO
  re-evaluation of any spent set): v1 4,091/4,091, v2 4,076/4,076,
  v3 4,076/4,076, all VERIFIED before and after; manifest sha256s
  unchanged (8233bdcf.../cf37654a.../ee503bd7...); seal copies
  byte-identical.
- Strategy POST-evaluation all-seals re-verification (recorded in-round
  duty): VERIFIED for v4 + v3 + v2 + v1 (receipt
  strat-verify-tmp/v4seal-result-post-20260923-013553.json, sha256
  833e60b85d0e8e1863f8eb2c93ad76a93a2eeb15d8e51cfe79b7987fbc1fdf8d,
  14,022 B; supervision tree, not committed).

## 8. Naturalistic consistency re-run (requirement 8)

With the frozen 008-i implementation (census in experiment-008i.json), the
70 naturalistic responses (private root; the disclosed adjudication loading
only) were re-run against the EXISTING completed v2 labels (fixed
population; no re-adjudication - the narrow predeclared label-correction
path was not triggered: no frozen label is factually wrong about the
source text). Committed: naturalistic_consistency_v4.py +
naturalistic-consistency-v4.json (data-free).

- 70 cases; 5 material cases (identical to the 008-g five:
  nat-glossary-entry-hidden-03, nat-json-config-hidden-01,
  nat-math-answer-hidden-04, nat-qa-answer-hidden-04,
  nat-table-equations-hidden-03); 6 material ranges.
- 15 exposed ranges - byte-identical (case, range, exposed bytes,
  resolution kind) to the committed 008-g/008-h aggregate: 15/15 resolved
  as DOCUMENTED-DIALECT-LIMITATION with the carried class references
  (markdown-table-dialect-boundary x9, list-item-candidate-prose x2,
  indented-code-lazy-continuation x1, residual-token-line-glue x3);
  0 CLOSED-RESIDUAL, 0 AMBIGUOUS, **0 UNRESOLVED** (predeclared
  expectation met; verdict condition (vii) satisfied).
- Monotonicity: the D1-5 refinement only ADDS protection (it fires where
  the 008-h implementation abstained on the exact measured condition); no
  previously protected byte is unprotected, so no new unresolved range can
  appear - confirmed by the byte-identical exposed set.
- Attribution (carried 008-g heuristic): parser-dialect 12,
  label-ambiguity 2, residual-class-gap 1 - identical to the 008-h
  aggregate.
- Label corrections: none (the narrow path not triggered).

## 9. Final objective-008 verdict (requirement 10)

The eight predeclared fields on the NEW evidence (v4 hidden set +
naturalistic per-case resolution + dev regression):

| # | Field | Result |
| --- | --- | --- |
| (i) | v4 construction-labeled protected bytes exposed = 0 | 0 of 266,807 B across 2,000/2,000 documents - **PASS** |
| (ii) | coordinates 0/0/0/0 | 0/0/0/0 - **PASS** |
| (iii) | malformed behaviour 0 violations | 0 violations; recomputation delta 0 - **PASS** |
| (iv) | e2e invariants 1-7 all PASS on the v4 set | 7/7 on 2,000/2,000 replays - **PASS** |
| (v) | determinism identical (50-case re-run) | identical output - **PASS** |
| (vi) | dev regression PASS (zero new safety exposures; over-suppression within the 1.0 point bound) | 0/0/0/0 on the dev corpus (526,315 B), the 008-g batch, the 008-h batch, and the 008-i batch; +0.0 pp vs the 008-h baseline - **PASS** |
| (vii) | every material naturalistic range resolved (no UNRESOLVED; the 008-g five material cases each carry a resolution kind) | 15/15 DOCUMENTED-DIALECT-LIMITATION, 0 UNRESOLVED, 0 label corrections - **PASS** |
| (viii) | zero model calls beyond the recorded v4 generation budget; zero linguistic-pipeline calls; zero objective-009 touches | 87 of 96 authorized HTTP attempts; 0; 0 - **PASS** |

All eight fields pass. **FINAL OBJECTIVE-008 VERDICT: PASS** - the single
predeclared decision state for "all eight fields pass". The 62 B
oracle-refined policy-coverage census (section 6) is NOT one of the eight
fields: it is the disclosed census with its circularity limitation, one
documented pre-existing class (yaml-hash-comment-heading, documented
since v1), and it does not gate the verdict.

Structural-safety-only classification: this verdict is STRUCTURAL-SAFETY
EVIDENCE ONLY (S-ICA-01/02): it establishes no linguistic quality, no
experimental-MVP acceptance, and no release or deployment authority.

FROZEN DECLARATION (predeclared PASS branch; made ONLY in this final PASS
report, never by the order; grants no release or deployment authority):
the objective-008 protection layer is hereby declared FROZEN at the
008-i census - frozen protection identity = the four-file census in
experiment-008i.json (sha256
24e25edafbbf0b1d67dc405440ea78c0b76a9011c00d5e53c82fe3c00fef6685:
research/curated/prose_boundary.py
c57e2901e52af962546ee83651850e22c91655a8cc822f2c43b1284da16b8c50;
research/curated/protected.py
27f22eaee129190b880851b958a8aa5a38d357cb4e711d70e762b2353d32e78f;
research/prose-boundary/config/structural-policy-v3.json
7ff6646e3c11d1241cb1c85bd96f08e3f0db08e73d4eb4a4e97fac0c17e1828f;
research/prose-boundary/config/structural-policy-v5.json
915f70d34ecc69ab6fea0b273f24ca20f251b6e181bd5e9281c5c012de2fe542),
policy of record = structural-policy-v5.json
(915f70d34ecc69ab6fea0b273f24ca20f251b6e181bd5e9281c5c012de2fe542). The
v4 hidden set is single-use as acceptance (first and only use); the v1,
v2, and v3 sets remain single-use spent.

Judgment debt: D1-1 (008-g mid-document raw-block fix mechanism), D1-3
(008-h composition-contract fix mechanism), D1-4 (008-h
final-round acceptance-criteria design), and D1-5 (008-i xml-fragment
class-boundary decision) were each predeclared to clear ONLY on a final
objective-008 PASS on the v4 set - with that verdict they are CLEARED.
No CRIT.

## 10. Model-call accounting (recorded and classified distinctly)

- Committed v4 generation stream (generation-identity-008i.json):
  87 total HTTP attempts (preflights + component calls +
  retries + bounded recovery), all authorized test-data generation on the
  007-lineage qwen3.8-27b deployment identity (endpoint/credential
  out-of-band; no endpoint value or credential material in any committed
  artifact); budget authorized_total_http_attempts = 96;
  87 <= 96.
- Interrupted prior stream (disclosed, discarded BEFORE any identity
  commit): 88 HTTP attempts (1 initial preflight + 81 component calls + 1
  recovery preflight + 5 bounded K=8 recovery calls; 352 initial
  components; 15 exact pool duplicates across five machine families); the
  run stopped at the final zero-overlap assertion because the first version
  of the recovery bookkeeping appended replacement fragments without
  removing the initial pool duplicates from the recovered family files;
  per the predeclared interruption rule the partial private root was
  removed and the stream regenerated fresh (the committed identity records
  the single committed stream; local evidence: scratch
  008i-v4-gen/gen_v4-run1-failed.log + run1-summary.json, never committed).
- Linguistic-pipeline model calls: 0. Objective-009 touches: 0.
  Label-correction calls: 0.

## 11. Limitations

- Structural-safety evidence only: the PASS verdict establishes no
  linguistic quality, no experimental-MVP acceptance, and no release or
  deployment authority; it is not an acceptance of the Slovenian repair
  system of any kind.
- The oracle-refined protected census (62 B / 59,085 across 8 documents,
  one documented pre-existing class - yaml-hash-comment-heading,
  documented since v1, a named policy-exposed dialect-ambiguity class)
  carries the disclosed circularity limitation (the label oracle is the
  implementation's own residual function) and does not gate the verdict;
  the set was sealed and evaluated as-is under the predeclared rule.
- The document-level s2 shape-check heuristic registered 10
  angle-bracket email/URL token witnesses that are artifacts of the
  heuristic (not true occurrences of the 60 B v3 shape; zero exposure;
  section 6).
- The interrupted first v4 generation stream (88 HTTP attempts, 15 pool
  duplicates) was discarded before any identity commit (section 10); the
  committed generation identity records the single committed stream.
- The naturalistic label evidence is complete (70/70 labels, frozen) with
  line-indexed granularity (disclosed); all 15 exposed ranges carry the
  carried class references; 0 material range unresolved.
- The evaluation runs under the local umask-077 environment; the local
  suite and the CI-parity runs use umask 022 (the contextual-validator
  mode test fails spuriously under 077 - an environmental observation,
  not a round artifact).
- Record-hygiene re-pin before publication (section 2 note): the
  committed shape-analysis file carried a JSON key name ("reference")
  that the committed publication guard denies in committed JSON; the key
  was renamed ("cited_record") under the stop-and-correct procedure and
  the dependent pointers and cited shas were re-pinned; the four-file
  frozen protection census and every measured value are unchanged.
- Objective 009 (fresh human-labelled linguistic confirmation of the
  complete frozen pipeline on a new target distribution) remains the
  separate reserved next objective; its future sample must not contain
  any case generated or inspected for objective 008 (every v1, v2, v3,
  and v4 component, mashup, naturalistic case, dev document, dev batch,
  and dev-edge document).
- PR #9 remains open and unmerged; the merge disposition is strategy's
  separate act after independent review of this round's final head (a
  PASS verdict is the predeclared merge precondition - satisfied; merge
  is NOT performed by this round). No milestone, release, deployment, or
  acceptance-claim language appears in any committed artifact of this
  round beyond these explicit denials.
